import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vvoyager3.qiguang',
  appName: '栖光',
  webDir: 'dist',
  server: { androidScheme: 'https' },
};

export default config;
