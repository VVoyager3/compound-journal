# 栖光：技术架构与数据设计

> 状态：开发前基线 v1.1  
> 适用范围：单用户、单设备、文字记录优先的 MVP  
> 上位文档：[产品规格与验收.md](./产品规格与验收.md)

## 0. 架构结论

首版采用“PWA 本地应用 + 最小 AI 中转接口”：

```text
手机浏览器 / 已安装 PWA
├─ 原生 HTML、CSS、TypeScript
├─ 固定像素房间与短动画
├─ IndexedDB：用户事实数据
├─ Service Worker + Cache API：应用壳离线缓存
└─ HTTPS 请求：仅在用户发起 AI 整理时发送必要上下文
                    ↓
             无状态 AI 中转接口
             ├─ 校验请求
             ├─ 隐去无关元数据
             ├─ 调用模型
             └─ 校验结构化结果后返回
```

核心决定：

1. PWA 优先，不先做 iOS、Android 两套原生应用。
2. 本地优先，不做账户、云同步和多租户后台。
3. 不使用游戏引擎。房间是固定场景，用 DOM、CSS 和精灵图实现。
4. 不先引入前端框架。七个核心页面用 TypeScript 模块、语义化 HTML 和事件委托即可。
5. 用户事实数据放 IndexedDB；Cache API 只缓存应用文件，不保存业务数据。
6. AI 不是数据源。AI 输出先进入候选区，按确认规则写入正式数据。
7. AI 密钥只存在于服务端环境变量，不进入浏览器包、日志或导出文件。
8. 图片、复杂通知、原生壳和同步都在真实使用证明需要后再加入。

## 1. 为什么选择这条路线

### 1.1 与产品形态匹配

栖光的首版是一个固定房间、七个核心页面和少量短动画，不是自由探索游戏。DOM 更适合表单、任务、日历、证据和无障碍；精灵图只负责角色与房间反馈。使用游戏引擎会重复建设导航、文本输入、可访问性和数据界面。

### 1.2 保持单一代码路径

PWA 可以直接在移动浏览器运行并安装到主屏。若封闭使用后确认需要应用商店、原生通知或系统级能力，再用 Capacitor 包装现有 Web 应用；首版不为这个可能性预付复杂度。

### 1.3 保护用户主权

记录、任务、经验和记忆默认留在用户设备。离线时仍能记录、查看历史、反馈任务和结算确定性的本地规则；只有 AI 整理暂时排队等待网络。

## 2. 技术边界

### 2.1 客户端负责

- 页面、导航、房间交互和动效。
- 原始记录的本地保存与版本历史。
- 目标、任务、习惯、状态、经验和复盘数据管理。
- 所有确定性量化：经验结算、等级、习惯动量、每日上限、状态合计上限。
- AI 候选结果的确认、修改、否认和回滚。
- 离线队列、导入、导出、删除和数据迁移。
- 无动画模式、键盘操作、语义化标签和可读替代界面。

### 2.2 AI 中转接口负责

- 验证请求大小、字段和版本。
- 将用户选择的上下文传给模型。
- 要求模型返回固定 JSON 结构。
- 校验并拒绝不合约的输出。
- 返回错误类型和可重试状态。
- 默认不保存日记正文、模型输入或模型输出。

### 2.3 AI 不负责

- 直接修改本地数据库。
- 计算最终经验、等级和动量。
- 在未确认时修改长期目标、领域、原则或记忆。
- 判断医学诊断、危险程度或人格定论。
- 在后台自动上传尚未由用户提交的日记。

## 3. 客户端模块

首版只需要以下模块，不建立额外的抽象层：

| 模块 | 责任 |
| --- | --- |
| `app` | 启动、路由、页面切换、全局错误边界 |
| `db` | IndexedDB 打开、迁移、事务、导入导出 |
| `journal` | 原始记录、日期归属、编辑版本 |
| `analysis` | AI 请求、候选结果、确认与重算 |
| `quests` | 目标、里程碑、任务与反馈 |
| `habits` | 计划日、豁免、动量 |
| `growth` | XP 账本、等级计算、里程碑证据 |
| `reviews` | 日、周复盘和系统候选 |
| `room` | 家具热点、角色动作、精灵动画 |
| `settings` | 领域、权限、提醒、导出、删除、无动画 |

建议项目结构在开始开发时保持为：

```text
src/
├─ app.ts
├─ db.ts
├─ model.ts
├─ rules.ts
├─ pages/
├─ room/
└─ styles/
public/
├─ manifest.webmanifest
└─ assets/
server/
└─ analyze.ts
```

只有当某个文件已经难以独立理解时才继续拆分。不开设通用 `utils`、单实现接口、仓储层或插件系统。

## 4. 数据设计原则

1. **原始记录不可被 AI 覆盖。** 编辑产生版本，保留上一个版本和修改时间。
2. **事实、推断和用户校准分开。** 每条结构化信息都记录来源和确认状态。
3. **账本优于累计字段。** XP 和状态变化先写不可重复的明细，再由明细重算结果。
4. **同一现实行动只结算一次。** 通过结算键阻止日记、任务和习惯重复加分。
5. **删除和否认可重算。** 派生结果不得成为无法解释的黑箱数字。
6. **日期与时刻分开。** 用户日记按本地日期归属，审计时间使用带时区的 ISO 8601 时刻。
7. **所有持久对象有版本。** 数据库升级和 AI 合约升级都必须可迁移或保留旧结果。

### 4.1 通用字段

所有实体至少包含：

| 字段 | 说明 |
| --- | --- |
| `id` | 浏览器生成的 UUID |
| `createdAt` | ISO 8601 时刻 |
| `updatedAt` | ISO 8601 时刻 |
| `version` | 从 1 开始的整数版本 |

按天归属的数据另含 `localDate`，格式为 `YYYY-MM-DD`。不依赖 UTC 日期推导用户当天，避免跨时区错日。

## 5. 核心实体

### 5.1 用户与系统

#### Profile

- 用户称呼、生活分身称呼。
- 当前头像：鹿角头饰男生或牛纹帽双辫女生；切换只改变外观。
- 当前人生章节及开始日期。
- 首选时区、周起始日。
- 冷启动与隐私提示完成状态。

#### Area

- 名称与排序。
- 模式：`build`、`maintain`、`explore`、`pause`。
- 是否为默认领域。

#### SystemMemory

- 类型：偏好、模式、原则、优势、约束。
- 内容、证据引用。
- 状态：`candidate`、`confirmed`、`forgotten`。
- 用户确认或遗忘时间。

#### AppSettings

- 是否允许 AI、发送前是否预览上下文。
- 是否启用减少动画、声音、触觉。
- 晚间提醒时间；首版仅一个提醒。
- 数据保存与导出设置。

### 5.2 记录与事件

#### JournalEntry

- `localDate`。
- 原始文字正文。
- 输入方式：键入、系统输入法语音转文字、导入。
- 编辑版本号。
- AI 整理状态：未提交、排队、处理中、成功、失败。

同一天允许多条记录。每日摘要是这些记录的派生结果，不把多次输入强制合并成一段原文。

#### JournalRevision

- 记录 ID。
- 修改前正文。
- 修改原因：用户编辑、撤销、导入冲突。

#### DailyAnalysis

- 日期、合约版本和模型输出版本。
- 今日标题、摘要、明确心情标签。
- 当日复盘和明日建议。
- 生成时使用了哪些记录与上下文。
- 原始结构化响应，供重新解析和审计。

#### Event

- 日期、标题、说明。
- 来源记录 ID。
- 原文证据片段及字符起止位置。
- 来源类型：`explicit` 或 `inferred`。
- 确认状态：`confirmed`、`pending`、`rejected`。
- 是否可能构成长成长证据。

### 5.3 状态

#### StateObservation

- 日期和维度：体力、心力、连接、推进、玩心。
- 值或变化量。
- 类型：用户自评、事件影响、用户校准。
- 证据 ID、AI 理由和确定程度。
- 是否生效。

最终状态由最近一次已确认基线加之后的有效事件变化重算。同一天每个维度的事件合计限制为 `-15..15`；用户当天校准覆盖 AI 估计，但不删除证据。

#### StateSnapshot

- 日期、五维结果和每一维最后证据时间。
- 是否“待了解”或“需要更新”。
- 生成所依据的观察 ID。

快照用于加速页面读取，可以删除后重建，不是事实源。

### 5.4 目标、任务和习惯

#### Goal

- 结果、意义、完成证据、可选时间范围。
- 主要领域、唯一主要成长分支。
- 角色：主目标、次要目标、愿望库。
- 状态：想法、进行中、暂停、完成、放弃。

#### Milestone

- 目标 ID、描述、验证证据。
- 状态和确认时间。
- 对应的 50 XP 是否已结算。

#### Quest

- 日期、类型：主线、BONUS、支线。
- 来源目标、习惯或恢复建议。
- 标题、推荐理由、最小完成版本、预计分钟。
- 难度：轻量、标准、困难、挑战。
- 主要状态、唯一成长分支。
- 状态：待做、完成、部分完成、跳过、豁免。
- 是否由 AI 建议、是否由用户修改。

每日生成或启用时验证：主线最多 1，BONUS 最多 3，支线最多 2。数据库允许保留历史记录，但首页只显示当天有效任务。

#### QuestFeedback

- 任务 ID、结果、反馈文字。
- 用户实际完成内容。
- 反馈时间和是否撤销。

口述反馈只是系统输入法生成的文字，不存音频。

#### Habit

- 名称、最小动作、计划日或触发场景。
- 主要状态、唯一成长分支、基础难度。
- 是否由用户主动设为 BONUS；只有启用后才在计划日生成 BONUS 任务。
- 状态：启用、暂停、结束。

#### HabitLog

- 习惯 ID、计划日期。
- 结果：完成、部分完成、跳过、豁免。
- 对应任务与结算键。

BONUS 是习惯的任务呈现方式，不建立第二套项目实体。动量按最近 7 个计划日即时计算；不保存“连续天数”，避免中断清零逻辑进入数据层。

### 5.5 成长与复盘

#### GrowthBranch

- 六类根资产之一或用户自定义分支。
- 父分支、名称、排序、是否暂停。
- 当前等级与经验可以缓存，但必须由账本重建。

#### XpLedger

- 唯一 `settlementKey`。
- 来源类型和来源 ID。
- 成长分支 ID。
- 基础 XP、结算比例、最终 XP。
- 难度、日期、撤销状态。

`settlementKey = 现实行动ID + 结算版本`。同一个任务同时出现在日记和习惯时，共享现实行动 ID。写账本与更新缓存必须在同一 IndexedDB 事务中完成。

经验规则固定为：

- 轻量 5、标准 10、困难 20、挑战 40。
- 部分完成乘 0.5 并向上取整。
- 里程碑额外 50。
- 跳过、未完成、暂停为 0，不写负数。

等级由累计有效经验顺序扣除：Lv.0 需 20，Lv.1 需 30，Lv.2 需 40，Lv.3–9 每级 50，Lv.10–29 每级 75，Lv.30 起每级固定 100。算法必须支持一次结算跨越多级。

#### Review

- 周或月的开始、结束日期。
- 状态趋势、收益事件、消耗事件、成长证据。
- 下一周期唯一主题和唯一最小实验。
- 用户确认状态。

#### SystemCandidate

- 来自复盘的偏好、模式、原则、优势、约束、目标调整候选。
- 证据引用、AI 确定程度。
- 用户操作：确认、编辑后确认、暂不处理、忘记。

## 6. IndexedDB 存储与索引

首版 object stores：

| Store | 必要索引 |
| --- | --- |
| `profile` | 无，只保存当前用户一条记录 |
| `areas` | `mode`、`order` |
| `entries` | `localDate`、`updatedAt` |
| `revisions` | `entryId` |
| `analyses` | `localDate`、`status` |
| `events` | `localDate`、`entryId`、`confirmation` |
| `observations` | `[localDate, dimension]`、`evidenceId` |
| `snapshots` | `localDate` |
| `goals` | `status`、`role` |
| `milestones` | `goalId`、`status` |
| `quests` | `localDate`、`status`、`sourceId` |
| `questFeedback` | `questId` |
| `habits` | `status` |
| `habitLogs` | `[habitId, localDate]` |
| `branches` | `parentId` |
| `xpLedger` | `settlementKey`（唯一）、`branchId`、`localDate` |
| `reviews` | `periodStart`、`type` |
| `memories` | `status`、`type` |
| `analysisJobs` | `status`、`nextAttemptAt` |
| `settings` | 无，只保存一条记录 |

不为 MVP 建通用实体表或动态 schema。明确的 store 更容易约束、迁移和排错。

## 7. 确定性规则与重算

所有量化规则放在无副作用的 `rules.ts`：输入普通对象，输出结果，不读页面也不直接写数据库。至少覆盖：

- 难度到 XP 的映射。
- 部分完成向上取整。
- 无限等级计算与连续升级。
- 同日状态变化 `-15..15` 截断。
- 用户校准优先级。
- 7 个计划日动量。
- 每日任务数量上限。
- 相同 `settlementKey` 幂等。

用户否认事件、撤销反馈或修改难度时：

```text
标记原明细失效
→ 从受影响日期重算状态或 XP
→ 更新可删除的快照/缓存
→ 页面展示修正结果
```

不直接“减去当前数字”，以免多次撤销产生漂移。

## 8. 离线、队列与冲突

### 8.1 离线可用

离线时支持：

- 新增和编辑文字记录。
- 查看所有本地历史。
- 完成、部分完成、跳过和豁免任务。
- 习惯动量、XP 和等级结算。
- 修改目标、领域和系统记忆。

### 8.2 AI 队列

用户点击“整理”时先建立 `analysisJob`。无网络或请求失败则保留任务，界面明确显示“已保存在本机，联网后可重新整理”。

首版不做静默后台上传。恢复联网后展示“可重新整理”，由用户点击继续，避免用户忘记某段私密文字将被发送。

每个任务包含：记录版本、合约版本、上下文摘要和幂等键。记录在排队期间被修改时，旧任务失效并要求重新提交。

### 8.3 无多设备冲突

首版没有账户和同步，因此不建设 CRDT、实时协同或服务端冲突解决。导入数据与本地数据发生 ID 冲突时，默认保留两份并标记导入来源，不静默覆盖。

## 9. 数据导出、导入与删除

### 9.1 导出

首版导出一个 UTF-8 JSON 文件，包含：

- `formatVersion`、导出时间和产品版本。
- 所有用户事实、确认状态、账本和设置。
- 不包含 API 密钥、缓存文件或内部错误日志。

另提供按日期导出的易读纯文本属于后续便利功能，不阻塞 MVP。

### 9.2 导入

- 先校验文件类型、大小、版本和必填字段。
- 在内存中完成解析和迁移验证，再开启写事务。
- 导入失败时不留下半份数据。
- 导入前提示用户先导出当前数据。

### 9.3 删除

“删除全部数据”必须二次确认，并说明范围。确认后删除 IndexedDB、Cache API 中的应用缓存和本地设置，再重新打开空数据库。AI 中转接口默认无用户内容可删除；若未来增加服务端存储，必须同时提供服务端删除结果。

## 10. 隐私与安全基线

### 10.1 威胁边界

本地优先不等于自动加密。浏览器的源隔离和操作系统磁盘保护可以抵抗普通网页互相读取，但不能保护已解锁设备、浏览器开发者工具、恶意扩展或 XSS。首版必须把边界写在隐私说明中。

### 10.2 必须实施

- 全站 HTTPS，严格 Content Security Policy。
- 不用 `innerHTML` 渲染用户文字或 AI 返回内容；只写 `textContent`。
- 限制第三方脚本和依赖，不接入广告、行为画像或会话回放。
- AI 请求只包含用户本次选择的记录和明确展示的必要上下文。
- AI 中转接口关闭正文日志；错误日志只保留请求 ID、耗时、状态码和合约版本。
- 服务端设置请求大小、速率、超时和结构校验。
- 导出文件生成后只通过用户主动下载，不自动上传。
- 删除和导入属于高风险操作，必须明确确认并采用原子事务。

### 10.3 本地加密决定

MVP 不做“密钥也存本机”的表面加密，因为脚本能同时取得数据和密钥，无法抵抗 XSS 或已解锁设备。若封闭使用明确需要独立保险库，再增加用户口令派生密钥、Web Crypto AES-GCM、不可恢复提示和自动锁定；在此之前依赖设备锁、浏览器隔离、最小依赖和 CSP。

### 10.4 存储持久性

IndexedDB 默认仍可能因存储压力被浏览器清理。首次形成有价值数据后请求 `navigator.storage.persist()`，无论浏览器是否同意，都持续提供导出提醒。不能把“请求持久化”描述成备份。

## 11. 动画与性能架构

- 房间由一张背景层、家具热点和一个角色精灵组成。
- 角色移动只改变 CSS transform；不做逐像素寻路。
- 家具到角色站位使用固定坐标表。
- 精灵图按动作与方向切换 CSS `background-position`。
- 同一时间最多播放一个角色动作和一个结果反馈。
- 任务完成后先立即更新可读状态，再播放 XP 飞行动画。
- `prefers-reduced-motion: reduce` 或应用内“减少动画”开启时，跳过移动和飞行，只保留即时状态替换与短淡入。
- 所有家具操作另有常规按钮，不让动画成为唯一入口。

性能目标不是游戏帧率，而是：普通移动设备首屏可交互、输入不掉字、页面切换无明显等待。图片使用整数倍像素缩放和 `image-rendering: pixelated`，避免运行时大图滤镜。

## 12. AI 接口边界

唯一首版接口：

```text
POST /api/analyze
```

请求和响应遵循 [AI整理契约与安全规则.md](./AI整理契约与安全规则.md)。服务端只接受已知 `contractVersion`；未知字段默认拒绝而不是忽略，以便发现客户端与服务端不一致。

首版不需要：用户账户接口、同步接口、远程任务调度、消息队列、向量数据库、搜索集群或长期 AI 记忆服务。

## 13. PWA 与部署

应用壳包含 Web App Manifest、图标、启动入口和 Service Worker。Service Worker 使用 cache-first 提供版本化静态文件，导航请求在离线时回退到应用壳；AI API 始终 network-only，不缓存日记请求或响应。

部署只需要：

- 一个支持 HTTPS 和静态文件的站点。
- 一个同源或受限跨域的 `/api/analyze` 函数。
- 服务端密钥与日志策略。

构建输出是静态目录。首版不需要常驻应用服务器或数据库。

## 14. 测试边界

架构必须支持三类最小测试：

1. `rules.ts` 的确定性规则单元测试。
2. IndexedDB 事务、迁移、导入和回滚集成测试。
3. 五条关键用户路径的移动端端到端测试。

视觉动效只测试最终可见状态和减少动画分支，不对每一帧截图。AI 模型表现使用固定样本集评估，不把随机模型输出写成脆弱的全文快照。

## 15. 失败模式与处理

| 失败 | 用户看到什么 | 系统动作 |
| --- | --- | --- |
| IndexedDB 打不开 | 无法安全打开数据 | 停止写入，提供重试与导入入口 |
| 本地保存失败 | 本次内容尚未保存 | 保留输入框正文，不清空 |
| 空间不足 | 存储空间不足 | 不覆盖旧数据，提示导出和释放空间 |
| AI 离线 | 已保存在本机 | 保留待整理状态，不自动上传 |
| AI 超时 | 整理暂未完成 | 可重试，不重复创建结果 |
| AI 格式错误 | 无法采用本次整理 | 保留原文，记录无正文错误码 |
| 用户否认推断 | 已撤销相关影响 | 使观察和结算失效并重算 |
| 导入失败 | 未导入任何数据 | 事务回滚，原数据不变 |
| 新版本迁移失败 | 暂停升级 | 保持旧库，提示导出与重试 |

## 16. 延后决定

以下内容没有真实证据前不设计：

- 账户、云备份和跨设备同步。
- 原生 App 商店发布和 Capacitor 包装。
- 图片 OCR、语音文件和附件云存储。
- 推送通知和后台自动整理。
- 多模型路由、向量数据库和长期服务端记忆。
- 复杂状态管理库、动画引擎和游戏引擎。
- 本地口令保险库。

触发条件写得很简单：十四天封闭使用中出现明确且重复的问题，再为该问题增加最小能力。

## 17. 开发前架构验收

- [x] 技术方案支持七个核心页面和五条关键路径。
- [x] 离线时仍能完成非 AI 核心动作。
- [x] AI 不拥有最终量化和长期系统修改权。
- [x] 所有数值都有账本、来源和重算路径。
- [x] 单设备 MVP 不包含同步基础设施。
- [x] 房间互动不依赖游戏引擎。
- [x] 数据导出、删除、存储清理风险已有明确处理。
- [x] 减少动画与常规操作入口已进入架构要求。

## 18. 官方技术依据

- MDN 的 [Making PWAs installable](https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps/Guides/Making_PWAs_installable) 说明可安装 Web 应用的 Manifest、HTTPS 与浏览器要求。
- MDN 的 [Offline and background operation](https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps/Guides/Offline_and_background_operation) 说明 Service Worker 与离线缓存模型。
- MDN 的 [IndexedDB API](https://developer.mozilla.org/en-US/docs/Web/API/IndexedDB_API) 说明浏览器端结构化、可索引数据存储。
- MDN 的 [Storage quotas and eviction criteria](https://developer.mozilla.org/en-US/docs/Web/API/Storage_API/Storage_quotas_and_eviction_criteria) 说明浏览器存储默认是尽力而为、可能被回收，以及持久化请求的边界。
- MDN 的 [Web Crypto API](https://developer.mozilla.org/en-US/docs/Web/API/Web_Crypto_API) 说明浏览器原生加密能力及其低层级使用风险。
- MDN 的 [`prefers-reduced-motion`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/At-rules/@media/prefers-reduced-motion) 说明如何尊重系统减少动画偏好。
- Capacitor 的 [官方文档](https://capacitorjs.com/docs/) 说明 Web 应用在后续需要时可包装到原生平台；本项目不在 MVP 提前引入。
- Vite 的 [Static Deploy](https://vite.dev/guide/static-deploy) 说明静态构建输出与部署方式。
