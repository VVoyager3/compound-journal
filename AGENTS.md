# 栖光项目工作规则

## 双文档工作基线

- `PRODUCT.md` 是产品定位、设计理念、核心闭环、产品规则、验收标准和明确边界的唯一基线。
- `TODO.md` 只记录已完成、待办、验证结果和工作记录，不再承载产品设计正文。
- 每次开始工作前先完整阅读 `PRODUCT.md`，再从 `TODO.md` 中最高优先级的未完成事项继续。
- 不新建与 `PRODUCT.md` 或 `TODO.md` 重复的产品逻辑、进度或待办文档；需要补充时更新对应基线。

## 文档结构

- 根目录只保留 `AGENTS.md`、`PRODUCT.md` 和 `TODO.md`。
- `docs/archive-unused-md/` 只保留历史视觉与交互参考，不记录开发进度。
- 技术架构、AI 合约和测试基线以实现代码、类型和自动化测试为准，不再维护与源码并行的长篇草案。
- 新建 Markdown 前先确认现有文档无法承载；一次性审查、旧计划和已过时线框不进入仓库。

## GitHub 操作

- 所有与 GitHub 远端交互的操作首选 GitHub CLI（`gh`），包括认证、仓库、远端分支、Pull Request、Issue、Actions、Release 和 API 操作。
- 本地状态、差异、提交和本地分支等纯本地版本控制继续使用 `git`。
- 只有 `gh` 没有对应能力或明确失败时，才使用 `git` 直接执行远端操作，并说明原因。

## 产品逻辑先于功能

- 每次新增、删除或调整产品逻辑，都必须在同一次工作中更新 `PRODUCT.md` 对应的产品定义、核心闭环、规则和验收标准，并在 `TODO.md` 记录进度变化。
- 产品功能必须跟随已经确认并写入 `PRODUCT.md` 的产品逻辑实现，不得让代码先行后再为现有实现补写理由。
- 如果实现需求与现有产品逻辑冲突，先更新并确认产品逻辑，再修改功能。
- 每项功能都应能对应到明确的用户价值、闭环位置和验收标准；无法对应的功能不进入开发。

## 每次工作的开始与结束

开始工作时：

1. 阅读 `PRODUCT.md` 和 `TODO.md`。
2. 核对当前分支、工作区状态和相关实现。
3. 选择 `TODO.md` 中最高优先级且具备实施条件的事项。

完成工作时：

1. 在 `TODO.md` 中记录本次已经完成的内容。
2. 只有实现和必要验证均通过后，才能把任务标记为已完成。
3. 记录仍未完成、被拆分或新发现的 TODO，并保持明确优先级。
4. 如产品逻辑发生变化，更新 `PRODUCT.md`；保证两份基线与实际功能一致。
5. 记录关键验证结果；不能验证的事项保持未完成并说明外部条件。
6. 下次工作继续从更新后的 `TODO.md` 开始。

## 完成定义

一项工作只有同时满足以下条件才算完成：

- 符合 `PRODUCT.md` 中已经确认的产品逻辑。
- 功能实现完整，没有用占位或假数据代替要求。
- 与现有记录、目标、任务、反馈、经验和成长闭环保持一致。
- 通过与风险相称的自动化或真实环境验证。
- `PRODUCT.md` 与 `TODO.md` 已分别同步产品逻辑及完成项、剩余项和验证结果。

测试通过只证明实现没有触发已知回归，不替代产品逻辑闭环审查。
