# 栖光设计验收清单

> `PRODUCT.md` 的“UI 设计理念”是唯一设计来源。本清单只用于检查本次 UI 变更，不另行定义产品逻辑。

## 一、设计方向

- [ ] 视觉仍是“温暖像素生活感 + 克制的新粗野主义”，没有变成通用后台、Material 默认皮肤或另一款游戏的仿制品。
- [ ] 像素人物与房间负责陪伴和游戏感，功能页面负责清晰执行，两部分使用同一套旧纸、林野绿、古铜、暖木和轮廓语言。
- [ ] 新颜色、装饰、卡片或动画具有真实功能；没有通用 AI 紫、霓虹外发光、玻璃拟态、无意义渐变或卡片套卡片。
- [ ] 页面方角、小圆角、边框和阴影规则一致；圆形只用于状态圆、头像、加号等明确语义。

## 二、信息与文案

- [ ] 当前页面只有一个最突出的主决定，详情、依据和高级设置按需展开。
- [ ] 同一事实和操作没有在标题、卡片、按钮、房间与底栏中重复出现。
- [ ] 用户可见文字是直接、自然的中文，不暴露 MAIN、BONUS、RECOVERY、内部字段名或面向开发者的解释。
- [ ] 页面只使用页标题、区块标题、正文、辅助状态四级文字；页面标题只出现一次并左对齐。
- [ ] 同一行或同一组并列组件使用相同的字体级别、高度、内边距、对齐和状态样式，不因文字长短形成锯齿、错位或局部缩放。
- [ ] 按钮、标签和短句没有孤字换行；在目标手机宽度及 200% 字体下，不出现最后一行只有一个字。
- [ ] 非必要说明已经删除；隐私、发送范围、错误原因、删除、导入和不可逆后果的文字完整保留。

## 三、布局与操作

- [ ] 今日、任务、记录、轨迹、设置及全屏二级页保持固定底栏；五个入口同尺寸且仅当前项着色。
- [ ] 普通任务整行完成、计数任务整行增加一次；编辑、删除和次级反馈从当前条目进入。
- [ ] 可见按钮可以小于 44px，但实际触控区域不小于 44px；没有为了放大触控区而制造巨大圆圈。
- [ ] 短上下文操作才使用弹层；长表单使用全屏二级页，内容独立滚动，键盘、操作区和底栏互不遮挡。
- [ ] Android 点击不出现默认蓝色粗框、蓝色阴影或触摸高亮；键盘可见焦点、屏幕阅读器名称和状态语义仍然存在。
- [ ] 待处理、按下、处理中、完成、错误、撤销、空状态和不可用状态都使用同一视觉语言。

## 四、人物与动效

- [ ] 用户可见头像只使用“鱼鱼”和“包包”的统一像素卡通版；素描原稿只作内部身份参考。
- [ ] 人物默认睁眼、正面、温和开心；情绪、庆祝和恢复都有真实上下文，结束后回到稳定姿势。
- [ ] 当前只显示完整居中的人物，等比 contain，无场景、家具热点和行走动画；角色面板可关闭且不点穿。
- [ ] 日历、习惯最近记录、热力图和星期完成率的方格宽高一致，误差不超过 1px。
- [ ] 动画只传达层级、反馈、状态或空间行为；同一时刻不出现多个强反馈，减少动画时功能完整。

## 五、移动端与验证

- [ ] 320、360、390、430px 无整页横向滚动，200% 字体下主要内容和操作仍可辨认。
- [ ] 状态栏、挖孔、软键盘、底部系统导航和应用固定底栏互不重叠。
- [ ] 文本和按钮对比度可读，键盘顺序、可见焦点和屏幕阅读器语义没有回退。
- [ ] 冷启动、加载、空状态和错误状态不使用纯空白或无意义旋转图标。
- [ ] 浏览器回归覆盖业务与响应式；Android Studio 模拟器覆盖 APK、WebView、安全区、键盘和离线启动；设备特有结论留给真机。

## 六、何时重审整套设计

只有导航、品牌语言、人物身份、核心闭环、主要载体或数据与隐私边界改变时，才重新审查全部设计。其他 UI 工作只检查本次命中的条目，并在 `TODO.md` 记录实际验证结果。
