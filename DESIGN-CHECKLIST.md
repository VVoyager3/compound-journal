# 栖光设计验收清单

> 用途：避免每个任务重复讨论已经确定的产品逻辑。日常开发只检查本次变更涉及的条目。

## 一、已锁定原则

- 主闭环仍是：真实记录 → 提取证据 → 用户确认 → 最小行动 → 反馈 → 周期回看。
- 默认页面只有一个主决定；编辑、依据、规则逐层展开。
- 人物是镜子、顾问、教练和见证者，不变成独立养成游戏。
- 不用惩罚、掉血、枯萎或随机负面情绪逼迫使用。
- 用户真实事实与已确认证据优先；AI 输出只能是候选。
- 未联网时普通记录、任务、状态与回看仍可用；只有 AI 调用需要网络。

## 二、人物与动画

- 性别与造型只使用用户原始男/女稿，不重画、不融合、不换身份。
- 选择、对话、回看用原画；房间行为用对应的透明动作表。
- 手机宽度下能看清人物身份与动作，不被房间陈设淹没。
- 动画由明确的状态或操作触发，有明确停止点，不持续循环耗电。
- 普通反馈约 300–800ms，同一时刻只有一个强反馈。
- 系统或应用开启“减少动态”时，跳过行走、环境和成长动画，功能仍完整。

## 三、页面与文字

- 首页优先显示近期状态、人物和今天唯一建议；记录、习惯编辑、依据进入二级页。
- 房间家具不重复底部导航的全部信息，只做直观快捷入口。
- 界面不出现 PPT 式、说明书式和自我表扬式文案。
- 能用按钮、标签、占位符或可视反馈表达的信息，不再增加解释段落。
- 安全、隐私、删除、导入覆盖和 AI 发送范围等高风险文字必须保留。
- 320/360/390/430px 无整页横向滚动，主要触控目标不小于 44px。

## 四、功能闭环门禁

- 首次使用 3 分钟内能完成一条真实记录和一个可选行动。
- 记录、编辑、整理、确认、行动反馈、撤销、日/周回看有自动化回归。
- 断网不自动上传；恢复网络后仍需用户检查范围并确认。
- 浏览器快速回归负责日常迭代；Android 模拟器负责 APK/WebView/离线启动；真机只做最终手感与平台验收。

## 五、什么时候才重审

只有以下情况重新审查全套逻辑：

1. `PRODUCT.md` 的主闭环、人物定位或数据/隐私边界改变。
2. 导航、核心实体或 AI 权限模型改变。
3. 新增一个会并行产生“今天建议”的系统。
4. 开始新的发布阶段（PWA、APK 或服务端边界改变）。

其他任务只记录本次命中条目、测试证据和剩余差距，不重复输出整套产品逻辑。

## 六、当前证据（0.6.9）

- [x] 首页与记录页保持单一主操作，原创人物在 320–430px 可辨认，动作播放一次后停止。
- [x] 任务次要操作、未开始成长方向和高级维护均已渐进展开；首次打开的任务、成长、周复盘页在 390×844 一屏内且无横向滚动。
- [x] 日历、空白日回顾与系统设置已收敛重复空态和说明文字；高级内容默认折叠，高风险数据操作仍保留完整说明。
- [x] 完成行动会触发一次性人物庆祝，并同时留下当日成功证据和成长分支证据；减少动态时不依赖动画表达结果。
- [x] 生活分身单击即可打开；面板只保留一句回应和一个状态驱动主操作，详细依据进入对应页面，设置保持 44px 次级入口。
- [x] 某日整理只把待确认推断、成功证据和明天一步放在第一层；已核对事件、更多复盘及技术范围按需展开。
- [x] 周复盘只展示真实趋势、重复模式、领域投入和实验；无证据项合并为空态，不用八条重复缺失信息制造压力。
- [x] 首页只保留一个人物状态出口；随机环境动作不冒充用户状态，人物标签只响应真实证据和行动 cue。
- [x] 五维状态槽紧接房间直接显示；未知值保持空槽，已知值按真实数值填充，完整证据日期留在点开详情中。
- [x] 房间只常显“日历、状态”两个非底栏信息；记录、任务、成长仍可从家具进入，但不再重复显示文字，默认人物姿态也不悬浮“在场”标签。
- [x] 首页的记录只显示数量与回看入口，习惯只显示名称与打卡；正文、最小动作和编辑操作进入二级页。
- [x] 记录页默认折叠日期并隐藏底栏，只保留正文、快速开头和保存；补记日期、逐日草稿与关闭返回仍可用。
- [x] 目标卡只展开下一步，编辑、里程碑与重新拆解收进“管理目标”；目标创建不再展示教程式说明段落。
- [x] 25 条 Playwright 闭环回归、200% 文字与 44px 触控门禁、`npm run check`、`npm run check:release` 全部通过。
- [x] Android `emulator-5554` 已验证 0.6.9 APK：首页五维槽与人物首层无横向滚动，人物面板主次清晰且设置不遮挡回应，覆盖安装保留记录。
- [ ] 真机后台回收、系统字体、深色模式、小组件尺寸和长时手感只在最终真机批次验收。
