# 栖光：产品逻辑与设计思路

本文档只回答栖光要成为什么、为什么这样设计以及用什么标准验收。已完成、待办、验证结果和工作记录统一保存在 `TODO.md`。

## 1. 产品定义

### 1.1 产品品类

栖光不是普通日记、待办清单、打卡软件或聊天机器人，而是一个本地优先、证据驱动的个人现实成长系统。

### 1.2 一句话定位

> 栖光根据用户的目标、状态和真实反馈，每次明确告诉用户最值得做的下一步，并在长期帮助用户实现多元成长。

### 1.3 核心关系

栖光中的伙伴是用户的电子分身与成长导师：它了解用户、记住经过确认的经历、看见用户容易忽视的变化，在关键时刻指出下一步，并随着用户成长逐渐减少不必要的指导。

伙伴同时承担四个角色：

- 镜子：还原真实经历，区分事实、推断和未知。
- 参谋：综合目标、状态、限制和历史反馈判断方向。
- 教练：把方向缩小为一个能够立即执行的动作。
- 见证者：保存成长证据、有效方法和长期变化。

### 1.4 核心承诺

用户不需要先维护复杂体系，只需要持续提供真实信息。栖光负责：

1. 理解今天发生了什么。
2. 看见用户做成、推进、坚持或照顾好的事情。
3. 判断当前应该推进、维持、恢复还是探索。
4. 给出一个明确、可解释、可执行的下一步。
5. 把任务反馈转化为经验、成长证据和计划调整。
6. 在周、月尺度保持多元发展，而不是每天平均用力。

### 1.5 产品不是什么

- 不替用户决定人生价值或重要目标。
- 不用断签、负经验、角色受罚或任务欠账制造焦虑。
- 不给出没有证据、无法解释来源的权威结论。
- 不一次堆出大量平级建议，让用户重新承担选择成本。
- 不把人物、等级、房间和桌宠做成脱离现实成长的独立游戏。
- 不以提高打开次数或使用时长为最高目标。

### 1.6 易用性与留存原则

功能闭环和易用性不能分开验收。用户只有能够低成本完成“表达—得到下一步—执行—反馈—看见变化”，闭环才真正成立。

产品优先级固定为：

```text
核心闭环成立
> 用户无需学习即可完成
> 系统反馈清晰且可信
> 功能丰富度与表现层扩展
```

留存不依赖连续登录奖励或频繁提醒，而来自四件事：

- 首次使用很快获得真实价值。
- 每次打开都知道下一步做什么。
- 记录和反馈足够轻，不形成维护负担。
- 用户能够感觉到系统正在根据历史变得更懂自己。

任何需要阅读说明、理解内部模型或重复填写系统已经知道的信息才能使用的功能，都视为尚未完成，而不是“高级但完整”。

### 1.7 简化不等于删减能力

栖光采用渐进呈现，而不是通过删除必要能力获得表面简洁：

- 默认流程由系统自动填充，只要求用户确认当前最重要的内容。
- 用户始终可以进入详情，查看系统依据并修改目标、任务、习惯和状态。
- 低频字段放入“调整”或“高级设置”，不占据首次创建流程。
- 系统自动填写的内容必须可见、可改、可撤销，不能形成不可解释的黑箱。
- 已经创建的对象必须支持完整生命周期，不能只能新增、不能修改或结束。

界面复杂度的判断标准不是功能数量，而是用户在当前时刻必须同时理解和决定多少事情。

### 1.8 首版产品边界

首版只验证“表达—得到下一步—执行—反馈—看见变化”是否持续产生价值。保留本地优先、个人 Android 安装包内的 MiniMax 中国区直连、Android 小组件、月度证据视图和数据导出恢复；暂不引入账户、云同步、自建 HTTPS 中转服务、跨设备、应用商店发布、常驻悬浮桌宠、更多装饰系统、季度/年度报告或付费。只有真实模型、真机和留存证据成立后，才重新评估这些扩展。

Microsoft To Do 与小米运动健康等外部数据源属于后续可选连接，不进入首版闭环。接入必须由用户主动授权；外部任务不能成为第二套 XP 账本，身体指标只作为可核对的状态证据，不进行自动诊断，也不默认上传日记或原始健康数据。

## 2. 现实系统的基本对象

| 对象 | 回答的问题 | 例子 |
|---|---|---|
| 人生领域 | 我希望在哪些方面发展 | 健康、事业、关系、财富、兴趣 |
| 目标 | 我希望获得什么明确结果 | 十二周后连续跑完五公里 |
| 完成证据 | 怎样证明目标真的完成 | 一次连续五公里记录 |
| 里程碑 | 目标路径经过哪些阶段 | 连续三周每周运动三次 |
| 习惯 | 哪些行为需要稳定重复 | 每周二、四、六运动 |
| 任务 | 我现在具体要做什么 | 步行与慢跑交替十五分钟 |
| 状态 | 我今天有多少行动能力 | 精力低、心理一般、连接稳定 |
| 成长证据 | 我实际获得或表现出了什么 | 第一次连续跑完两公里 |
| 系统记忆 | 什么方法和限制对我长期有效 | 下班直接换衣服更容易开始 |

必须保持以下边界：

- 这些对象是系统内部的组织方式，不等于用户创建目标时需要填写的表单字段。
- 默认界面使用“生活分类、提升方向、当前投入、今天要做、习惯打卡、阶段目标、经验、状态自评”等直白称呼；MAIN、BONUS、支线等优先级只保留为内部调度语义，不在今日任务列表中分组展示。
- 人生领域是用户可自定义的发展方向。
- 五维状态是近期传感器，不是人生目标，也不要求每天填写。
- 目标定义结果，里程碑定义路径，任务只定义当下动作。
- 习惯负责重复行为，不自动等同于某个长期目标。
- 成长证据必须来自记录、任务反馈、里程碑或用户确认。
- 系统记忆只保存用户能够查看、修改、拒绝或删除的内容。

## 3. 核心闭环

### 3.1 总闭环

```text
用户确定价值与目标
→ 系统帮助澄清并拆解路径
→ 状态和现实限制决定今天能做多少
→ 系统明确推荐一个 MAIN
→ 任务进入简单看板与伙伴小组件
→ 用户执行并勾选或口述反馈
→ 系统结算经验并形成成长证据
→ 系统更新对路径和用户的理解
→ 自动调整下一步
```

只有当本次反馈能够有理由地改变下次建议，这条闭环才算成立。

### 3.2 每日闭环

1. 打开“今日”后立即看到一个明确推荐，而不是先选择页面。
2. 推荐包含做什么、为什么现在做、完成标准、最低版本、预计时间和关联目标。
3. 用户可以接受、缩小、替换、暂不做或指出方向错误。
4. 接受后的 MAIN、BONUS 和支线进入简单任务看板。
5. 用户可以直接勾选完成；需要补充时可以口述“完成、部分完成、今天不做或豁免”。
6. 系统即时结算经验，提供撤销入口，并形成成功或阻力证据。
7. 结束今天时只保留关键事实、成功记录、未解决阻力和下一步。

### 3.3 周期闭环

- 每周判断各领域处于重点推进、稳定维持、恢复修复还是观察等待。
- 每周检查目标是否推进、习惯是否需要降难度、触发条件是否有效。
- 每月检查多元发展是否失衡，而不是要求每天照顾所有领域。
- 当证据显示路径无效时重新拆解目标；当目标不再重要时允许暂停或结束。

## 4. 目标自动拆解

### 4.1 用户输入

创建目标时，唯一必填内容是一句自然语言：

> 你想让什么事情发生？

用户可以直接输入或口述，例如“我想在三个月内跑完五公里”。为什么重要、时间范围、每周投入、当前基础和现实限制全部是可选补充，不得阻塞创建。

“关注领域”“唯一成长分支”“目标位置”等内部字段不得出现在默认创建表单中。系统根据目标内容和现有目标自动生成建议；无法可靠判断时允许暂不归类，不要求用户先理解产品模型。

如果目标过于模糊，大模型最多提出一个真正必要的澄清问题；能够合理假设时直接生成可编辑草案，并明确列出假设。没有连接 AI 时仍可先保存目标，并让用户只填写一个手动下一步。

### 4.2 大模型输出

一次拆解必须形成结构化候选：

1. 更清晰的目标结果。
2. 可验证的最终完成证据。
3. 2—5 个有先后关系的里程碑。
4. 当前阶段和当前阻塞点。
5. 一个可立即执行的下一步。
6. 下一步的完成标准、最低版本和预计时间。
7. 关键风险、假设和需要用户确认的地方。
8. 所属生活领域、主要成长方向和当前优先级的后台建议。

### 4.3 权限与落库

- 大模型只生成候选，不直接修改正式目标。
- 第一次确认只突出目标结果和当前下一步，不要求核对所有内部分类。
- 用户可以编辑目标结果、里程碑和下一步。
- 用户确认“保存目标”后才写入目标结构；保存本身不暗中创建今日任务。
- 需要立即推进时，用户从目标详情点击“放入今天”，在已填好目标下一步的任务表单中再次确认日期、时长和完成标准；选择“以后再做”的目标不提供该入口。
- “立即下一步”只是推进动作；除非它的完成标准与某个里程碑证据明确等价并由用户确认，否则不能直接绑定该里程碑，更不能因完成一个更小动作提前结算 50 XP。
- 大模型不能自动启用 BONUS、修改目标优先级或改变截止时间。
- 所属领域和成长方向可以暂不归类，之后由系统建议或用户在高级设置中修改。
- 如果已经存在主目标，系统用自然语言询问“替换当前重点、作为次要方向，还是先保存”，不展示抽象的目标位置字段。

### 4.4 动态重规划

- 连续完成：进入下一动作或下一里程碑。
- 部分完成：保留有效进展，判断继续还是缩小。
- 多次未开始：识别时间、能力、环境、情绪或目标意愿问题。
- 状态过低：生成恢复任务或最低版本。
- 路径被证伪：提出重新拆解候选。
- 目标失去意义：建议暂停、修改或结束，但不得自动处理。

## 5. 今日方向与任务决策

### 5.1 决策输入

- 用户明确设置的目标优先级。
- 截止时间和里程碑依赖。
- 距离上次推进的时间。
- 最近任务反馈和反复阻力。
- 当天状态、可用时间和用户限制。
- 已启用 BONUS 习惯。
- 各人生领域近期投入与失衡情况。
- 用户确认过的有效方法和禁用建议。

### 5.2 决策顺序

1. 是否存在必须优先处理的恢复或安全问题。
2. 是否存在临近截止或阻塞其他工作的目标。
3. 当前重点目标的下一个未完成里程碑是什么。
4. 哪个动作是当前最小且真正可执行的推进。
5. 用户今天是否有能力和时间承担。
6. 其他领域是否需要最低限度维持。
7. 最近反馈是否要求改变原计划。

### 5.3 每日输出

- 1 个 MAIN：今天最值得完成的行动。
- 最多 3 个 BONUS：用户主动启用的维持习惯。
- 最多 2 个支线：可选、低压力、不与 MAIN 竞争的事项。
- 必要时 1 个恢复方案：用于替代不适合当前状态的行动。

每个 MAIN 必须展示：

```text
今天的方向
现在的下一步
为什么是现在
完成标准
最低版本
预计时间
关联目标与里程碑
完成后准备怎样继续
```

系统不默认展示三个平级建议。用户可以主动要求替换，但系统必须先承担推荐责任。

当前实现先用本地确定性优先级决定入口：恢复能力不足时优先恢复；已有待完成 MAIN 时优先真正仍需行动的主线；否则使用当前重点目标、上一日最小步骤，最后才回到记录探索。已完成、部分完成、跳过或豁免的旧 MAIN 只能作为历史证据，不能遮住新的待完成 MAIN。每个入口都保留可见理由，AI 只负责候选内容，不绕过每日上限。

## 6. 简单任务看板与经验结算

### 6.1 看板目标

任务看板服务于执行，不承担复杂项目管理。用户打开后应立即看见当前任务，而不是先理解系统结构。默认只分为两个用户能理解的清单：

- 今天：今天待做和今天已反馈的具体行动。
- 计划：目标、里程碑、习惯和之后已安排的行动；目标与习惯可以继续生成某一天的任务。

每日执行区使用一个可拖动排序的“今天要做”任务清单，不再向用户区分“今日重点”和“其他任务”；今日计划习惯使用独立的“习惯打卡”清单，直接显示当天已打卡次数与所需次数。已完成任务折叠为历史，内部优先级、来源、经验和完成标准进入详情。

待完成任务必须排在已反馈历史之前；今日页、人物指导、任务板和小组件必须引用同一个待完成 MAIN，不能各自用不同排序选任务。

### 6.2 任务操作

- 普通任务的整行是主操作：待完成时显示小尺寸空心圆，点击一次完成；完成后改为勾选、标题划线，并立即结算经验。
- 计数任务的整行是一次打卡：点击一次只增加一个真实进度；背景色按完成比例逐步填充，达到目标次数时完成并结算唯一一次经验。
- 每行只保留标题、必要的次数进度或截止事实；来源、最低版本、预计时间、完成标准和调整操作统一放入右侧详情入口，不在主行重复。
- 普通任务不显示“完成”或“有进展”文字按钮；左侧方框与任务主行承担完成操作，右侧省略号承担查看与编辑。用户可以拖动待完成任务调整当天顺序，刷新后仍保持该顺序。
- 今日习惯复用计划页的打卡组件；一次型习惯显示“打卡”，计数型习惯显示当天 `已完成/所需次数`，每次点击只增加一次，达到当天目标后才完成。
- 任务页不提供脱离对象的“管理”模式或批量选择；任务、目标和习惯的编辑、删除只放在对应条目或详情中，用户先确认对象再操作。
- 任务详情与修改弹层在可用视口内居中展示；键盘弹出时继续相对剩余视口居中并允许内部滚动，不退回贴底抽屉。
- 修改任务默认使用“任务名称、做个更小版本、换一个任务、改到明天”等直白表达；系统事实和记忆只在用户主动展开“为什么这样建议”时展示，不把内部术语放在首屏。
- 普通任务和计数任务完成后都提供短时间可见的撤销入口，防止误触；计数误触也能在详情中减一次。
- “今天不做、豁免、缩小、替换”等低频处理放入任务详情或次级入口，不挤占主看板；“有进展”不作为待完成任务的快速操作或列表状态展示。
- 勾选完成不强迫填写说明；用户愿意时可以口述补充实际结果。
- 已完成任务不自动重复结算，修改反馈后按账本差额重算。
- 待处理任务可以编辑或删除；删除由习惯自动生成的当日任务时保存软删除标记，刷新后不得重新生成该实例，也不得删除既有反馈、经验或习惯历史。
- 安排任务时可以选择计划日期；未来任务在“之后已安排”中可见，到达计划日后进入当天清单。
- 任务和习惯都可以选择“完成一次”或“计数打卡”。计数任务每次 `+1` 只增加真实进度，允许 `-1` 纠正误触；达到目标次数时才完成任务并结算唯一一次经验，不能按点击次数重复发放经验。
- 习惯的次数目标按计划日生成独立 BONUS，例如“每天喝 8 杯水”每天从 `0/8` 开始；错过后不补课、不跨日继承欠账。
- 快速添加普通任务只保存用户填写的标题和计划日期，不伪造“临时任务”、五分钟、最低动作等信息；用户主动补充的计划信息只在详情中展示。
- 新建习惯默认从符合频率的计划日开始生成 BONUS，创建确认即表示用户主动启用；用户可以在创建时关闭，之后也可以暂停或移出每日任务。

### 6.3 经验规则

- 轻量任务完成 5 XP，部分完成向上取半。
- 标准任务完成 10 XP，部分完成向上取半。
- 困难任务完成 20 XP，部分完成向上取半。
- 挑战任务完成 40 XP，部分完成向上取半。
- 里程碑首次确认完成额外奖励 50 XP。
- 已撤销的里程碑再次由用户确认完成时，应恢复唯一一笔有效的 50 XP，并把结算日期更新到真实重做日期；不能出现“里程碑显示完成但经验仍被撤销”的状态。
- 页面展示可获得 XP 的恢复、周实验和其他系统任务，完成后必须按同一难度表真实结算；如果某类行动不结算 XP，页面不得展示虚假 XP。
- 任务创建、编辑、连续登录和重复勾选不产生经验。
- 跳过、豁免、暂停、中断和撤销不扣为负数。
- 等级无上限；Lv.30 后每级固定需要 100 XP。
- 升级时先展示现实成长证据，再展示数字变化。

价值顺序必须保持为：

```text
现实证据 > 伙伴的具体反馈 > 能力与方法变化 > 经验数字
```

### 6.4 修改与状态更新

系统自动生成目标或任务以后，用户仍拥有完整控制权。

任务详情支持：

- 修改标题、完成标准、最低版本和预计时间。
- 调整难度、日期和 MAIN/支线位置。
- 查看并修改关联目标、里程碑和主要成长方向。
- 更新为待完成、完成、部分完成、今天不做或豁免。
- 缩小、替换、顺延、撤销结算或重新打开。

目标详情支持：

- 修改目标结果、原因、完成证据、里程碑和下一步。
- 调整当前重点、次要方向或暂存状态。
- 更新为构想、进行中、暂停、完成或结束。
- 请求系统基于新情况重新拆解，但正式变更仍需用户确认。

习惯详情支持：

- 修改名称、最低版本、频率和触发条件。
- 启用或移出 BONUS。
- 更新为培养中、暂停或结束。
- 保留历史反馈和经验账本，不因修改而删除过去证据。
- 暂停、结束或移出 BONUS 后，当天已经生成但尚未反馈的习惯任务应立即无惩罚收束；暂停期和创建前日期不进入动量欠账，频率变化不能改写旧计划事实。

状态更新不要求用户重新创建对象；所有修改都应保留审计与撤销所需的版本信息。

### 6.5 未按计划完成、晚完成与执行失败

系统必须区分“计划日期”和“硬截止时间”：

- 计划日期表示希望在哪一天行动，可以无惩罚调整。
- 硬截止时间表示错过后会产生现实后果，只在用户明确提供时存在。
- 未在计划日期完成不自动等于失败，也不扣经验。
- 超过硬截止时间后完成，应记录为“已完成，但晚于截止”，并评估对目标路径的影响。

计划日期结束后，未反馈的普通任务和目标行动进入“待决定”，而不是自动失败、自动顺延或永久堆积。用户下次回来时，伙伴只询问一次：

> 这一步还没有结算，实际情况更接近哪一种？

默认选择：

- 已经完成：记录真实完成日期；如晚于截止，保留晚完成事实。
- 做了一部分：保留真实进展，并建议继续或缩小。
- 继续推进：由系统生成更小版本，再由用户确认是否安排到今天。
- 不再做：记录放弃、目标变化或已经失去意义，不产生惩罚。
- 外部原因：记录豁免，不把不可控情况解释为执行力问题。

失败后的重规划规则：

- 第一次未开始：优先提供缩小、顺延或放下，不追问复杂原因。
- 同一行动第二次未开始：询问一个必要阻力问题，例如任务过大、时间不足、精力不足、依赖未完成、忘记或方向不认同。
- 同一路径反复失败：停止重复生成同一任务，提出更换路径、暂停目标或重新拆解候选。
- BONUS 习惯错过后退出当前行动面，只在原日期保留“未记录”的历史事实，用户可以从当天回顾补记；它不进入“待决定”，也不顺延、不补课、不形成欠账，下个计划日生成新的独立任务。
- 旧 MAIN 未处理时不直接复制到今天；先处理一次，再决定继续、缩小或放下。

经验与证据规则：

- 晚完成的真实任务仍按实际难度获得正常经验，不设置迟到扣分。
- 经验和成功证据归入真实完成日期，计划历史保留原计划日期。
- 未完成、跳过、放弃和豁免不产生经验，也不产生负经验。
- 及时性作为独立事实和模式证据，不通过额外 XP 奖励诱导用户虚报完成时间。
- 系统只有在多次出现同类证据并经用户确认后，才能形成关于时间安排或执行方式的长期记忆。

## 7. 多元发展逻辑

### 7.1 长期平衡，短期聚焦

多元发展不等于每天在所有领域平均用力。系统采用四种领域策略：

- 重点推进：当前阶段集中资源增长。
- 稳定维持：使用低成本习惯保持已有水平。
- 恢复修复：先处理已经影响生活和执行能力的问题。
- 观察等待：暂不行动，但保留信息和重新评估时间。

每天只需要一个主要推进方向；多元平衡在周、月尺度上判断。

### 7.2 任务类型

- 推进任务：直接接近目标或里程碑。
- 维持任务：避免已有成果退化。
- 恢复任务：恢复精力、秩序或行动能力。
- 探索任务：在方向不清楚时获得信息并减少不确定性。

系统不能把“完成更多”当成唯一进步。休息、拒绝不合理需求、暂停错误方向和完成必要恢复都可以形成成功证据，但不为了安慰而虚构进展。

## 8. 成功日记与成长证据

成功日记不是独立打卡模块，而是整个系统的正向证据层。成功可以来自：

- 完成了具体行动。
- 推进了尚未完成的事情。
- 在困难中保持了最低行动。
- 识别并修正了错误方向。
- 照顾了身体和心理状态。
- 建立了边界或寻求了帮助。

系统不强制每天凑够数量，也不把推断当成成功事实。成功证据必须进入未来决策，例如引用过去有效的启动方法、发现持续进步的能力，或调整不适合的任务难度。

记录页用“成功记录、发生的事情、感受、想记住”作为快速开头，并显示当前选择的小标题；不再增加“设为成功记录”复选框。只有用户点击“成功记录”时，这条原始记录才带有成功类型；正文偶然出现相似措辞不能自动改类。AI 可以提出成功证据候选，但仍需遵守事实与确认边界。

一次保存只新增一条记录并进入当天回顾；同一天允许任意多条记录，回顾时按稳定时间顺序展示并支持新增、编辑、查看版本和删除。今日页最多展示三条单行摘要，点击整行进入记录详情，“查看今天”进入完整列表。记录类型使用独立元数据保存并可在编辑时修改或撤销。

记录页只保留记录类型、今日一句和正文，不提供序号工具或图片入口。正文草稿按日期与类型同步保存在本机；切换主页面或返回记录页时自动恢复，只有成功保存后才清除。

### 8.1 徽章的产品角色

成就是已经由现实证据证明的成长节点，徽章是成就的像素化纪念。二者不建立第二套事实、积分或任务系统，只回答一个问题：

> 哪些事情已经被真实证据证明，值得我记住？

成就采用多来源、强证据的纯派生规则；每项成就对应一枚徽章，不增加独立 Badge/Achievement 数据表或备份字段：

- 里程碑成就 `milestone:<id>`：里程碑已确认完成且 50 XP 已结算，一项里程碑一枚。
- 目标章节成就 `goal:<id>`：目标由用户标记完成，至少有一个已结算里程碑，且当前路径的全部有效里程碑均已完成。
- 习惯沉淀成就 `habit:<id>:<门槛>`：同一习惯在 1、3、7、14、30、60、100、180、365 次真实完成时分别留下纪念；不要求连续，不因暂停或结束消失。
- 恢复能力成就 `recovery:<门槛>`：在 1、3、7、14、30、60、100 个由用户确认的恢复行动时分别留下纪念；跳过、豁免和未反馈不计入。
- 实验实践成就 `experiment:<reviewId>`：周复盘已由用户确认，并且对应最小实验已经实际完成且留下反馈。

这些来源可以持续产生很多不同成就，但不能靠登录、打开页面、创建对象或 AI 推断解锁。能力、方法和成功日记仍是证据来源，不直接授予徽章；只有它们进入上述可核对现实行动后才形成成就。

### 8.2 解锁、撤销与展示

- 解锁时先说明“你具体做到了什么”，再显示徽章；徽章不代替成功证据和伙伴的具体反馈。
- 解锁反馈使用一次轻量、非阻塞提示，提供“查看证据”，不打断下一步、不强迫分享。
- 来源事实被撤销时，对应成就同步停止展示；这是事实校正，不是惩罚。里程碑事实仍成立时，目标暂停、结束或更换路径不会抹去其历史成就。
- 成就不额外奖励 XP；原任务与里程碑 XP 仍只结算一次，避免同一事实重复奖励。
- 成就册放在“轨迹 → 成长证据”内，不新增主导航。摘要展示最近获得的三枚、总数和来源分类，展开后查看全部；只显示已经获得的成就，不展示灰色锁定图鉴。
- 详情按来源展示名称、获得日期、证据、目标或习惯、成长方向、来源行动和确认方式，不能只展示图案。
- 成长证据页可以显示一个透明的“下一项可达成成就”，仅报告已存在习惯或恢复行动距离固定证据门槛还有多少，不设截止时间、不进入今日页、不改变任务优先级。
- 房间继续只显示克制的纪念物数量，并在 1、3、8、20 项真实成就时形成四个陈列阶段，不铺满徽章；桌面小组件不增加成就提醒。

视觉使用统一的 48—64px 像素珐琅章语言：固定外轮廓保持识别一致，里程碑使用奖杯、完整目标使用靶心与勾、习惯使用打开书本、恢复使用爱心、实验使用羽毛笔与墨水瓶；颜色只表达来源，不表达稀有度。摘要只显示可完整读出的短标签，完整成就名称和证据保留在无障碍名称与详情中。减少动画时直接显示静态徽章；屏幕阅读器读出成就名称和证据摘要。

成长页所有有业务含义的图标使用独立透明像素资产，不复用抽象方块或家具代替概念：身体状态为浇水与新芽，做决定为带勾的路牌，学习能力为算式黑板，人际关系为对话人物，创作产出为完成页与铅笔，生活自主为打开的门与钥匙。图标在实际手机尺寸必须仍能一眼识别。

### 8.3 反成瘾与可信边界

- 不为登录、连续打卡、创建任务、打开页面或累计点击发放成就。习惯与恢复门槛只统计已经确认的现实行动，不要求连续，也不制造补签压力。
- 不设置普通、稀有、传奇等稀有度，不显示尚未获得的灰色图鉴、完成百分比或全收集压力。
- 不设置限时徽章、随机掉落、隐藏条件、排行榜、分享奖励或徽章兑换货币。
- 恢复、求助、建立边界和停止错误方向都可以通过恢复行动或目标里程碑形成成就，不能只纪念高产出。
- 离线状态可以从本地证据完整计算和查看成就；AI 不可用不影响成就册。
- 成就永远不能改变每日任务优先级、难度、经验结算或伙伴对用户的判断。

## 9. 伙伴、房间与桌面小组件

### 9.1 伙伴成长阶段

- 初识：多倾听、少判断，明确说明信息不足。
- 熟悉：引用多次出现且有证据的模式。
- 默契：复用用户确认有效的方法，并提前识别常见阻力。
- 成长：减少不必要指导，让用户自己完成已经掌握的判断。

关系阶段不能仅由登录天数决定，应由真实记录、用户确认和方法稳定性决定。

### 9.2 App 内伙伴互动

- 早上解释今天为什么选择这条主线。
- 用户记录时进入倾听状态。
- 用户卡住时帮助判断阻力并缩小行动。
- 完成后指出具体成功，而不是只说“做得很好”。
- 晚上整理事实、成功证据和下一步。
- 每周指出有证据的模式与领域失衡。
- 用户中断后欢迎回来，不追究断签，不堆积欠账。

人物与房间的默认视觉遵守以下规则：

- 两位生活分身只使用正式名字：女生为“鱼鱼”，男生为“包包”；选择与设置界面不再用头饰、性别或外观描述代替名字。
- 伙伴默认是睁眼、温和开心的正面表情；闭眼只用于明确的平静或休息状态，没有真实事件依据时不显示哭泣、委屈或沮丧。
- 伙伴停留、倾听和说话时尽量正面对着用户；侧面和背面只用于实际移动或与家具交互，动作结束后回到正面。
- 步行采用短促、可读的离散格步：抬脚、前进一小格、落地和短暂停稳使用同一节拍。点击家具时优先把路线拆成水平与垂直两段，每一段的画面朝向必须与实际位移一致；人物在拐点停稳后才能换向，到达后再面向家具。动作帧需在移动前准备完成，不出现倒着走、空白闪烁、连续漂移、瞬移、人物拉伸或旋转。散步包含停留和回正面的节奏。
- 人物闲置且没有任务反馈、移动或家具交互时，可以间隔随机做一次打招呼、放松、思考或阅读等短动作；短动作必须互斥、自然回到正面开心状态，不能连续抽动，也不能在某日快照中播放。
- 庆祝、思考、恢复等表情只响应已经发生的真实事件，结束后回到温和开心状态；恢复不是受罚，也不表现为无缘由的哭泣。
- 房间保持可理解的俯视生活空间：家具沿边缘形成记录、任务、轨迹、成长和状态区域，中部留作清晰可行走区；热点与图案位置一致。
- 房间只保留能被用户理解的家具、人物状态和有真实里程碑依据的纪念物；习惯成长由任务与成长证据页表达，不在房间底部放置无说明、无交互的花盆或装饰性色块。
- 视觉语言采用原创的温暖荒野冒险感：林野绿、旧纸、古铜、暖木和少量金色微光贯穿页面与房间，配合克制的纸张纹理、方向光和边缘暗角；不得复制现有游戏的角色、标志、界面或场景资产。
- 所有用户可见的伙伴头像统一使用由用户原创身份参考转化的暖色像素卡通版，并与房间人物保持相同头饰、发型、表情和色彩语言；素描原稿只保留为内部身份参考，不进入运行界面、桌面小组件或可点击原型。
- 点击生活分身只展开一个轻量指导面板；面板内部点击继续交互，点击面板与人物触发点以外的任意区域立即收起，并同步辅助技术可读的展开状态。
- 家具互动统一遵循“沿房间路线走近—到达与转向—使用姿势—一句明确反馈—进入对应功能或回到房间”的短链路：书桌与椅子进入记录，任务板进入任务，日历进入轨迹，工作台进入成长证据，窗边进入状态；床只提供一次不改变任务、经验或状态的短暂休息，并让人物沿原路径回到房间中央。人物必须停在家具旁的开放地面，交互姿势只使用纯人物帧，不把自带桌子、任务板等场景的复合插画再次叠进房间。
- 房间依靠像素场景、家具层次、人物动作和克制反馈建立游戏质感，不在场景上叠加常驻黑色外框、热点标签框或按钮框；鼠标悬停和键盘焦点仍需给出明确可见反馈。
- 家具被选择时只短暂显示与场景融合的金色光环和微光；人物移动包含起步准备、按脚步换帧、落脚阴影和到达停稳，不用持续闪烁或无限循环装饰制造游戏感。
- 冷启动和本地数据初始化期间必须立即显示与页面一致的静态主题占位，数据就绪后直接进入真实页面；不得以空白屏幕代替加载反馈。
- 减少动画开启时，人物稳定显示正面开心姿势，不播放持续移动。

### 9.3 今日任务小组件

第一阶段按 Android 桌面小组件实现，不申请常驻悬浮窗权限，也不建立独立 PC 桌宠进程。

小组件只承担“看见并完成今天的任务”，不复制 App 首页、伙伴面板或成长页。提供三个尺寸：

- 小尺寸：显示优先级最高的一项待完成任务和完成勾选。
- 中尺寸：显示最多两项待完成任务及剩余数量。
- 大尺寸：显示最多四项待完成任务及剩余数量。

任务顺序与 App 任务板一致：MAIN 优先，其次是当天计划的 BONUS，最后是可选支线。已经反馈或不属于今天的任务不占据组件；当天没有待完成任务时只显示完成状态。

核心交互：

- 点击任务正文打开 App 内对应任务。
- 点击任务前的完成勾选，复用同一任务账本结算经验并进入可撤销的任务板。
- 点击标题或剩余数量打开今天的任务板。
- 用户可在设置中主动把今日任务小组件添加到系统桌面；系统已添加时不重复请求，系统不支持时不展示无效入口。
- 今日任务变化后及时刷新，不依赖高频后台轮询。
- 已打开 App 接收小组件动作时只分发一次性原生事件，不重载 WebView，不打断尚未保存的记录或编辑。

隐私与边界：

- 小组件不展示伙伴头像、人物状态、经验进度、记录入口、日记正文、心情原文、系统记忆或成就提醒。
- 用户可以选择隐藏任务标题，仅显示今日待完成数量。
- 锁屏或他人可见场景不展示敏感目标内容。
- 离线时读取本机今日任务快照；AI 不可用不影响查看与勾选。

### 9.4 后续桌宠形态

Android 常驻悬浮桌宠、Windows/macOS 桌宠和跨设备同步只有在桌面小组件验证了使用价值后再决定。后续桌宠仍必须服务于当前任务、记录和伙伴反馈，不能变成独立养成游戏。

## 10. AI 与规则系统分工

### 10.1 大模型负责

- 理解自然语言目标和记录。
- 生成目标拆解候选。
- 识别可能的阻力、模式和成功证据。
- 把规则选择出的方向表达为清晰、自然的指导。
- 在证据不足时说明不确定性或提出一个必要问题。

### 10.2 确定性规则负责

- 每日任务数量上限。
- 目标、里程碑和任务生命周期。
- 优先级、截止时间、状态保护和冲突处理。
- XP 结算、撤销、去重和重算。
- 小组件状态同步和离线任务操作。
- 数据权限、版本、证据引用和用户确认。
- 防止模型自动修改目标、记忆、习惯和正式任务。
- 可选状态候选只有在维度、方向、强度、数值与理由相互一致时才能进入确认流程；无法证明的候选直接丢弃，不用系统猜值修补后影响状态。
- 单日反例只能作为 `observe` 候选；明确完成当前目标的可检查交付物只能形成待确认里程碑候选，两者都不能直接改记忆、完成目标或结算 XP。
- 模型输出不符合 JSON 或合约时最多有界修正三次，修正请求保留上一份原始输出；仍不合格时失败关闭，本地记录与执行闭环继续可用。

### 10.3 用户主权

- 用户拥有目标、价值和最终解释权。
- 重要判断必须可查看依据。
- 事实、推断和未知必须分开。
- 用户可以纠正、拒绝、删除和关闭个性化记忆。
- 个人 Android 可使用安装包内的 MiniMax 密钥，也可在设置中选择支持模型并保存或清除本机自定义密钥；自定义密钥只交给原生请求层，不在界面回显，也不进入导出备份。
- Android 系统云备份和设备迁移不得自动复制日记、目标、任务、记忆或本机密钥；跨设备带走数据只通过用户主动导出和导入。
- 周复盘默认发送合约允许的全部相关事实摘要，但始终不发送整周日记原文。用户可以在设置中一次性关闭事件、状态、任务、习惯、成长、目标、实验或记忆类别；选择长期保存，每次周复盘只展示只读摘要并确认生成，不再逐项重复勾选。
- AI 未配置、离线或不可用时，记录、成功日记、本地目标、任务反馈和成长闭环必须继续可用。
- 健康、危机、法律和财务等高风险领域不提供越界权威结论。

## 11. 信息架构原则

用户每天只需要理解三个问题：

1. 今天最值得做什么。
2. 今天发生了什么。
3. 今天留下了什么。

主导航按用户使用频率安排为五项：

1. 今日：伙伴、当前状态、今天要做、习惯打卡、当天记录和今日收束。
2. 任务：简单看板，以及目标、任务、习惯的查看、修改和状态管理。
3. 记录：自由输入事实、感受、成功记录和口述反馈。
4. 轨迹：日历、某日回顾、成功证据、成长变化和到期的周复盘。
5. 设置：伙伴、状态自评、AI、显示、数据与高级规则，固定在最右侧。

今日、任务、记录、成长和设置采用一致的主页面骨架与固定底部导航；记录页底部只保留“保存记录”，不提供重复的取消按钮。

日历中的日期是“先看一眼”的入口：点击后先在当前日历页弹出该日快照，集中展示房间画面、当日状态、记录摘要和行动结果；URL 与日历滚动位置保持不变。每个日期可以保存一条独立、可编辑的“当日一句话”，也可以采用 AI 已整理的概括后继续编辑；一句话随备份恢复并显示在快照中。用户需要编辑、分析或逐项查看时，再主动进入完整某日回顾。没有数据的日期也显示真实空状态，不伪造内容。

其他低频功能不新增主导航：

- 数据与隐私：从设置进入，管理 AI 权限、备份、导入、存储和删除。
- 高级系统：管理领域策略、确认记忆、成长分类和个性化规则。
- 桌面小组件：无需先浏览 App 即可查看并完成今天的任务。

页面内部采用“摘要—详情—高级设置”三级结构：

- 摘要只展示当前需要知道和操作的内容。
- 详情提供修改、状态更新、来源和系统依据。
- 高级设置保留完整字段，但默认折叠，不阻塞主要任务。

设置页首屏只展示可直接理解、可展开的设置类别；人生领域数量、重点名额、记忆计数等内部统计不作为默认概况占据首屏，需要时再进入对应高级设置查看。

高级页面不能成为首次使用的必经流程。前七天优先形成“记录—接受下一步—勾选反馈—获得经验—看到调整”的最小习惯。

首次使用只要求选择“鱼鱼”或“包包”，随后直接进入记录；第一次保存后只显示一次短指引，说明“今日”看建议、“任务”完成、编辑或删除行动，不增加整页教程或连续弹窗。状态自评使用身体、心理、关系、工作、玩乐五道带参照选项的问题，不要求用户理解或填写原始数字。

### 11.1 UI 设计理念

栖光的界面采用“温暖像素生活感 + 克制的新粗野主义”。像素房间和人物负责建立陪伴感、时间感与轻游戏氛围，功能页面负责让记录、任务和回看足够清楚。两者共享同一套颜色、字体、边框和反馈语言，不能做成一个游戏首页外接一套通用管理后台。

视觉优先级固定为：

```text
当前最重要的信息与动作
> 清晰可信的状态反馈
> 伙伴与像素世界的陪伴感
> 装饰、动效和游戏化表现
```

#### 视觉语言

- 主色使用林野绿、旧纸、古铜和暖木，少量珊瑚、金色与低饱和蓝色只承担真实分类或状态；不引入通用 AI 紫色、霓虹外发光、玻璃拟态和无来源渐变。
- 功能界面延续新粗野主义的清晰轮廓、轻微错位阴影和直接反馈，但保持克制；不使用遍地粗黑框、巨大圆形控件、过重阴影或卡片套卡片。
- 页面容器、按钮、输入框和弹层遵守稳定的方角或小圆角规则；圆形只用于状态圆、头像、加号等具有明确语义的局部元素。
- 像素人物、头像、徽章、房间和家具保持一致的比例、像素密度、轮廓与暖色语言；没有功能或成长证据的装饰不得占据界面。
- 页面以浅色主题为当前品牌基线；Android 状态栏、启动页、小组件和 WebView 内容应像同一个产品，不能出现突然切换的系统蓝或陌生材质。

#### 字体与文案

- 默认使用 Android 和 WebView 可稳定渲染的系统无衬线字体；标题依靠字号、字重和留白建立层级，不混用装饰性衬线体或把普通中文大面积改成等宽字体。
- 全应用只保留页标题、区块标题、正文和辅助状态四级文字；同一级的字号、行高和字重保持一致。
- 同一行或同一组并列组件必须复用相同的字体级别、高度、内边距、对齐和状态样式；内容长短不能让同级按钮、标签、输入框或列表项各自变形。
- 页面标题只出现一次并左对齐；按钮和标签避免孤字换行，任何手机宽度下都不能出现最后一行只剩一个字的排版。
- 默认文案使用用户立即能懂的中文，不直接展示 MAIN、BONUS、RECOVERY、事实包、记忆命中等内部词；任务执行区统一使用“今天要做”和“习惯打卡”，不把内部优先级翻译成新的可见分组。
- 能通过位置、状态、图标或交互结果表达的信息不再写成说明段落；只有首次必要引导、隐私发送范围、错误原因、危险操作和不可逆后果保留解释。

#### 布局与信息层级

- 一个页面只突出一个当前主决定；其他能力按“摘要、详情、高级设置”逐层出现，不让多个同权按钮争抢注意力。
- 同一事实只展示一次。任务主行只保留名称、必要进度与截止事实，来源、依据、最低版本和调整项进入详情。
- 今日、任务、记录、轨迹和设置使用一致的页面骨架与固定底部导航；房间家具是空间化快捷入口，不复制完整导航和说明。
- 弹层只用于需要保留当前上下文的短操作；任务详情与修改在可用视口中居中，日历快照留在原月份上方，长流程使用完整页面。
- 列表优先使用整行操作、留白和少量分隔，不用大量独立卡片、胶囊标签或重复按钮制造视觉噪声。

#### 互动与动效

- 可见控件可以小巧，但触控区域不得小于 44px；普通任务整行完成，计数任务整行增加一次，次级编辑从当前条目进入。
- Android 触摸和鼠标点击不显示浏览器或 WebView 默认蓝色粗框、蓝色阴影或高亮；键盘焦点必须继续清晰可见，并使用与场景或页面一致的反馈样式。
- 每个交互都覆盖待处理、按下、处理中、完成、错误、撤销和不可用等实际状态；不能只设计静态成功画面。
- 动画只用于表达角色生命感、空间移动、操作反馈或状态变化。人物移动必须朝向位移方向，具有起步、脚步、停稳和交互阶段，不闪烁、漂移、倒走、穿模或用平移冒充步行。
- 人物默认睁眼、正面、温和开心；闲时动作低频且互斥，情绪和庆祝只响应真实上下文，结束后回到稳定姿势。
- 系统开启减少动画时，所有功能仍可完成，人物稳定显示正面开心帧，不依赖动画传达唯一信息。

#### 移动端与可访问性

- Android 手机是当前首要载体。设计必须在 320、360、390 和 430px 宽度下无整页横向滚动，并适配 200% 系统字体。
- 页面、固定底栏、底部输入栏和弹层必须避让状态栏、挖孔、键盘及系统导航安全区。
- 正文和按钮满足可读对比度；屏幕阅读器名称、键盘顺序、可见焦点和语义状态不得为了去掉触摸蓝框而删除。
- 空状态直接说明当前情况和唯一下一步；加载状态使用与页面同风格的静态占位，错误在发生位置说明，不能用纯空白或无意义旋转图标代替。

任何新页面或重构都必须先证明新元素服务于当前闭环。仅仅“更像游戏”“更丰富”或“更有设计感”不足以成为增加文字、卡片、颜色、动画和入口的理由。

### 11.2 外部产品方向参考：Meo Life

2026-09-01 记录参考：[《快捷构建你的 Life OS — Meo Life》· 一人研究室](https://www.xiaohongshu.com/explore/6a954fdc0000000025019462)。它与栖光接近的不是视觉皮肤，而是“打开就能记，一页看清一天，并让记录逐渐形成长期视图”的产品组织方式。

栖光后续设计优先参考以下结构：

- “今日”在一页内组织当前任务、目标、习惯、状态和快速记录，减少用户在多个模块间寻找下一步。
- 某日回顾逐步收敛为统一的时间顺序，让记录、任务结果和习惯完成形成一条可回看的当天轨迹。
- AI 只生成候选步骤、时间估计或整理建议；用户看清变化并确认后，才写入正式目标、任务或习惯。
- 任务与习惯分析使用周期热力图和年度证据墙，帮助用户直观看见频率、连续性和中断，而不是只显示总数。
- 语气保持安静、非催促、非打卡内卷；成长反馈服务于理解自己的生活，不制造额外压力。

参考边界固定如下：

- 保留栖光的暖色像素房间、纸张感、深绿色组件、五维状态、本地优先和成长证据，不复制 Meo Life 的渐变、通用卡片外壳或品牌视觉。
- 不因参考页面出现记账、倒数、宠物或照片墙，就扩充首版功能；只有能加强“记录—行动—反馈—回看”闭环的能力才进入计划。
- 本节是产品结构参考，不替代已审核通过的栖光界面设计稿；实现仍以栖光设计稿的比例、排版和组件状态为像素级验收基线。
- 后续经用户明确确认的页面调整优先于旧设计稿；设备外壳、系统状态栏和仿真数据内容不属于 App 像素复刻范围。

## 12. 复杂度与闭环审计原则

### 12.1 现有功能复杂度审计

2026-08-19 对当前浏览器真实界面逐页审计，结论是：系统普遍把内部数据模型、分类和维护工作交给了用户，功能虽然完整，但认知成本已经阻碍核心闭环。

| 功能 | 当前可见复杂度 | 审计结论 |
|---|---:|---|
| 今日页 | 5 组标题、18 个按钮（含 2 个更新提示按钮） | 房间入口、开始/结束节奏、五维状态、快捷入口和底部导航重复争夺注意力 |
| 创建目标 | 7 个表单控件 | 严重过度设计；默认只应保留一句自然语言目标 |
| 手动安排任务 | 8 个表单控件 | 严重过度设计；理由、最低动作、时间、类型、难度、分支和状态不应全部由用户填写 |
| 创建习惯 | 15 个表单控件 | 最严重的创建流程；计划日、状态、分支、难度和 BONUS 应由系统建议或渐进设置 |
| 成长页 | 13 组标题并允许手动建立分支 | 应以查看真实证据为主，不要求普通用户设计资产树 |
| 我的系统 | 16 组标题、33 个输入控件、25 个按钮 | 实质上是管理后台，不适合作为普通用户主导航页面 |
| 周复盘确认 | 主题、假设、最小动作、指标、日期、停止条件 | 把实验设计工作交给用户；应由系统生成一个可接受或调整的候选 |
| 记录页 | 1 个主要输入、4 个提示入口 | 当前最接近正确方向，继续保持自由输入，不增加强制分类 |
| 日历与查找 | 月历、关键词、日期 | 复杂度合理，属于低频回看工具 |
| 备份、恢复与删除 | 多步确认 | 安全复杂度必要，不为追求简洁而删除保护 |

复杂度治理原则：用户只负责表达目标、经历和真实反馈；分类、拆解、优先级、难度建议、成长归属和后续安排由系统承担。安全确认、隐私授权和不可逆操作不适用“减少步骤”原则。

本次审计不主张删除目标编辑、任务调整、状态更新、领域管理或成长查看能力。需要删除的是默认流程中的强制决策和重复入口；完整能力保留在对象详情或高级设置中。

### 12.2 开发前剩余闭环审计

2026-08-19 对数据生命周期、AI 时效、离线降级和原生扩展再次审计，新增四项必须处理的结构性风险：

- 目标暂停、完成或结束后，关联的待完成任务和里程碑目前不会同步收束，可能继续出现在行动面。
- 目标、任务或系统记忆改变后，已生成但尚未接受的 AI 拆解与任务建议缺少统一的来源版本检查，可能应用过时计划。
- 自动指导是核心价值；MiniMax 不可用时由本地确定性方向与手动任务闭环兜底，不能让网络故障阻塞记录和行动。
- Android 桌面小组件无法直接可靠读取 WebView 内的 IndexedDB；实现前必须设计最小原生快照同步层，避免两套业务账本。

除此之外，现有隐私确认、不可逆操作保护、离线记录、备份校验和 XP 幂等账本可以继续沿用，不需要为了页面简化而重做。

## 13. 验收标准

### 13.1 目标拆解

- 创建目标只有一句自然语言是必填项。
- 默认流程不要求用户理解或填写领域、成长分支和目标位置。
- 用户能在一分钟内从模糊目标得到可编辑方案。
- 方案包含 2—5 个可验证里程碑和一个立即可执行的下一步。
- 每个下一步都有完成标准、最低版本、预计时间和关联里程碑。
- 比里程碑更小的立即下一步不会被错误当成里程碑完成或提前奖励 50 XP。
- 未经用户确认，不产生正式目标、任务、记忆或 BONUS。
- 保存目标不会暗中生成任务；用户可以在目标详情中明确选择“放入今天”，核对任务后再安排。

### 13.2 每日指导与任务看板

- 打开今日后，无需进入其他页面就能知道下一步做什么。
- 默认只有一个明确 MAIN，且能够解释推荐依据。
- 同时存在已反馈 MAIN 与待完成 MAIN 时，今日页、人物、任务板和小组件都选择后者；小组件不展示已反馈任务。
- 用户可以一击接受，也可以缩小、替换或暂不做。
- 看板的任务主行只展示标题、必要进度或截止事实，不重复展示来源、标题、最低动作和虚构用时。
- 普通任务显示小空心圆，整行点击即可完成；完成后显示勾选和标题划线，并能撤销。
- 待完成任务不显示“完成 / 有进展”文字按钮，不区分“今日重点 / 其他任务”，支持拖动排序并持久保存；右侧省略号与边框保持安全内距。
- 用户可以安排未来日期；计数任务整行点击增加一次，背景随进度填充，只有达到目标次数时完成并结算一次经验，详情允许减一次纠错。
- 今日页和任务页都展示当天计划习惯；计数习惯在每个计划日生成独立进度，以“今天已完成/所需次数”展示，不继承昨天的次数，也不制造补打欠账。
- 新建习惯默认启用计划日打卡；如果今天符合频率，保存后立即进入今天的 BONUS。
- 任务详情和修改弹层在手机与键盘可见状态下均保持居中、可滚动，且不与系统状态栏或底部导航重叠。
- 任务页标题旁没有全局“管理”按钮；每个待处理任务、目标和习惯都能从自身条目进入编辑或删除，并在删除前确认。
- 新用户无需理解“事实、已确认记忆、最小动作”等内部术语即可修改任务；建议依据默认折叠，展开后仍以自然语言说明。
- 自动生成的目标和任务可以进入详情修改，并能更新状态或请求重新规划。
- 待处理任务可以编辑或删除；删除习惯生成任务后刷新不再重建该实例，历史反馈与经验保持不变。
- 状态不足时，系统能够合理选择恢复而不是机械推进。
- 用户能够只通过今日页或桌面小组件完成“查看下一步—反馈结果—看到结算”，不必进入高级页面；小组件动作回到任务板提供撤销与结算反馈。

### 13.3 易用性与留存

- 首次使用在三分钟内完成第一次真实记录并得到一个可执行的下一步。
- 选择伙伴后直接进入记录；首次保存后最多出现一次短指引，不出现整页教程或连续弹窗。
- 目标、任务和习惯的默认创建流程都只有一个自然语言必填输入。
- 每个步骤默认只要求用户作出一个主要决定，不同时展示多个系统分类问题。
- 用户不需要理解领域、成长分支、状态归属、任务类型或难度即可开始行动。
- 状态自评以五道带参照选项的问题呈现，不暴露无说明的数字校准。
- 系统已经拥有或能够合理推断的信息不重复询问；不确定内容允许暂不归类。
- 中断后返回不产生欠账、惩罚或批量清理压力，直接从当前情况重新判断下一步。
- 用户能够看见上一次反馈如何影响了本次推荐，这是长期留存成立的必要条件。
- 简化创建流程后，目标、任务和习惯的完整修改与状态管理能力仍然可达。

### 13.4 页面架构

- 主导航只保留今日、任务、记录、轨迹和设置五个目的地，设置固定在最右侧。
- 记录与另外三个主导航目的地使用一致的完整页面和固定底栏；进入、退出和当前页反馈不依赖弹层语义。
- 记录页没有“设为成功记录”复选框、序号工具或图片入口；点击快速开头决定记录类型并显示小标题，页面切换后恢复未保存草稿，保存后进入当天回顾。
- 今日记录摘要保持单行并可整行打开详情；当天回顾展示全部记录，支持继续新增、编辑、查看版本和删除。
- 用户能够在任务页完成查看、勾选、修改、暂停、结束和重新规划，不必进入系统页。
- 日历、成功日记、成长证据和周复盘在轨迹中形成连续时间视图。
- 点击日历日期先在当前页打开该日快照，能够看见房间画面、当日一句话、状态、记录和行动结果，并可继续进入完整某日回顾；一句话可手写或采用 AI 概括后编辑，关闭快照后仍停留在原月份与原滚动位置。
- 任务页只有“今天”和“计划”两层；计划下用“目标、习惯、之后已安排”表达长期对象，不再把目标称作“长期任务”。计划日期、次数目标和低频调整收进任务安排或详情，不增加新的主导航。
- 成长证据页不重复放置“打开周复盘”等同页跳转入口；周复盘由轨迹中的既有入口承担。
- 普通设置、数据与隐私、高级系统职责分离，危险操作不与日常功能混排。
- 同一功能在默认界面只有一个主入口，房间表现不复制完整导航。

### 13.5 自适应闭环

- 完成、部分完成、未开始和方向错误会产生不同的后续安排。
- “今天不做”只停止当天对该目标的重复提醒，不等于否定或永久屏蔽目标；只有用户明确暂停、结束或确认新路径后，目标才退出后续推荐。
- 昨日复盘存在可靠下一步时，用户可直接打开已预填的任务确认框；没有可靠下一步时明确不预先安排，不能把占位文案当成任务。
- 同一失败任务不会在没有新判断的情况下重复出现。
- 用户能看见因为哪条反馈，明天发生了什么变化。
- 用户纠正推断后，旧推断不再继续影响决策。
- 过期的普通任务和目标行动不会消失、自动失败或无限堆积，而是通过一次轻量“待决定”处理进入完成、缩小、放下或豁免路径。
- 晚完成记录真实完成日期，经验和成功证据不会错误写回原计划日期。
- BONUS 错过后只在原日期保留可补记的未记录事实，不进入待决定、不顺延；连续失败不会导致相同任务被机械复制。
- 新习惯动量从创建后的真实计划日开始；暂停、结束、移出 BONUS 和修改频率不会制造历史欠账。
- 暂停或结束习惯会无惩罚收束已经生成但尚未反馈的 BONUS。

### 13.6 多元发展

- 每天保持一个焦点，周/月视图能够发现长期领域失衡。
- 恢复、维持和探索能够成为正式方向，而不是推进任务的附属选项。
- 系统不会因某个领域缺少每日行动而制造负罪感。
- 周复盘能够接收正式规则产生的整数或小数动量，不因内部数值口径冲突阻断生成。
- 周复盘默认提供一击采用候选；缩小或修改时才展开实验细节，不能要求普通用户逐项核对六个系统字段。
- 周复盘发送范围默认包含全部允许的事实摘要；用户在设置中修改一次后长期生效，单次复盘不再重复展示类别复选框。

### 13.7 伙伴与小组件

- 伙伴引用的个人事实都有来源或用户确认。
- 伙伴能够指出具体成功、真实阻力和明确下一步。
- 小组件只显示当天待完成任务；小/中/大尺寸分别显示最多一、二、四项，并可勾选完成或打开对应任务。
- 小组件不泄露日记和敏感目标内容，不依赖高频后台轮询。
- 小组件唤醒已打开的 App 时不重载当前页面，任务反馈和 XP 仍只结算一次。
- 用户中断后不会受到惩罚或欠账提示。
- 伙伴的长期目标是提升用户自主判断，而不是增加依赖。
- App 内伙伴默认睁眼、正面开心，无真实依据时不出现负面表情；移动使用预先准备的连续脚步帧和匹配落脚节奏的离散格步，每一段位移与左右/前后朝向一致，结束后回到正面，不倒着走、闪烁或漂移。
- 人物闲置时会低频随机做一次打招呼、放松、思考或阅读；闲时动作不与散步、家具交互或快照展示重叠，结束后稳定回到正面开心姿势。
- 点击生活分身能够展开当天指导；点击面板内部不会误关，点击面板外部会立即收起且人物按钮同步恢复为未展开状态。
- App、人物设置预览和可点击原型只展示同一组男女卡通头像；不再向用户展示素描原稿，头像与房间人物的身份特征和默认表情一致。今日任务小组件不展示人物头像。
- 页面外层与像素房间共享林野绿、旧纸、古铜和暖木材质语言；场景可随时段出现方向光、暗角和一次性微光，但不得出现仿制现有游戏的品牌或素材。
- 房间家具图案、热点和行走终点一致，中部保持无遮挡的可行走空间；减少动画模式显示稳定的正面姿势。
- 房间不显示无说明、无交互的习惯花盆或装饰性色块；累计成就纪念物继续只由真实完成证据解锁。
- 书桌与椅子、工作台和床都有可辨认的走近、使用和反馈阶段；需要跳转的家具在反馈后进入真实功能，床在休息后回到中央且不产生虚假成长数据。
- 家具互动能够辨认起步、行走、停稳和使用四个阶段；落脚时人物与地面阴影同步变化，当前家具出现短暂场景光环，非当前热点不争夺注意力。
- Android 冷启动在真实内容绘制前显示“正在点亮房间”的主题占位，初始化完成后自动移除；离线冷启动不得出现不可解释的纯空白页。
- Android 页面必须避让状态栏、刘海/挖孔和底部系统导航区域；应用内容、标题、弹层和固定底栏不得与系统 UI 重叠。
- 房间和家具热点不显示装饰性黑色 UI 边框，键盘焦点环、触控区域和辅助技术名称仍然完整。

### 13.8 工程与安全

- 本地记录、目标、任务、反馈、证据、经验和备份在离线状态可用。
- AI 服务失败不会阻塞本地核心功能。
- Android 系统自动云备份和设备迁移均排除应用私有数据，用户主动导出/导入是唯一数据迁移入口。
- 全量检查、发布检查、AI 固定评估和浏览器端到端测试通过。
- 完成过目标后导出的备份仍能校验、恢复并继续使用；目标后续行动的内部 ID 不得导致合法备份被拒绝。
- 里程碑完成、撤销和重做后只有一笔有效结算，状态、经验和真实结算日期一致。
- Android 新版本完成发布配置、构建、安装、离线、小组件和备份真机回归。
- 用户可以查看、导出、导入和删除自己的全部数据。

### 13.9 成就与徽章

- 里程碑、完整目标、习惯沉淀、恢复行动和已实践周实验都只能从相应的用户确认事实派生成就，并展示真实证据与日期。
- 每个稳定身份只产生一项成就；重复确认、重载、导入和重新计算不会重复解锁。
- AI 候选、系统记忆、未确认推断、登录、任务创建、连续登录和累计点击不能产生成就。
- 成就不产生额外 XP、正式任务、提醒压力或每日方向权重；“下一项可达成”只在成就册内提供无期限进度说明。
- 撤销来源事实会同步停止展示；暂停或结束习惯不删除已成立的沉淀成就，目标路径变化不删除仍有结算证据的里程碑成就。
- 成就册在成长证据页内可达，在 320px、200% 字体、键盘、屏幕阅读器和减少动画模式下可用。
- 每个成就家族的派生、去重、撤销、离线重算、跨版本备份和证据详情都有自动化回归。

## 14. 当前明确不做

- iPhone/iOS 构建、真机回归和 App Store 上架。
- 自建录音和语音识别，继续使用系统输入法。
- 排行榜、社交、公会、抽卡、商店和虚拟货币。
- 成就稀有度、限时成就、随机掉落、锁定图鉴、全收集百分比、分享奖励和徽章兑换。
- 自由地图、家具摆放、背包、战斗、NPC 和经营系统。
- 全局人生总分、断签、负经验和连续登录奖励。
- 未经用户确认的长期记忆、目标修改、正式任务或 BONUS 启用。
- 与现实成长证据无关的大量装饰图片和游戏平台能力。

## 15. 实施顺序

```text
统一产品语义
→ 升级目标拆解
→ 建立每日方向决策
→ 打通反馈与重规划
→ 收敛今日页与任务看板
→ 接入并验证个人 Android 真实 AI 直连
→ 增加温和召回与备份提醒
→ 实现 Android 今日任务小组件
→ 建立伙伴连续记忆与关系反馈
→ 实现由多类已确认现实证据派生的成就徽章册
→ 完成三十天确定性模拟与逐日页面回归
→ 修复阻断后进行真人封闭使用
→ 决定同步、商业化和平台扩展
```

下一阶段的完成标准不是增加页面，而是证明：用户每次提供真实反馈以后，系统给出的下一步都会发生有依据、可解释、可纠正的变化，并且用户能从 App 或今日任务小组件中低成本地查看、完成和继续行动。
