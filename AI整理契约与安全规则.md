# 栖光：AI 整理契约与安全规则

> 状态：开发前基线 v1.1  
> 目标：把 AI 限定为“有证据的整理与建议”，而不是拥有用户生活数据的自动决策者  
> 相关文档：[产品规格与验收.md](./产品规格与验收.md) · [技术架构与数据设计.md](./技术架构与数据设计.md)

## 0. 核心结论

AI 的完整工作流必须是：

```text
用户选择记录并点击整理
→ 客户端展示将发送的范围
→ 服务端裁剪与校验
→ 模型只返回结构化候选
→ 服务端校验结构
→ 客户端展示事实、推断、依据和影响
→ 用户确认、修改或否认
→ 本地确定性规则完成状态与 XP 结算
```

AI 可以整理、解释和提议；不能直接写入长期系统、最终数值或用户身份判断。

## 1. AI 的四种操作

首版只允许四种 `operation`：

| 操作 | 触发方式 | 结果 |
| --- | --- | --- |
| `daily_analysis` | 用户点击整理当天记录 | 摘要、事件、状态影响候选、每日复盘、明日任务建议 |
| `task_feedback` | 用户提交任务文字反馈 | 完成情况候选、实际结果摘要、证据 |
| `weekly_review` | 用户主动生成周复盘 | 趋势、收益与消耗、成长证据、下周主题与实验 |
| `system_candidate_review` | 用户查看长期候选 | 合并重复候选、展示支持与反例，不自动确认 |

月复盘不进入首轮 AI 接口，待周复盘稳定后复用同一结构扩展。

## 2. 请求信封

所有请求共用：

```json
{
  "contractVersion": "1.0",
  "operation": "daily_analysis",
  "requestId": "uuid",
  "locale": "zh-CN",
  "timeZone": "Asia/Shanghai",
  "localDate": "2026-08-14",
  "userInput": {},
  "context": {},
  "permissions": {}
}
```

### 2.1 通用校验

- 只接受列入合约的字段和枚举。
- 单次正文首版限制为 20,000 个 Unicode 字符，超出时提示用户分段，不静默截断原文。
- ID、日期、时区、字符串长度、数组数量必须服务端再次校验。
- 不接收二进制音频；系统输入法转写结果按普通文字处理。
- 请求必须有一次性 `requestId`；重复请求返回同一结果或明确冲突，不重复生成多份候选。

## 3. 每日整理输入

`daily_analysis.userInput`：

```json
{
  "entries": [
    {
      "entryId": "uuid",
      "revision": 2,
      "text": "今天会议很多，晚上散步后好了一些。"
    }
  ]
}
```

`daily_analysis.context` 只允许包含：

- 当天已经确认的事件，防止重复提取。
- 最近七天的五维状态摘要，不发送全部原文。
- 当前一个主目标、最多两个次要目标。
- 当天最多三个由用户主动启用的 BONUS 习惯。
- 用户已确认且与本次内容相关的系统记忆。
- 用户明确填写的明日时间、地点、体力等现实约束。

默认不发送：

- 全部历史日记。
- 已忘记或未确认的长期记忆。
- 与本次任务无关的联系人、设备和网络元数据。
- 参考文章正文；只发送用户选择的摘要，并标记为“材料，不是亲身经历”。

## 4. 每日整理输出

服务端返回：

```json
{
  "contractVersion": "1.0",
  "requestId": "uuid",
  "operation": "daily_analysis",
  "result": {
    "title": "忙碌之后重新恢复",
    "summary": "会议占据了白天，晚间散步带来了一些缓解。",
    "explicitMoods": ["疲惫", "缓和"],
    "events": [],
    "reflection": {},
    "questSuggestions": [],
    "memoryCandidates": []
  },
  "warnings": []
}
```

### 4.1 标题、摘要与心情

- 标题不超过 20 个中文字符，不使用人格标签或鸡汤句。
- 摘要不超过 120 个中文字符，先写发生了什么，再写用户明确表达的感受。
- `explicitMoods` 只能来自用户明确表达或常识上等价的情绪词；AI 推测的情绪不能混入该数组。
- 心情与“心力”状态分开，不能因为用户说“难过”就自动判定心力低。

### 4.2 事件结构

最多六个事件：

```json
{
  "candidateId": "event-1",
  "title": "连续参加多场会议",
  "description": "白天的会议安排较密集。",
  "sourceType": "explicit",
  "confirmation": "confirmed_by_default",
  "confidence": "high",
  "evidence": [
    {
      "entryId": "uuid",
      "quote": "今天会议很多",
      "start": 0,
      "end": 6
    }
  ],
  "stateImpactCandidates": [],
  "growthEvidenceCandidate": null
}
```

规则：

- `sourceType` 只有 `explicit` 和 `inferred`。
- 用户明确说出的事实为 `confirmed_by_default`，但仍可撤销或编辑。
- 推断事件必须为 `pending`，确认前不影响状态和 XP。
- 每个事件至少有一个证据片段；证据必须是原文中的连续字符。
- `start`、`end` 以 Unicode 字符位置计算，服务端验证 `quote` 与原文一致。
- 同一事件被多条记录重复描述时合并，并保留多个证据，不创建多个事件。
- 参考材料中的陈述不得变成用户事件，除非用户明确写了自己实践或经历。

### 4.3 状态影响候选

```json
{
  "dimension": "energy",
  "direction": "negative",
  "strength": "medium",
  "suggestedDelta": -6,
  "reason": "用户明确描述会议密集，并在之后通过散步缓解。",
  "confidence": "medium"
}
```

维度枚举：

| 内部值 | 展示名 |
| --- | --- |
| `energy` | 体力 |
| `mental` | 心力 |
| `connection` | 连接 |
| `progress` | 推进 |
| `play` | 玩心 |

强度与候选范围：小 `2–4`、中 `5–8`、大 `9–15`。正负由 `direction` 决定。

客户端负责最终校验：

- 未确认推断不生效。
- 同日同维度所有事件合计限制在 `-15..15`。
- 用户同日校准优先。
- 同一证据不能作为两个重复事件反复影响同一维度。
- 没有证据时保持“待了解”，AI 不补默认值。

### 4.4 成长证据候选

```json
{
  "branchId": "uuid-or-null",
  "suggestedBranchName": null,
  "evidenceType": "practice",
  "description": "完成了一个可验证的脚本成果。",
  "isMilestoneCandidate": false,
  "reason": "记录中出现了完成结果，而不只是阅读材料。"
}
```

- 每个事件最多关联一个主要成长分支。
- 阅读、收藏、打开 App 和仅表达打算不产生 XP。
- AI 只标记证据，不计算 XP。
- 里程碑始终需要用户确认，并提供可观察的成果证据。

### 4.5 每日复盘

```json
{
  "whatHappened": "今天会议密集，晚间散步后有所恢复。",
  "specificCredit": "你在注意到耗尽后主动选择了散步，而不是继续硬撑。",
  "patternCandidate": {
    "observation": "会议密集的晚上可能更需要低刺激恢复。",
    "evidenceCount": 1,
    "neededEvidence": "还需要观察至少两次相似日子。"
  },
  "nextSmallStep": "明天会议结束后预留十分钟无屏幕过渡。"
}
```

禁止：

- “你就是拖延型人格”等固定身份结论。
- 没有具体行为的空泛表扬。
- 把一次事件写成稳定模式。
- 低状态时默认建议增加工作量。

### 4.6 明日任务建议

每日输出最多：主线 1、支线 2。BONUS 任务由客户端从用户已经主动设为 BONUS 的习惯生成；AI 可以推荐候选，但不能在每日整理中创建、启用或替换 BONUS。

```json
{
  "type": "main",
  "title": "会议后留十分钟过渡",
  "why": "最近记录显示会议后注意力容易耗尽。",
  "minimumVersion": "离开座位，十分钟不打开新工作。",
  "estimatedMinutes": 10,
  "difficulty": "light",
  "primaryState": "mental",
  "growthBranchId": null,
  "sourceGoalId": null,
  "isRecovery": true
}
```

规则：

- 每项必须说明为什么推荐、最小版本、预计时间、难度和主要状态。
- 有成长经验的任务必须只有一个成长分支。
- 任务应优先来自已确认目标、状态补给和现实约束。
- 不能通过虚构紧迫性、恐惧或高 XP 诱导用户接受困难任务。
- 任务建议默认是草案，用户接受后才成为任务。

### 4.7 长期记忆候选

```json
{
  "type": "constraint",
  "statement": "会议密集日的晚间可用注意力可能较低。",
  "confidence": "low",
  "supportingEventIds": ["event-1"],
  "counterEvidence": [],
  "recommendedAction": "observe"
}
```

- 一次观察通常只能推荐 `observe`，不能推荐直接确认。
- 候选必须展示支持证据、反例和证据次数。
- 长期记忆的新增、编辑、合并、遗忘均由用户决定。

## 5. 任务反馈契约

用户可能说：“我只做了前半部分，但把需求列清楚了。”AI 只判断反馈含义，不直接结算。

输出：

```json
{
  "completionCandidate": "partial",
  "actualResult": "完成了前半部分，并产出需求清单。",
  "evidenceQuote": "我只做了前半部分，但把需求列清楚了",
  "suggestedDifficultyCorrection": null,
  "followUpQuestion": null,
  "confidence": "high"
}
```

枚举：`complete`、`partial`、`skipped`、`unclear`。

- 明确反馈可以默认选中结果，用户点击确认后结算。
- `unclear` 时只问一个必要问题，不连环追问。
- AI 可以建议修正难度，但修正后再次展示 XP 变化。
- 完成事实与“是否有效”分开；不能因为用户觉得效果一般就取消真实完成。
- 同一任务重复反馈时只更新原反馈，不创建第二笔 XP。

## 6. 周复盘契约

输入只包括选定周内：

- 已确认事件和状态快照。
- 任务结果、习惯动量和 XP 账本摘要。
- 当前目标、实验及已确认系统记忆。
- 用户主动加入的补充说明。

不发送该周全部原文，除非用户在发送预览中明确选择。

输出：

```json
{
  "stateTrends": [],
  "recurringBenefits": [],
  "recurringCosts": [],
  "growthDeposits": [],
  "habitDecisions": [],
  "nextWeekTheme": {},
  "nextExperiment": {},
  "systemCandidates": []
}
```

约束：

- 每个趋势引用至少两个不同日期的证据；不足时写“尚不足以判断趋势”。
- 区分相关与因果，不把同时发生写成确定因果。
- 习惯建议只有保留、降低难度、改变触发器、暂停、停止。
- 下周只给一个主题和一个最小实验。
- 实验必须写假设、最小动作、观察指标、结束日期和停止条件。
- 用户拒绝某个建议不产生负面评价或惩罚。

## 7. 提示词分层

服务端按固定顺序组成提示：

1. **系统约束**：产品角色、禁止事项、结构化合约和安全规则。
2. **操作说明**：本次是每日整理、任务反馈或周复盘。
3. **已确认上下文**：目标、习惯、状态摘要和长期记忆。
4. **用户材料**：明确以数据区块包裹，不能作为系统指令执行。
5. **输出 schema**：字段、枚举、长度和数量上限。

用户日记、链接摘要和参考材料中的“忽略之前指令”等文字都属于内容，不改变系统约束。这些材料在提示中使用清晰边界和不可执行标记。

## 8. 事实、推断与确定程度

### 8.1 确定程度

| 值 | 使用条件 |
| --- | --- |
| `high` | 用户直接、清楚地表达，且证据无歧义 |
| `medium` | 有合理证据，但存在其他解释 |
| `low` | 线索有限，只值得作为待观察候选 |

确定程度不是概率，不在界面显示伪精确百分比。

### 8.2 冲突处理

优先顺序：

```text
用户当次明确修正
> 用户已确认事实
> 用户原始记录
> AI 推断
> 通用建议
```

当同一记录出现矛盾时，AI 保留冲突并说明无法确定，不替用户选择“更合理”的版本。

## 9. 安全与敏感场景

### 9.1 一般边界

AI 不进行：

- 医学、精神健康、法律或财务诊断与确定性结论。
- 对人格、疾病、关系对象动机的武断判断。
- 以 XP、任务或连续完成奖励危险、过度工作、极端饮食、睡眠剥夺或自我伤害。
- 把哀伤、疾病、危机和照护责任解释成自律失败。
- 要求用户公开、上传或保留不必要的敏感信息。

### 9.2 明显危机信号

如果文字明确显示用户可能即刻伤害自己或他人：

1. 停止普通游戏化反馈、XP 庆祝和效率建议。
2. 使用简短、平静、非评判语言确认当下安全最重要。
3. 建议立即联系当地紧急服务、危机资源或身边可信任的人。
4. 提供一个清楚的“查看本地求助资源”入口，资源按地区配置，不由模型临时编造号码。
5. 不声称 App 能监控、报警或替代专业帮助。
6. 原始记录仍按用户选择保存在本机，不自动通知第三方。

这不是诊断流程，也不产生状态分、任务难度或成长经验。

### 9.3 隐私提示

发送前界面展示：本次哪些记录、哪段日期范围、哪些系统记忆将离开设备。用户可以取消某项上下文；取消后模型若证据不足，应明确不知道。

## 10. 服务端结果校验

服务端和客户端都必须验证：

- JSON 可解析、合约版本一致。
- 没有未知顶层字段。
- 枚举、长度、数量和数值范围正确。
- 证据 quote 与输入原文一致。
- 最多六个事件、一个主线、两个支线。
- 状态变化符合小、中、大范围。
- 事件只有一个成长分支。
- 不存在负 XP 或模型直接给出的最终 XP。
- 不存在模型直接确认的长期记忆或目标修改。

任何关键字段不合法时，整份结果不进入正式数据。允许保留原始响应用于本机调试，但生产日志不记录正文。

## 11. 错误、重试与版本

### 11.1 错误码

| 错误码 | 含义 | 是否重试 |
| --- | --- | --- |
| `OFFLINE` | 当前无网络 | 用户联网后重试 |
| `INPUT_TOO_LARGE` | 输入超过上限 | 用户分段，不自动截断 |
| `RATE_LIMITED` | 请求过快 | 显示等待时间后重试 |
| `MODEL_TIMEOUT` | 模型超时 | 可重试，使用同一幂等键 |
| `INVALID_MODEL_OUTPUT` | 输出不合约 | 可重试一次，否则保留原文 |
| `UNSUPPORTED_CONTRACT` | 客户端版本不受支持 | 需要更新应用 |
| `SAFETY_REVIEW` | 进入敏感场景分支 | 展示安全界面，不继续普通流程 |

### 11.2 重试规则

- 自动重试最多一次，只用于网络抖动或格式错误。
- 用户再次点击使用相同请求 ID 时不得产生重复分析。
- 模型成功但客户端保存失败时，客户端可以用请求 ID 取回短期结果；若服务端严格无状态，则在当前响应未保存前不清空用户页面。
- 记录版本改变后，旧结果标记过期，不能套用到新正文。

### 11.3 合约升级

- `contractVersion` 使用明确版本号。
- 破坏性字段变化升级主版本。
- 客户端保留旧分析原始 JSON 和解析版本，以便历史页面继续显示。
- 不在同一版本里悄悄改变状态枚举或数值含义。

## 12. 模型评估方案

开发前建立去身份化的固定样本集，至少覆盖：

1. 一句话普通记录。
2. 同一天多次追加和重复事件。
3. 事实与推断混合。
4. 难过但心力稳定。
5. 工作推进但体力下降。
6. 阅读材料但没有实践。
7. 用户明确完成里程碑。
8. 部分完成和模糊任务反馈。
9. 无证据时应回答不知道。
10. 否定表达，例如“我没有焦虑”。
11. 引用内容中含提示注入文字。
12. 危机、疾病、哀伤和照护情境。
13. 长文本超过六个事件时的取舍。
14. 与已确认记忆冲突的新证据。
15. 周趋势证据不足。

### 12.1 评估指标

| 指标 | 首轮门槛 |
| --- | ---: |
| JSON 合约通过率 | ≥ 99%（含一次格式重试） |
| 明确事件证据可定位率 | 100% |
| 推断被错误自动确认 | 0 |
| 参考材料被当作亲身经历 | 0 |
| 最终 XP 由模型直接决定 | 0 |
| 长期记忆未经确认写入 | 0 |
| 用户样本事件确认/轻改率 | 封闭使用中观察，不先伪设目标 |

模型质量还要记录：事件被确认、编辑、否认的比例；状态建议被校准的比例；任务被接受、缩小、替换、跳过的原因。这些数据首版只在本机汇总，由用户主动导出用于评估。

## 13. 人格与文案规则

生活分身说话遵循：

- 先事实，再解释，最后给一个可选行动。
- 温暖但不装作全知。
- 具体，不使用“你已经很棒了”替代证据。
- 不使用“打败懒惰”“不能断签”“你必须”等羞耻语言。
- 建议使用“可以”“如果你愿意”“我看到的证据是”。
- 一次最多提出一个主要问题或下一步。
- 用户拒绝后不重复劝说。

示例：

```text
我看到你连续两次在会议密集的晚上写到注意力耗尽。
这还不足以说明长期规律，但值得再观察一次。
明天可以把主线缩成十分钟的收尾，你也可以保持原计划。
```

## 14. 开发前 AI 验收

- [x] AI 输入范围、发送时机和预览规则明确。
- [x] 每个事件都有可定位原文证据。
- [x] 事实、推断、估计和长期候选彼此分开。
- [x] 状态、XP、等级和动量由客户端确定性规则结算。
- [x] 任务数量和难度边界进入合约。
- [x] 参考材料不会自动成为用户经历。
- [x] 危机情境会暂停普通游戏化反馈。
- [x] 重试、幂等、版本和格式错误已有处理。
- [x] 封闭使用前的固定评估样本与硬性门槛明确。
