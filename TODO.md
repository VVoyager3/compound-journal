# 栖光：已完成与 TODO

## 当前 TODO 与完成项

### P0：让现实成长闭环成立

#### P0.0 重构任务页与设置页

- [x] 重构任务页面：使用“今天 / 计划”分离执行与管理；普通任务整行完成，计数任务整行打卡并按进度填充；详情和修改居中展示，同时保留日期、目标关联、习惯与后续安排能力。
- [x] 固化当前 UI 设计理念与验收基线：温暖像素生活感结合克制的新粗野主义，功能页面保持低密度、单一主操作、直白中文与移动端可访问性；后续页面、人物、房间、动画和小组件改动统一遵守。
- [x] 清理现存界面与新 UI 基线的冲突：恢复建议卡使用“状态照顾”等新用户可理解的中文，不再显示内部英文术语。
- [x] 重构设置页面：按日常设置、功能与设备、数据与隐私、高级设置重新组织人物、状态自评、AI、显示与语气、小组件、安装存储和本地数据；保留按需展开和高级能力。
- [x] 收紧移动端组件系统：统一四级文字、48px 按钮、轻描边输入与卡片、安全区、长弹窗可视窗口和短屏滚动；任务快捷操作改为两列，设置只允许一个一级栏目展开，危险数据操作独立成区。

#### P0.1 统一产品语义

- [x] 将人生领域、近期状态、目标、里程碑、习惯、任务、成长证据和系统记忆落实为唯一产品词典；模型层保留唯一语义注释和字段边界。
- [x] 检查页面和数据模型中的重叠命名，保证同一概念只有一个含义。
- [x] 明确五维状态只用于近期能力判断，不承担全面发展目标管理；目标和成长证据仍走独立实体。
- [x] 明确每个任务的来源、关联目标、里程碑、任务类型和完成证据；Quest 显式保存 sourceType/sourceId/milestoneId，反馈记录完成证据并结算 XP。
- [x] 将领域、成长分支和目标位置定义为系统内部元数据，不再作为创建目标的必填项。
- [x] 数据模型允许目标暂不归类，不能为了满足数据库字段而强迫用户选择。
- [x] 建立“用户输入字段白名单”：只有无法从自然语言、历史或规则可靠获得的信息才允许成为必填项。
- [x] 所有内部字段提供未分类或系统默认状态，不能因技术模型要求阻塞用户行动。
- [x] 定义目标暂停、完成、结束时关联里程碑和待完成任务的收束规则，保留历史但不继续主动安排。

#### P0.2 升级目标自动拆解

- [x] 将创建目标收敛为一个自然语言必填输入，其他信息全部折叠为可选补充。
- [x] 移除默认表单中的“关注领域”“唯一成长分支”“目标位置”。
- [x] 由系统按目标文字确定性建议所属生活领域、主要成长方向和当前优先级；无法判断时保持未归类。
- [x] 已有主目标时使用“我现在就想推进它（当前重点）/我想同时照顾它（次要方向）/我先存下来，以后再做”的自然语言选择。
- [x] 在现有 2—5 个里程碑草案上增加最终完成证据、当前阶段、风险、假设和预计投入。
- [x] 为下一步保存完成标准、最低版本、预计时间和关联里程碑；普通任务默认以最小动作作为完成标准，目标跟进任务使用里程碑证据。
- [x] 支持逐项编辑并确认目标结构，不允许模型直接落库。
- [x] 支持根据执行证据发起重新拆解候选，而不是永久沿用首次计划。
- [x] 增加模糊目标、冲突目标、无截止目标、过大目标和不可执行建议的合约测试。
- [x] 目标、记忆或相关计划改变后，旧拆解候选必须失效或要求重新确认来源版本。
- [x] AI 不可用时提供一个可编辑的本地最小下一步，不让创建目标停在无行动状态。
- [x] 目标拆解草案绑定创建时的目标内容与分类指纹；用户修改来源后旧草案立即隐藏并要求重新生成。

#### P0.3 建立每日方向决策引擎

- [x] 建立本地确定性方向选择器，并为恢复、主线、目标、昨日步骤和探索返回可解释理由。
- [x] 用确定性规则筛选恢复优先、截止风险、里程碑依赖、停滞时间、状态容量和领域平衡。
- [x] 每日方向已纳入当前目标的待完成里程碑依赖，并在建议卡中说明“先推进下一里程碑”；恢复、截止风险和状态容量规则继续独立生效。
- [x] 已加入停滞识别：目标持续至少 7 天且近 7 天没有完成/部分完成证据时，只提示缩小或换路，不自动改变目标状态。
- [x] 每日方向选择器已纳入主线可选截止风险（截止已过或 24 小时内），并在理由中明确“仍由用户决定”。
- [x] 默认只展示一个明确的开始方向，并说明为什么是它；没有足够依据时明确进入探索记录。
- [x] 今日重点和任务卡提供最低版本、预计时间及完成后的反馈/下一步入口；完整完成标准仍可在详情中补充。
- [x] 保留 1 个今日重点、3 个可选任务、2 个其他任务的硬上限，不让 AI 绕过限制。
- [x] 任务板允许接受、缩小、替换、暂不做、顺延和指出方向不合适。
- [x] 没有足够依据时进入探索任务，不伪造确定方向。

#### P0.4 打通反馈到重规划

- [x] 完成后自动判断继续下一动作、完成里程碑或进入下一阶段。
- [x] 目标行动完成后，若仍有未完成里程碑，自动生成唯一且可编辑的下一里程碑任务；重复反馈不会重复生成。
- [x] 部分完成时保留真实进展，并生成继续或缩小候选。
- [x] 目标行动部分完成时，自动生成唯一的“缩小继续”候选；它仍受任务上限约束，可继续编辑或放下。
- [x] 多次未开始时询问一个必要阻力问题，不重复生成相同任务。
- [x] 同一目标行动再次未开始时要求记录一个主要阻力，并停止自动复制该目标行动，交由用户调整或暂停。
- [x] 状态过低时允许恢复任务替代推进任务，并说明原因。
- [x] 路径被证伪或目标失去意义时提出暂停、修改、结束或重新拆解候选。
- [x] 目标任务调整对话框已提供“编辑关联目标”和“暂停关联目标”入口；不自动删除历史，暂停后停止主动安排。
- [x] 每次调整显示使用了哪些事实和用户确认记忆。
- [x] 增加跨日端到端测试，证明今天的反馈确实改变明天的推荐。
- [x] 将计划日期与可选硬截止时间分离；同时保存真实完成时间和实际完成日期。
- [x] 任务支持可选硬截止时间，与计划日期和实际完成日期分离；截止后只显示事实，不自动失败、不扣经验，仍由用户决定。
- [x] 为过期但未反馈的 MAIN、支线和普通任务建立一次性“等待处理”入口。
- [x] 支持晚完成、部分完成、继续推进、不再做和外部原因五种处理路径。
- [x] 晚完成经验归入真实完成日期，保留原计划日期和实际完成日期事实。
- [x] BONUS 习惯错过后退出当前行动面，只在原日期保留可补记的未记录事实；不进入“待决定”，不顺延、不补课、不形成欠账。
- [x] 同一行动第二次未开始时询问一个必要阻力；反复失败时停止复制并提出换路或暂停。
- [x] 已覆盖计划日跨越、硬截止跨越、晚完成 XP、BONUS 不积压、重复失败和中断回归测试。
- [x] 已覆盖硬截止字段保存与回读；完整的时间流逾期交互仍待真机/时钟场景补测。
- [x] 已覆盖晚完成日期、完成后下一里程碑唯一生成、目标行动跳过后的人工决策入口、硬截止事实展示和 BONUS 不积压；设备时钟与桌面启动器行为仍留待统一真机验收。
- [x] 修复“今天不做”永久屏蔽整个目标：当天停止重复提醒，次日重新提供继续、缩小或改写入口；只有用户明确暂停、结束或重拆才长期退出推荐。

#### P0.5 收敛今日页与任务看板

- [x] 进入今日后优先看到“我建议你下一步做什么”，不先面对模块导航。
- [x] 今日页先展示单一“今天的节奏”建议卡，包含方向、理由、行动入口、最低版本；任务详情继续提供反馈和关联来源。
- [x] 核心动作只保留一个默认入口；房间热点只作游戏化快捷方式，底部导航只承担稳定全局导航，不再新增平级快捷区。
- [x] 今日页已删除底部重复快捷入口；记录和任务的默认行动入口由今日方向卡承担，底部导航与房间热点作为稳定辅助入口保留。
- [x] 五维状态默认收起为一句状态判断；只在需要校准时询问一个维度。
- [x] 五维状态改为五道带明确选项的“状态自评”，避免用户面对无标准的数字打分；某日回顾仍显示已有状态记录。
- [x] 将当前任务、完成勾选、经验反馈和详情入口收敛为简单看板。
- [x] 普通任务使用至少 44px 的整行触控区和 20px 可见状态圆；完成后显示勾选与标题划线。计数任务整行增加一次，背景按比例填充。
- [x] 复选框完成后即时结算经验并提供撤销；复杂反馈保留在次级入口。
- [x] 勾选复用现有幂等 XP 账本并立即刷新为可撤销状态；复杂反馈仍保留在次级入口。
- [x] 手动任务默认只输入一句“我现在想做什么”；快速添加不伪造“临时任务”、五分钟或最低动作，用户主动填写的计划信息只在详情中展示。
- [x] 创建习惯默认只要求一句“我想养成什么”，并默认启用计划日打卡；频率、次数、触发条件、成长归属、难度和暂停状态保留为可选调整。
- [x] 为自动生成和手动创建的目标、任务、习惯统一提供详情入口；任务详情与修改弹层在可用视口内居中展示。
- [x] 详情支持修改、状态更新、暂停/结束、重规划和历史保留，不因简化创建流程损失控制权。
- [x] 删除任务页标题旁的全局“管理”模式；任务、目标和习惯的编辑与删除只放在对应条目或详情中，删除前统一确认并保留历史。
- [x] 任务修改弹层改用新用户可直接理解的名称和操作；系统依据默认折叠为“为什么这样建议”，展开后也不暴露内部术语。
- [x] 成长页默认只用于查看证据和变化；“建立成长分支”等根资产/父分支维护入口已收进“高级维护（可选）”。
- [x] 设置作为底部第五个固定入口，位于最右侧；系统页按人物、状态自评、AI、显示、数据与高级规则渐进展开。
- [x] 伙伴承担解释和调整入口，不再主要充当导航按钮。
- [x] 结束今天时展示成功证据、未解决阻力和明天准备继续的一步。
- [x] 日终收束现在显示成功证据、未解决任务选择和已有复盘中的“明天准备继续的一步”；没有依据时明确允许不预先规划。
- [x] 保证 320px、200% 字体、键盘、屏幕阅读器和减少动画模式可用。

#### P0.6 重组功能页面

- [x] 主导航调整为“今日、任务、记录、轨迹、设置”五项，设置固定在最右侧。
- [x] 日历、某日回顾和周复盘从轨迹上下文进入；设置不再占用今日页顶部空间。
- [x] 将日历、某日回顾、成功证据、成长变化和周复盘整合进“轨迹”，通过页内分段和按时出现降低导航层级。
- [x] “轨迹”上下文已提供日历、成长证据和周复盘三段入口；旧路由保留，降低跨页面寻找成本。
- [x] 将目标、任务和习惯统一放入“任务”，默认看板优先，管理功能按需展开。
- [x] 将人物、状态自评、AI、显示、数据与隐私放入独立设置页，并使用渐进展开避免一次展示全部选项。
- [x] 生活分身面板只解释当前方向和行动；系统旧地址继续兼容并跳转到固定设置入口。
- [x] 将领域、记忆和成长树维护放入高级系统，不占主导航。
- [x] 为所有旧入口建立清晰迁移关系，避免同一功能在房间、快捷区和底部导航重复出现。
- [x] 已走查首次使用、每日执行、目标修改、任务反馈、习惯暂停、某日回顾、周复盘和备份八条路径，并有浏览器回归覆盖。

#### P0.7 接入真实 AI（个人 Android 直连）

- [x] `.env` 仅保存于本机且被 Git 忽略；Gradle 将密钥写入 Android 原生 `BuildConfig`，WebView JavaScript 不读取密钥。
- [x] 原生插件只允许 HTTPS MiniMax 中国区接口，在 Java 层添加鉴权，并限制超时和响应体大小。
- [x] 每日整理、目标拆解、任务反馈、周复盘共用同一严格合约、提示词、证据校验、安全门和最多三次格式修正。
- [x] 默认使用 MiniMax-M3 并关闭 thinking，避免 M2.x 思考内容耗尽输出配额；服务端代理仅保留为浏览器开发和未来分发兼容路径。
- [x] `npm run check:android-release` 已验证密钥不进入 Web 产物、同步 WebView 并成功构建 Android APK，不再要求自建 HTTPS 中转服务。
- [x] `npm run eval:ai:direct` 已使用真实 MiniMax 中国区接口完成 16 类、17 条评估，覆盖目标拆解、每日指导、任务反馈、周复盘、注入与敏感场景，结果 17/17。
- [x] Android 设置支持选择 MiniMax-M3/M2.7，并保存或清除本机自定义密钥；保存后立即启用 AI 入口，清除后恢复安装包配置，自定义密钥不进入导出备份。
- [x] 断网或服务失败时保留记录、手动目标、任务反馈和本地成功日记闭环。

#### P0.8 建立温和召回与数据安全提示

- [x] 设计可关闭的“今天留下一件真实事情”提醒。
- [x] 今日无记录时显示一次可关闭的“今天留下一件真实事情”提示；一句话即可，支持写一句或今天先不用。
- [x] 设计可关闭的“昨天的下一步要继续、缩小还是放下”提醒。
- [x] 有昨日复盘下一小步时只显示一个可关闭决策入口；点击后打开已预填的任务确认框，用户确认后才加入今天，不再跳到空任务板。
- [x] 每日整理允许没有可靠下一步；空值明确表示“不预先安排”，不再把“暂不额外安排”占位文案误当成任务。
- [x] 中断后任务只进入一次性“等待处理”入口，BONUS 不补课、不顺延，不展示断签或责备文案。
- [x] 增加定期本地备份提醒，说明持久存储不等于备份。
- [x] 有记录且超过 30 天未导出时显示可关闭的本地备份提醒；导出成功后重新计时。
- [x] 第一版不申请通知权限；后续只有在用户理解价值并主动开启后才允许请求。

#### P0.9 修复 30 天闭环审计阻断

- [x] 将原 14 天账本脚本扩展为确定性的 30 天组合模拟，复用正式 IndexedDB、目标拆解落库、日记版本、五维状态、MAIN/支线/BONUS、全部反馈类型、晚完成、撤销、每日整理、周复盘、记忆、小组件与备份流程。
- [x] 30 天模拟完成 30 个日历日、27 个活跃日、3 天中断、27 条日记、8 次每日整理、4 次周复盘、53 条有效任务反馈、4 个里程碑和 436 XP；除明确隔离的已知缺口外，最终 pending debt 为 0。
- [x] 使用正式浏览器页面实走首次记录、目标、反馈、习惯、BONUS、今日指导、轨迹和周复盘；将确定性阻断固化为可重复的自动化 TODO，而不是只写主观结论。
- [x] 修复今日页与人物指导选错 MAIN：任何已反馈旧 MAIN 都不能遮住当前真正待完成 MAIN；任务板同步改为待处理优先、已反馈折叠。
- [x] 修复 AI 初始下一步误绑首里程碑：更小的下一步不得提前完成更大的里程碑或结算 50 XP。
- [x] 修复新习惯动量：从创建后的真实计划日开始，创建前日期、暂停期和结束后日期不算欠账；频率变化保留历史口径。
- [x] 统一习惯动量与周复盘合约的小数口径，确保合法的 `0.7/5` 等值不会阻断周复盘。
- [x] 暂停、结束或移出 BONUS 时，无惩罚收束当天已经生成但尚未反馈的习惯任务，不让它跨日成为待处理债务。
- [x] 修复里程碑撤销后重做：恢复唯一有效的 50 XP，并把账本日期更新为真实重做日期。
- [x] 统一系统任务的 XP：恢复任务和周复盘实验要么带合法成长方向并真实结算，要么不展示虚假可得 XP。
- [x] 修复含目标后续行动的备份恢复；目标后续行动可通过合法备份校验并完整恢复。
- [x] 成功日记改用明确元数据或用户确认事件，不再依赖正文保留固定提示前缀；“写下一条小成功”直接进入对应输入状态。
- [x] 周复盘增加一击采用；仅在用户选择修改时展开主题、假设、最小动作、指标、日期和停止条件。
- [x] 任务完成后在原操作面即时展示本次 XP、总 XP/等级变化和具体成长证据，不要求用户另去轨迹页寻找结算。
- [x] 上述 P0 修复完成后补齐正式页面逐日推进的 30 天组合回归，并完成 Android Studio 模拟器完整闭环；真机仍按统一验收批次执行。
- [ ] 另行进行真人 30 天封闭使用，验证理解成本、压力感、信任和留存；确定性模拟与工程人员走查不能代替真人结论。

### P1：让电子分身可以随时陪伴

#### P1.1 Android 今日任务小组件

- [x] 完成小、中、大三种今日任务布局和隐私模式；分别显示最多一、二、四项待办。
- [x] 只展示今天尚未完成的 MAIN、BONUS 和支线任务；移除伙伴头像、状态、经验、记录入口和其他重复信息。
- [x] 支持逐项勾选完成、打开并定位对应任务；具体反馈与撤销仍回到同一任务板完成。
- [x] 小组件完成任务复用同一结算账本，保证幂等、撤销和离线可用。
- [x] 今日任务创建、修改、反馈或撤销后正确刷新；过期快照不会冒充今天的任务。
- [x] Android Studio API 34 模拟器的真实 Pixel Launcher 已验证添加组件、空态、今日待办、逐项完成、隐私开关及任务定位；标题、待办数、隐私操作和任务行已按固定基线重排，短行单行省略。
- [x] 设置页提供一次“添加到桌面”入口；已存在组件时不重复请求，不支持固定组件的系统不展示入口。
- [x] 已打开 App 接收小组件动作时不再重载 WebView；真实模拟器验证进程和页面标记保持不变，任务只结算一次并刷新组件。
- [ ] 后续统一真机验收：重启、离线、后台限制、字体放大、深色模式和不同桌面尺寸；按用户决定不在当前开发批次执行。
- [x] 不申请常驻悬浮窗权限，不在第一版建立 PC 桌宠进程。
- [x] 完成 WebView IndexedDB 到原生小组件的版本化快照同步；原生只保存展示快照和一次性用户动作，不建立第二套业务数据库。
- [x] `qiguang.widget-snapshot.v1` 只包含生成时间、本地日期和最多六项待完成的今日任务；完成动作回到 WebView 复用现有结算链路。

#### P1.2 伙伴长期记忆与指导阶段

- [x] 将用户确认的目标、限制、有效方法、禁用建议和稳定偏好形成可查看的伙伴记忆。
- [x] 区分初识、熟悉、默契和成长阶段，不按登录天数机械升级。
- [x] 伙伴引用历史时展示证据日期，并允许用户纠正。
- [x] 支持温和、直接等指导强度偏好；只改变伙伴措辞，不改变事实、任务规则或安全边界。
- [x] 已被用户掌握的方法可由用户标记为减少提醒，仍可查看和恢复主动提醒。

#### P1.3 人物与房间反馈

- [x] 让倾听、思考、指导、庆祝、恢复和周复盘对应明确人物状态。
- [x] 主线存在时显示“指导”状态；倾听、思考、庆祝、恢复和放松状态已有真实回访、cue、时间或完成事件来源。
- [x] 生活分身状态由真实 cue、时间动作、回访和已确认庆祝事件驱动，显示“倾听/思考/恢复/庆祝/放松”等状态，不随机抽取情绪。
- [x] 表情、状态标签和语言响应具体事件，不用随机情绪假装理解用户。
- [x] 用累计成长证据解锁克制的永久房间变化，不依赖连续登录；完成里程碑后房间显示纪念物，累计三个升级样式。
- [x] 保持正式男女角色身份一致，并遵守透明、居中和减少动画要求；资产与减少动画浏览器回归已覆盖。
- [x] 将男女伙伴默认姿势统一为温和开心、正面对屏幕；女生不再使用委屈或哭泣的默认帧。
- [x] 重做步行、停留、回正面和事件表情的逐帧状态，删除平移、拉伸和旋转模拟动作。
- [x] 以现有像素房间稿重构家具图案、热点终点与中部行走区，并完成浏览器和 Android Studio 模拟器视觉回归。
- [x] 将书桌与椅子、工作台和床补成“走近—使用—反馈—跳转/返回”交互；床休息不写入任务、经验或状态。
- [x] 删除房间叠加外框、家具热点标签框与底栏分隔框，保留场景木质边界、44px 热点和键盘焦点环。
- [x] 行走帧同时裁掉图集横向相邻姿势和纵向下一行头部；女生默认改为睁眼微笑的正面帧。
- [x] 家具往返与环境散步采用离散格步路线，动作帧、人物起伏和地面阴影使用同一节拍；帧预加载并解码后才启动，去掉连续漂移、首次闪空和太空漫步感。
- [x] 以用户原创男女头像为身份基准重新生成精细 6×6 动作表；男女均使用 36 个固定 256×256 透明单元格、统一人物比例和脚底基线，覆盖四向站立、事件姿势与四向六帧步行。
- [x] 房间已直接使用男女各 36 个独立 PNG 帧并以 `background-image` 切换动作；删除运行时图集坐标和串帧裁切，六个家具交互、环境散步、恢复动作、男女切换与离线缓存回归通过。
- [x] 用户已确认新男女人物形象与动作本身没有问题。
- [x] 男女各 36 个动作按真实透明像素边界精细裁成独立 RGBA PNG；不重新拉伸人物，四周固定保留 8px 安全边距。
- [x] 用户已逐帧确认独立 PNG 的蓝色裁切边界完全没有问题；72 帧已接入运行时并保留固定透明安全边距。
- [x] 记录改为与今日、任务、轨迹一致的完整主导航页面，保留底栏并取消特殊凸起按钮和关闭模式。
- [x] 删除房间底部三个无说明、无交互的 BONUS 花盆色块及其查询、状态与动画；习惯反馈继续由任务和成长证据页承载，真实里程碑纪念物保留。
- [x] 修正男女横向步行帧命名与实际朝向相反的问题；向左、向右六帧现在与画面方向一致。
- [x] 修正左右站立帧的运行时映射；书桌互动结束朝左，工作台互动结束朝右，与实际家具方向一致。
- [x] 六条家具路线拆成明确的水平/垂直格步段；每段位移与人物朝向一致，拐点停稳后转向，到达后面向家具。
- [x] 点击生活分身后，面板内部保持可操作；点击面板与人物以外的区域立即收起，并同步人物按钮展开状态。
- [x] 删除头饰与性别式临时角色名；选择页、设置页和伙伴展示只使用女生“鱼鱼”、男生“包包”，首次选择同步保存对应名字。

#### P1.4 多元发展与周期复盘

- [x] 支持给每个人生领域设置重点建设、稳定维持、探索观察或暂停等待；每日方向会尊重领域模式。
- [x] 周复盘检查各领域投入、恢复需求和目标停滞，并提出一个调整候选。
- [x] 月度视图回答哪些领域在进步、维持、退化或缺少证据。
- [x] 不用单一总分评价人生，不要求每天所有领域都增长；五维状态、成长证据和 XP 分离展示。
- [x] 周复盘默认只呈现一个主题和一个实验候选；用户可以接受、缩小或拒绝，不要求填写完整实验表。

#### P1.5 降低首次使用成本

- [x] 第一次使用只完成选择伙伴、留下一条记录并进入一个可确认的下一步，不要求先配置完整人生体系。
- [x] 首次记录后显示一次上下文指引，只解释“今日看建议、任务可完成/编辑/删除”，不使用整页教程或连续弹窗。
- [x] 目标、五维、成长分支和系统记忆按实际需要渐进出现，默认表单保持最小。
- [x] 首次价值体验不要求先配置完整人生体系；浏览器首次使用流程覆盖选择伙伴、记录和回看。
- [x] 无目标、目标过多、长期未记录和刚刚回归分别提供探索、名额提示、温和记录提醒和回归入口。

#### P1.6 三十天闭环审计与封闭使用

- [ ] 后续统一真机验收：使用真实 Android 构建完成新核心闭环回归；按用户决定不在当前开发批次执行。
- 本轮设备验证只使用 Android Studio 模拟器，未执行真机门禁；APK、Lint、签名和模拟器闭环已验证，不用模拟器替代真机结论。
- [x] 已提供 `npm run test:android-device` 一键真机门禁：只接受一台已授权真机、拒绝模拟器、保留数据覆盖安装、启动应用并运行原生仪器测试；人工观察项在命令结束时明确列出。
- [x] 已完成浏览器真实备份导出、删除和恢复演练；原生文件权限场景仍待 Android 真机。
- [x] 使用 `npm run test:30-day` 快速执行 30 个日历日的组合模拟，覆盖日记、目标拆解确认、MAIN/支线/BONUS 反馈、里程碑、经验、三天中断、晚完成、撤销、每日整理、周复盘、数据库重开、小组件和最终债务。
- [x] 模拟脚本复用正式 IndexedDB、方向规则、任务反馈、里程碑、XP、习惯、AI 合约、周复盘和小组件快照，不建立测试专用业务实现。
- [x] 验证反馈引用历史并改变后续行动：完成进入下一里程碑，部分完成生成缩小继续，建议不合适时停止复制，恢复优先覆盖机械推进。
- [x] 验证三天中断不会自动生成 BONUS 欠账；回归时只处理真实遗留任务，最终 pending debt 为 0。
- [x] 将 30 天模拟纳入常规 `npm run test`；当前结果为 30 个日历日、27 个活跃日、53 条有效任务反馈、4 个里程碑、436 XP。
- [x] 本轮确定性 30 天模拟与工程人员浏览器走查不代替真人连续使用；理解成本、压力感、信任和留存不伪装成已验证事实。
- [x] 修复 P0.9 阻断后，在现有 Playwright 时间控制能力上补一条逐日推进的 30 天组合场景：当天才写入数据、每天重载、从正式页面完成建议—确认—反馈—次日变化，并覆盖恢复、成功日记、周复盘和目标自然收束。
- [x] 自动验证任意一天只给出一个带理由、完成标准和最低版本的 MAIN，可作为“用户能否知道今天做什么”的功能代理。
- [x] 自动验证系统建议引用前序任务并产生有理由的变化；真人是否理解该理由仍保留在上一项访谈。
- [x] 自动验证任务看板和小组件快照直接暴露今天待完成的 MAIN、BONUS、支线及完成/打开入口；真实操作成本留待 Android 真机观察。
- [x] 自动验证中断后无惩罚、无 BONUS 欠账、无批量补课，仅保留一个待处理 MAIN；主观压力感留待用户研究。
- [x] 固定对外区分语句：栖光不是记录或管理更多事情，而是根据目标、状态和真实反馈，持续告诉用户最值得做的下一步。
- [x] 30 天严审发现的今日 MAIN、习惯动量、周复盘、里程碑重做、目标初始绑定和备份阻断均已修复；账本模拟与正式页面逐日组合回归均通过。

#### P1.7 成长徽章

- [x] 完成五类成就产品设计：徽章是现有成长证据的视觉纪念，不建立第二套事实、积分或任务系统。
- [x] 实现纯派生成就选择器：从里程碑、完整目标、习惯沉淀、恢复行动和已实践周实验派生稳定身份，不新增 Badge 数据表、备份字段或数据库版本。
- [x] 在“轨迹 → 成长证据”完成成就册摘要与详情：最近三枚、总数、来源分类、获得日期、成长方向及完整证据；展开后查看全部。
- [x] 首次满足条件后显示一次非阻塞解锁反馈；先陈述现实证据，再展示徽章，并提供“查看证据”。
- [x] 完成统一像素徽章视觉与克制房间陈列阶段；不设计稀有度、限时、随机掉落、锁定图鉴、排行榜或徽章货币。
- [x] 覆盖五类派生、去重、来源撤销、生命周期变化、离线重算、下一项可达成、320px、200% 字体、键盘、屏幕阅读器和减少动画回归。

### P2：首版已作出的暂缓决定

- [x] 首版不建立账户和加密云同步，继续使用本地数据与导出恢复。
- [x] 首版不做跨设备连续使用；云同步验证后再讨论。
- [x] 真实 AI 直连和 Android 真机验收完成前不提交应用商店。
- [x] 首版只提供 Android 桌面小组件，不做常驻悬浮桌宠或 PC 桌宠。
- [x] 留存证据成立前不增加更多房间、服装和家具；只保留克制的累计成就陈列阶段。
- [x] 首版保留月度证据视图，不做季度和年度报告。
- [x] 真实 AI 成本与留存价值明确前不设计付费方案。
- [ ] 后续接入 Microsoft To Do：由用户主动授权，可选同步每日任务与长期任务；先定义冲突、重复完成、删除和离线合并规则，本地账本仍是经验与成长结算的唯一来源。
- [ ] 后续评估接入小米运动健康等身体状态数据：仅在用户主动授权后读取必要指标，转化为可核对的状态证据；不自动诊断，不默认上传日记或原始健康数据。

## 当前已完成基线

- [x] 等级无上限；Lv.30 后每级固定 100 XP。
- [x] 无断签、负经验、植物枯萎或离开惩罚。
- [x] 每日上限为 1 MAIN、3 BONUS、2 支线。
- [x] 用户可以主动把习惯设为 BONUS，并暂停、替换或结束。
- [x] 本地记录、日历、某日回顾、版本、草稿、导入、导出和删除。
- [x] 目标、里程碑、成长分支、任务反馈、XP 账本、撤销和重算。
- [x] 简单任务列表、任务详情、完成/部分完成/跳过/豁免和经验结算。
- [x] 待处理任务支持直接编辑和删除；删除自动生成的习惯任务后保留删除标记，不会在刷新后被重新生成。
- [x] 任务页收敛为“今天/计划”；手动任务支持底部快速添加、计划日期与计数打卡，习惯可设置每日次数目标，达标后只结算一次经验。
- [x] 成功日记、本地成功证据和有证据的 AI 每日整理候选。
- [x] 记录页移除“设为成功记录”复选框，由快速开头决定类型；保存后留在原页，同一天可连续新增多条记录。
- [x] 日历快照支持独立“当日一句话”，可手写、采用 AI 概括后编辑，并随备份恢复。
- [x] 周复盘默认发送全部允许的事实摘要；发送类别只在设置中配置一次并长期保存，单次复盘不再重复逐项勾选。
- [x] 目标 AI 拆解第一版：2—5 个里程碑、下一步、发送确认和落库确认。
- [x] 2026-08-19 目标创建收敛为一句必填；说明、证据、下一步和系统分类收入可选设置，离线时也会生成并安排最小下一步。
- [x] 2026-08-19 文档结构收敛：根目录只保留工作基线，历史视觉与交互参考集中到 `docs/archive-unused-md/`，删除已被实现和当前产品逻辑取代的开发前草案。
- [x] 周复盘、系统记忆候选、事实/推断分离和权限预览。
- [x] 由用户原创素描身份参考转化的男女统一卡通头像、动作 Sprite、像素房间和时间状态动作。
- [x] PWA、IndexedDB、Service Worker、CSP、持久存储说明和离线降级。
- [x] Capacitor Android 原生壳、品牌图标、启动页、APK 和 GitHub 发布流水线。
- [x] 当前自动检查基线：`npm run check` 通过并包含确定性 30 天组合模拟；`npm run check:release` 15/16（仅原生生产密钥条件项跳过）；AI 离线 17/17；浏览器 54 条顶层流程全部通过，包含正式页面逐日 30 天闭环。
- [x] 真实 MiniMax 中国区直连的 17 个场景均已取得合约通过结果；批量评测仍可能受模型随机输出影响，非法结果会在三次有界修正后失败关闭，不写入正式状态。
- [x] 当前容器镜像：`ghcr.io/vvoyager3/compound-journal-server:0.6.4`。
- [x] 当前 Android 包名：`com.vvoyager3.qiguang`，0.6.18（versionCode 618），targetSdk 36。

## 工作记录

### 2026-08-27：首次使用、全局语言与任务控制收敛

- 首次使用：选择伙伴后直接进入记录；首次保存后只显示一次短指引，说明“今日”用于查看建议、“任务”用于完成、编辑或删除行动，不增加整页教程或连续弹窗。
- 导航与页面：首页删除重复的“开始记录”和顶部设置入口；底部固定为“今日、任务、记录、轨迹、设置”五项，设置位于最右侧。所有页面不再显示无意义的左上角小标题，任务页并列区块标题统一字号。
- 语言：用户界面统一使用“生活分类、提升方向、主要提升、当前投入、今日重点、可选任务、其他任务、阶段目标、经验、状态自评”等直白表达；内部数据字段保持兼容。
- 状态自评：原数字校准改为身体、心理、关系、工作、玩乐五道选择题，每题提供五个有参照的状态选项，保存后仍写入既有五维状态模型。
- 任务控制：待处理任务可编辑或删除；自动生成的习惯任务删除后写入软删除标记，刷新不会重新出现，历史反馈和账本不被破坏。
- 视觉密度：统一系统页和主要功能页的字号层级、段落宽度与留白；删除教程式、PPT 式说明，详细设置继续按需展开。
- 自动验证：`npm run check`、发布检查、45 条浏览器主流程与正式页面 30 天闭环全部通过；数据库 82 项通过，含任务删除后不重建。
- Android 模拟器：Debug APK 完成签名一致性检查与保留数据覆盖安装，设置、状态自评和五项底栏实图复核通过；原生仪器测试 2/2。当前未检测到真机，因此没有触碰真机安装或数据。

### 2026-08-26：记录、周复盘与计数任务收口

- 已完成：记录页删除“设为成功记录”复选框；“成功记录、发生的事情、感受、想记住”直接决定当前类型并显示小标题，保存后清空输入但留在原页，同一天可以连续新增多条记录；编辑时仍可改类和撤销。
- 已完成：周复盘默认使用全部合约内事实摘要，不再在每次生成时逐项选择；设置页提供八类长期发送范围，修改一次后重载仍保留。成长证据页删除同页重复的“打开周复盘”入口。
- 已完成：任务页整理为“每日任务”和“长期任务”；任务可指定计划日期并在“之后已安排”中查看，可选择一次完成或计数打卡。计数任务支持逐次 `+1`、`-1` 纠错，达到目标后才完成并结算唯一一次 XP；习惯可设置每日次数目标并按计划日独立生成 BONUS。
- 已完成：日历快照加入可编辑“当日一句话”，支持手写、采用 AI 已生成概括后修改，写入独立本地数据并纳入备份、恢复与旧版本迁移。
- 已完成：修正人物横向图集语义与实际移动方向，加入互斥的低频闲时动作并替换为统一运行时 PNG；日历日期在原页打开快照，Android 内容避让状态栏、挖孔和底部系统导航区。
- 后续待办：已记录 Microsoft To Do 可选同步与小米运动健康等身体状态数据接入；两项均未提前实现，继续遵守主动授权、本地账本唯一结算和健康数据最小化边界。
- 自动验证：`npm run test` 全部通过（数据库 81 项）；`npm run test:e2e` 45/45，通过多记录、记录类型、当日一句话手写/AI 草案/用户编辑、周复盘设置持久化、未来计数任务和 30 天正式页面闭环。
- Android Studio 模拟器：0.6.18（618）完成最新 Web 资源同步、Debug APK 构建、保留数据覆盖安装、冷启动、原生仪器测试、断网冷启动与版本/进程检查；`testDebugUnitTest` 和 `lintDebug` 通过。未用模拟器替代后续真机人工验收。

### 2026-08-26：今日任务小组件收敛与角色正式命名

- 已完成：确认习惯打卡闭环已存在，用户启用 BONUS 后在计划日自动生成“习惯打卡”，可完成或撤销，并复用动量、XP 与成就证据；未新增重复入口。
- 已完成：桌面小组件收敛为纯今日任务视图，小/中/大尺寸最多显示一、二、四项；删除头像、人物状态、XP、最低动作和记录入口，支持隐私、逐项完成、任务定位与过期日期保护。
- 视觉修复：标题、待办数、隐私操作与任务行改为稳定两级排版；空态居中，勾选改为空心圆，任务名保持单行省略，不再出现末行只剩少数字符；删除无引用的小组件按钮资源。
- 角色命名：女生只显示“鱼鱼”，男生只显示“包包”；首次选择同时保存角色名，旧默认称呼在界面显示时映射为对应正式名字。
- 自动验证：`npm run check` 全部通过；完整 `npm run test:e2e` 全部通过，含角色选择、习惯、任务、离线动作帧、Android 小组件动作与正式页面 30 天闭环；Android Lint 为 0 错误，Debug APK 构建成功。
- 设备结果：Pixel Launcher 模拟器完成保留数据覆盖安装、空态与角色选择实图检查；原生 2 条仪器测试通过。一次未限定序列号的 Gradle 仪器命令同时枚举到已连接真机，测试仅临时读写并恢复小组件偏好，未卸载、清数据或写业务记录。用户随后明确授权安装到真机 `25060RK16C`：Debug APK 0.6.18（618）安装成功，冷启动成功且 `MainActivity` 处于前台；小组件、重启、后台限制和不同字体倍率仍保留在统一真机验收项中。

### 2026-08-26：功能闭环修复与全量回归

- 已修复：周复盘只校验用户明确选择的记忆及其精确版本，未授权的新记忆不再误使结果过期；被选择记忆变化仍会阻止旧结果确认。
- 已修复：模型入口丢弃方向、强度、数值或字段不一致的可选状态候选；单日反例只保持观察；目标明确交付可形成待确认里程碑候选；格式修正保留上一份原始输出。
- 已修复：成功日记测试夹具遵守真实成长证据规则，正式页面 30 天回归可从展开后的完整成就册追溯旧里程碑；晚完成改期等待页面完成重算后再验证。
- 自动验证：`npm run check` 全部通过；`npm run check:release` 15/16（仅原生生产密钥条件项跳过）；离线 AI 17/17；浏览器端到端 40/40，含正式页面逐日 30 天闭环；确定性模拟结果为 30 个日历日、27 个活跃日、53 条反馈、4 个里程碑、436 XP、最终 0 待处理债务。
- Android Studio 模拟器：0.6.18（618）完成 Debug 构建、覆盖安装、冷启动、原生仪器测试、断网冷启动与进程/版本检查；未执行真机测试。
- MiniMax 直连：本轮批量评测 15/17，两个随机漂移场景随后分别单独通过；任何连续三次仍非合约输出都会失败关闭，本地记录、任务和反馈继续可用。

### 2026-08-25：30 天全功能闭环模拟与使用感受审计

- 已完成：原 14 天账本脚本升级为 `npm run test:30-day`；30 个日历日中模拟 27 个活跃日和 3 天中断，覆盖日记读写/搜索/撤销、五维状态、确认后的目标拆解、MAIN/支线/BONUS、完成/部分/跳过/豁免、晚完成、反馈撤销、8 次每日整理、4 次周复盘、记忆确认、小组件和中途备份。
- 模拟结果：27 条日记、53 条有效任务反馈（完成 42、部分 3、跳过 5、豁免 3）、4 个已完成里程碑、436 XP、最终 0 个待处理任务；三天未打开期间没有自动生成 BONUS 或补课任务。
- 已完成：使用正式浏览器界面复现目标、任务、习惯、BONUS、今日指导和周复盘；新增自动化待修证据，覆盖初始下一步误结算里程碑、今日页遗漏真正待办 MAIN、新习惯历史欠账、合法小数动量阻断周复盘、暂停习惯遗留 BONUS、里程碑撤销后重做不恢复 XP，以及目标后续任务导致备份无法恢复。
- 工程模拟使用感受：第 1 天记录入口和一句话创建足够轻；第 2—7 天 `0.7/5` 动量和今日页看不到真正待办 MAIN 会快速破坏信任；第 2—4 周周复盘会被“习惯动量无效”直接阻断，产品会退化成日记加打卡列表；任务完成后的即时 XP/具体成长反馈偏弱，周复盘确认和“更新下一步”仍暴露过多系统字段。
- 证据边界：以上“使用感受”是确定性模拟与工程人员实际点击后的产品观察，不是 30 天真人留存、压力感或信任研究；真人封闭使用继续保持未完成。
- 验证：`npm run check` 通过；`npm run check:release` 15/16（原生生产密钥条件项按预期跳过）；浏览器 33 条顶层流程执行完成并记录 4 条业务 TODO；AI 离线 17/17、真实 MiniMax 中国区直连 17/17；Android Studio API 34 模拟器完成构建、覆盖安装、原生仪器测试、离线冷启动，0.6.17（617）保持可启动；未执行真机测试。

### 2026-08-25：生活分身面板点击外部关闭

- 交互修复：点击人物继续展开“今天想从哪里开始”指导面板；点击面板内部不关闭，点击人物与面板以外的任意区域立即收起，人物按钮的 `aria-expanded` 同步恢复为 `false`。
- 实现保持现有房间面板结构，只增加一次根层点击委托，不引入新组件或依赖。
- 自动验证：`npm run check` 全部通过，`npm run check:release` 15/16（仅原生生产密钥条件项按预期跳过），浏览器端到端 31/31；人物面板回归覆盖内部点击、外部关闭、展开状态同步和再次打开。
- Android Studio 模拟器验证：0.6.17（versionCode 617）完成构建、安装、冷启动和仪器冒烟，包名 `com.vvoyager3.qiguang`，targetSdk 36。

### 2026-08-25：点击移动朝向与格步逻辑

- 根因修复：男女原横向动作文件名与人物实际朝向相反，导致工作台等路线出现“面朝左、向右滑”的月球漫步；现已按真实画面交换左右命名并保持运行时语义一致。
- 路线修复：书桌、任务板、日历、工作台、窗边和床统一采用两段正交格步；每段只沿一个方向移动，拐点停稳后换向，到达后再切换为家具互动姿势，返回路线按相反顺序执行。
- 自动验证：`npm run check` 全部通过，`npm run check:release` 15/16（仅原生生产密钥条件项按预期跳过），浏览器端到端 31/31；新增回归会暂停六条路线的两段动画，分别核对实际位移和当前人物朝向。
- Android Studio 模拟器验证：0.6.16（versionCode 616）完成构建、安装、冷启动和仪器冒烟，包名 `com.vvoyager3.qiguang`，targetSdk 36。

### 2026-08-25：成长徽章功能设计

- 产品定位：徽章用于纪念已经成立的现实成长证据，不承担新积分、任务、稀有度、收集进度或每日方向权重。
- 最小模型：首版只从已确认完成且经验已结算的里程碑派生，稳定身份直接使用 `milestone:<id>`，不新增 Badge 数据表、经验来源、备份实体或数据库版本。
- 闭环设计：现实行动形成证据 → 用户确认里程碑完成并沿用原有 50 XP 结算 → 确定性派生徽章 → 一次轻量反馈 → 在成长证据页查看来源；来源事实撤销时同步停止展示。
- 页面设计：成长证据页显示最近三枚和总数，详情展示日期、目标、成长方向及原始证据；首版不进入今日页、房间和桌面小组件。
- 反成瘾边界：不奖励登录、连续打卡、任务创建或纯次数，不做稀有度、限时、随机掉落、锁定图鉴、排行榜、分享奖励与徽章货币。
- 本次只完成产品设计并拆分实施 TODO，尚未写入功能代码或提高应用版本号。

### 2026-08-25：14 天闭环严审与离散格步动画

- 产品逻辑更新：“今天不做”只停止当天重复提醒，次日必须恢复人工处理入口；昨日复盘的可靠下一步直接进入预填确认框，没有可靠下一步时允许为空。
- 功能修复：目标不再因任一历史跳过永久退出推荐；次日会显示上次行动并允许继续、缩小或改写。日终只读取当天整理的下一步，不再把昨日内容错当成明天计划。
- 动画修复：男女各 36 个动作帧预加载并解码后才启动；家具移动和环境散步改为离散格步，换帧、落脚起伏和地面阴影同步，去掉连续漂移、首次闪空、动态裁切和额外滤镜。
- 场景简化：彻底删除底部三个无说明、无交互的 BONUS 花盆色块及其数据查询和庆祝状态；里程碑纪念物保留。
- 14 天实测结论：账本模拟通过，结果仍为 14 天、12 个活跃日、24 条反馈、4 个里程碑、279 XP、0 待处理债务；严格审计确认它不能替代逐日页面使用或真人留存证据，完整组合场景已保留为 TODO。
- 自动验证：`npm run check` 全部通过，`npm run check:release` 15/16（仅原生生产密钥条件项按预期跳过），浏览器端到端 30/30；Android Studio 模拟器已完成 0.6.15（versionCode 615）构建、安装、冷启动和仪器冒烟测试，现场截图确认房间底部无多余色块。

### 2026-08-25：伙伴素描头像统一替换为卡通版

- 产品逻辑更新：所有用户可见伙伴头像统一使用由用户原创素描身份参考转化的暖色像素卡通版；素描原稿只保留为内部身份参考，不进入 App、设置预览、开发原型或 Android 桌面小组件。
- 已完成：使用用户原创男女头像与已确认的正面动作帧分别生成 `avatar-female-cartoon.png`、`avatar-male-cartoon.png`；两张头像均为 1254×1254 PNG，统一正面睁眼开心表情、像素线条、暖旧纸背景和安全裁切。
- 已完成：App 内人物选择、生活分身面板、周复盘、设置预览、开发前可点击原型与 Android 小组件全部切换到卡通 PNG；原生同步产物只包含两张 `avatar-*-cartoon-*.png`，不再打包素描 JPG。
- 自动验证：`npm run check` 全部通过；`npm run check:release` 15/16（原生密钥条件项按预期跳过）；浏览器端到端 30/30，并同时验证男女卡通头像与对应房间人物身份一致。
- Android Studio 模拟器验证：0.6.14（versionCode 614）在 API 34 模拟器完成保留数据覆盖安装、仪器测试、断网冷启动；现场截图确认生活分身面板显示卡通头像且无棋盘格、裁切或旧素描残留。

### 2026-08-25：温暖荒野冒险界面与自然家具互动

- 产品逻辑更新：页面与房间统一为原创的温暖荒野冒险语言，使用林野绿、旧纸、古铜、暖木和少量金色微光；不复制现有游戏角色、标志、界面或场景资产。
- 已完成：全局页面、主要按钮、底部导航、设置入口、五维状态和 Android 系统栏改用统一的纸张、森林与古铜材质；房间加入随早晚变化的方向光、边缘暗角、一次性微光和克制的场景阴影。
- 已完成：家具热点增加短暂场景光环；人物走近家具的时长和节奏重新校准，补充起步准备、落脚阴影、到达停稳与使用阶段；环境散步放缓，保留减少动画下的即时导航。
- 已完成：家具反馈改为与场景一致的旧纸提示，设置入口由大块“设置与数据”收敛为图标加“设置”，五维状态移除表格式虚线与粗重空框。
- 已修复：Android WebView 冷启动初始化期间原本可能只显示空白背景；HTML 入口现立即显示“栖光 · 正在点亮房间”的主题占位，真实页面就绪后由既有渲染流程自动替换。
- 自动验证：`npm run check` 全部通过；发布检查 15/16（原生密钥条件项按预期跳过）；浏览器端到端 30/30，覆盖场景光影、热点光环、落脚阴影、家具锚点、减少动画和离线动作帧。
- Android Studio 模拟器验证：0.6.13（versionCode 613）在 API 34 模拟器完成保留数据覆盖安装、仪器测试 2/2、断网冷启动和真实页面渲染；冷启动 1 秒截图可见主题占位，约 8 秒后进入今日页，不再出现不可解释的纯空白。

### 2026-08-25：统一比例人物动作表与项目 README

- 已完成：基于用户原创男女头像重新生成精细像素角色；每名角色包含 36 个固定单元格，统一人物缩放和脚底基线，默认使用正面睁眼开心帧，并提供倾听、阅读、思考、休息及四向六帧步行。
- 已完成：将新男女 RGBA 动作表直接替换现有运行资产，房间改用精确 6×6 坐标；移除为旧图集补救的步行裁切，思考状态改用独立思考帧。
- 用户确认：新男女人物形象与动作本身没有问题；后续只调整素材裁切，不重新生成或改变角色。
- 已完成：男女各 36 帧按真实透明像素边界导出为独立 RGBA PNG，保持原始比例与像素，不逐帧拉伸，四周统一保留 8px；文件位于 `design-assets/pre-development/character-frames/`。
- 用户确认：逐帧预览中的 72 个蓝色裁切边界全部通过人工审查。
- 已完成：房间运行时改为直接加载独立 PNG，沿用既有 CSS 动画状态机并用 `contain` 统一显示尺度；Service Worker 缓存覆盖 72 帧，不再依赖 6×6 图集坐标。
- 已完成：新增根目录 `README.md`，说明产品定位、核心体验、本地运行、MiniMax 中国区配置、Android 构建、验证命令和本地数据边界。
- 素材验证：男女动作表均为 1536×1536 RGBA、36 个 256×256 单元格；72 个单元格边缘均无不透明像素，男女默认人物有效高度均为 192px，产物分别为 1.13MB 与 1.26MB。
- 独立 PNG 验证：男女各 36 帧、共 72 个 RGBA PNG 均小于原 256×256 单元格，四周边缘不透明像素为 0；发布检查 15/16（原生密钥条件项按预期跳过），`git diff --check` 通过。
- 工程验证：`npm run check` 全部通过；浏览器端到端 30/30；发布检查 15/16（原生密钥条件项按预期跳过）；Android Studio API 34 模拟器完成 0.6.12 Debug 构建、覆盖安装、原生仪器测试与断网冷启动，现场截图确认默认人物透明、居中且无相邻帧残片。

### 2026-08-24：人物素材重新切片待人工审查

- 人工复核未通过：当前 App 继续从大图集定位人物，家具交互仍有严重穿模；撤回对应完成结论，暂停继续接入。
- 已完成：保留原创像素与透明度，将男女素材分别裁成 32 张、统一 180×240 透明画布并按表情、朝向、互动和步行序号命名；步行帧按各自正面站立高度等比放大并保持脚底对齐，含书桌和任务板的复合帧另放入 `review-only-composites/` 并标记 `DO_NOT_USE`。
- 已完成：生成带文件名的男女总览图与 `design-assets/review/character-frames/index.html` 人工审查页；尚未替换 App 内素材。
- 验证：64 张候选帧尺寸一致、透明边缘无溢出、有效像素行连续；待人工逐帧确认内容质量后再接入与回归。

### 2026-08-24：家具互动与无边框游戏界面

- 产品逻辑更新：家具互动只作为真实功能的空间入口或一次非惩罚休息，不新增独立数值与装饰玩法；统一采用走近、使用姿势、短反馈和跳转/返回的链路。
- 已完成：书桌与椅子使用坐下书写帧后进入记录；工作台使用操作帧后进入成长证据；床提供短暂休息并自动走回中央；任务板、日历和窗边也在跳转前显示对应互动反馈。
- 已完成：女生默认帧切换为正面温和开心；环境散步更快进入真实脚步位移；移除房间 UI 外框、常驻热点标签框、生活分身面板硬边框和底部导航分隔线，同时保留键盘焦点反馈。
- 已修复：行走帧按横轴左右与纵轴底部共同裁剪，不再带出相邻人物头部；默认改用睁眼微笑的正面挥手帧，闭眼微笑仅用于平静或休息。
- 已修复：家具往返加入中间路径点、缓入缓出、落脚起伏和拐弯换向；环境散步同步缩短步幅节奏并加入落脚起伏，不再在线性轨迹上滑行或机械跳格。
- 已修复：写字和查看任务板旧帧自带家具图案、交互终点进入家具占地区域的问题；六类互动全部改用纯人物朝向帧并停在家具旁开放地面，不再出现双重家具或人物穿模。
- 已统一：记录页恢复四项底部主导航，删除“关闭”式退出和记录按钮特殊凸起外观，与今日、任务、轨迹保持相同页面模式。
- 已覆盖：浏览器回归新增无边框、家具使用帧、床边休息返回、减少动画静态反馈和家具锚点检查。
- 模拟器实证：书桌、任务板、日历、工作台、窗边和床六类点击动作均完成“分段接近—按路径转向—纯人物姿势—跳转/原路返回”；逐一截取交互帧确认人物位于开放地面，无家具图案重复、人物穿模或横竖轴相邻图块串入。
- 验证：`npm run check` 全部通过；发布检查 14/15（1 条原生密钥条件项按预期跳过）；浏览器业务回归 30/30；Android App Debug 与 AndroidTest 组装通过，API 34 模拟器覆盖安装、仪器测试 2/2、断网冷启动、默认开心帧、双轴裁剪、家具转向和完整记录页截图通过；远端 Release candidate #32704286650 的 verify、container、android 三个任务全部通过。

### 2026-08-24：人物逐帧动作与像素房间重构

- 已完成：男女伙伴默认使用正面温和开心姿势；女生夜间/低状态使用正面闭眼平静姿势，不再显示无依据的哭泣或委屈。
- 已完成：点击家具与环境散步改为左右/前后连续脚步帧和同步位移，动作结束回到正面；删除整个人物拉伸、旋转和单帧平移的假步行。
- 已完成：以开发前像素房间稿重做无人物环境层，记录桌、任务板、日历、书架工作台、窗边与床铺沿边缘布置，中部保持可行走，热点和终点与图案一致；时间色调、状态提示、成长花盆和里程碑仍保留。
- 已修复：重新校准记录桌、书架工作台和窗边的移动终点；五个家具热点现在都会停在对应家具旁的开放地面，新增 320px 实际坐标回归防止人物走进床、椅子或工作台。
- 已简化：删除设置页占据首屏的系统概况，不再默认展示领域名额、目标/习惯和记忆计数；人物、状态、AI、显示、存储与备份继续按需展开，完整高级能力仍保留。
- 已修复：200% 字体下短页头按钮不再逐字换行；模拟器/真机仪器测试结束后应用都会重新启动；设置页可一次请求把桌面伙伴添加到系统桌面。
- 已修复：键盘回归用 Enter 验证跳过房间，并验证记录入口保持原生链接语义与直接路由；不再断言异步重绘期间某一毫秒的焦点，避免把浏览器时序当成产品失败；桌面组件固定请求在调用 Android 8 API 的位置显式保留版本守卫。
- 验证：`npm run check` 全部通过（含 50 条数据库回归和 14 天闭环）；发布检查 14/14（1 条原生密钥条件项按预期跳过）；浏览器业务回归 29/29；Android Studio API 34 模拟器覆盖安装、仪器测试 2/2、断网冷启动和最终截图通过；Android 单元测试、Lint 与 Debug APK 共 233 项 Gradle 任务通过；`git diff --check` 无错误。
- 仍待真机：不同厂商 WebView 的动画节奏、长时观看舒适度和桌面组件固定确认窗口不以模拟器结果替代。

### 2026-08-24：Android 小组件设备闭环与系统备份边界

- 产品逻辑更新：小组件唤醒已打开的 App 时不得重载 WebView；Android 系统云备份和设备迁移不复制应用私有数据，用户主动导出/导入是唯一迁移入口。
- 已修复：`MainActivity.onNewIntent()` 改为向当前 WebView 分发一次性动作事件；冷启动继续消费原有待处理动作，任务反馈、后续里程碑和 XP 账本仍复用同一事务链路。
- 已完成：Manifest 关闭系统备份，并用 Android 12+ `dataExtractionRules` 同时排除云备份与设备迁移中的文件、数据库和偏好数据；发布回归固定检查该边界。
- 模拟器实证：真实 Pixel Launcher 小组件完成 MAIN 后 App 进程和当前页面均未重载，任务完成、XP 从 0 增至 10、组件刷新；隐私模式隐藏 MAIN/XP，任务和记录跳转正确；原生导出生成 JSON 并打开系统分享面板。
- 显示与离线实证：API 34 冷启动、断网冷启动、200% 系统字体和系统夜间模式均保持页面可渲染且无横向溢出；这些结果不替代不同厂商真机。
- 验证：0.6.10（versionCode 610）模拟器仪器测试 2/2；`npm run check`、`npm run check:release`、原生发布安全 14/14、离线 AI 17/17、浏览器闭环 27/27、Android 单元测试/Lint/APK 构建全部通过。
- 仍待真机：后台回收、重启、不同桌面和组件尺寸、真实系统字体/深色模式、分享目标权限、真实 MiniMax 网络切换及长时手感。

### 2026-08-24：Android Studio 模拟回归基线

- 已完成：通过 `gh` 将本地 `codex/latest-ui` 同步到远端最新提交 `fdd4549`，并按新版 `PRODUCT.md`、`TODO.md` 与 `DESIGN-CHECKLIST.md` 继续工作；旧本地改动保留在 stash，不混入最新基线。
- 已完成：安装 Android Studio 2026.1.3.7 到 `D:\dev\android-studio`，将 IDE 数据、Android SDK、Android 用户目录、AVD 与后续缓存统一放在 D 盘；创建并启动 `Qiguang_Pixel_7_API_34`。
- 已完成：新增 `npm run test:android-emulator`，复用同一设备门禁完成 APK 构建、覆盖安装、正式包名仪器测试、断网冷启动和网络恢复；默认 `npm run test:android-device` 仍只接受真机。
- 已修复：原门禁的根级 `connectedDebugAndroidTest` 会误跑 Capacitor 子模块，并在测试后卸载正式应用；现在只组装应用测试 APK 并通过 `adb am instrument` 执行，不卸载正式应用或清除数据。
- 已修复：自定义 MiniMax 密钥保存后 AI 入口仍保持禁用、Web 端可能误走原生插件的问题；保存/清除现在即时更新可用状态，备份导出与导入边界均剔除本机密钥。
- 已修复：日终回归不再依赖执行电脑恰好处于 18:00 后；伙伴测试按最新信息架构验证今日页顶部设置入口，不再寻找已移出的伙伴面板按钮。
- 已修复：记录保存后的任务页回归会先等待保存与回顾路由完成，避免较慢 Linux CI 中异步保存回调反向覆盖测试导航。
- 已验证：0.6.9（versionCode 609）在 API 34 Android Studio 模拟器上覆盖安装、冷启动、仪器测试 1/1、断网冷启动和本地页面渲染通过；正式应用在测试后仍保持安装。
- 验证：`npm run check` 全部通过（含 50 条数据库回归与 14 天模拟）；`npm run check:release` 13/13；浏览器业务闭环 26/26；自定义密钥原生桥接 3/3；`git diff --check` 无错误。
- 仍待真机：后台限制、系统字体、深色模式、桌面小组件尺寸、系统分享与长时手感不以模拟器结果替代。

### 2026-08-23：电脑端发布门禁复核

- 已完成：在新 worktree 同步锁文件依赖并复跑逻辑、数据库、服务端、发布安全与 24 条浏览器端到端测试，全部通过。
- 已修复：Android 构建入口只执行 `:app:assembleDebug`，不再额外组装无关的 Capacitor 库归档；一键原生发布检查可稳定产出 APK。
- 已验证：原生发布安全检查 14/14，MiniMax 密钥未进入 Web 产物；JDK 21 下 Android Debug APK 构建成功。
- 已收紧：首页去掉重复主线与标签说明，记录页删除教程段落；人物手机尺寸提升到约 68×102px，修复动作表裁到两个人物的错误帧，环境动画改为播放一次后停止。
- 已固化：新增 `DESIGN-CHECKLIST.md`，后续只审查本次命中的设计条目，不再重复复述整套产品逻辑。
- 已收敛：任务板只保留一个新增入口，详细反馈与调整渐进展开；无成长证据时六个默认方向折叠为一行，成长页从 2179px 降到一屏；周复盘人物提示进入正常信息流并删除重复英文标题。
- 已收敛：日历把八条重复“缺少证据”合并为一个空态；空白日回顾只保留一次补记和一次状态校准；系统页将人物、AI、显示、存储与数据设置按需展开。390px 页面高度分别由 1717/1496/3968px 降至 1112/844/1299px。
- 已补强：任务完成后人物进入一次性庆祝状态，并由浏览器回归同时验证人物反馈、当日成功证据与成长分支；只有行动证据的日期不会被误判为空白日。
- 已简化：生活分身由两次点击改为单击直接打开；今天已有记录或行动反馈时，伙伴明确提示“可以回看，也可以停下”，首页不再重复催促记录。
- 已收敛：整理后的某日回顾默认展开待确认推断、成功证据和明天一步，已确认事件、更多复盘及整理范围按需展开；同一测试数据下页面由 3419px/845 字降至 2607px/487 字。
- 已收敛：周复盘不再逐条展示八个默认领域的“暂无证据”，空收益/消耗合并为一句，无习惯建议时不显示无关按钮；页面由 2206px/660 字降至 1354px/370 字。
- 已简化：首页删除人物下方重复的“已有记录/优先维度”说明；随机走动、读书和休息只影响动作，不再把环境动画标成用户真实的“恢复/思考”状态。
- 已简化：首页不再展开日记正文和习惯最小动作，只保留“今天的记录”入口与单击打卡；习惯管理进入任务页。
- 已收敛：任务页以“待处理/已反馈”代替容量计数；目标卡只露出下一步，编辑、里程碑与重新拆解收入“管理目标”，目标创建删去两段教程式说明。
- 已验证：静态壳升至 0.6.6，旧 0.6.5 页面能提示用户手动更新并保留本地数据；25 条浏览器闭环与发布门禁通过。
- 模拟器已复核：覆盖安装 0.6.6 后保留原记录，首页不再展开正文，任务与首页在 412px 均无横向滚动。
- 已收敛：记录页进入专注编辑模式，默认折叠日期并隐藏固定底栏；0.6.7 的浏览器回归、发布门禁和 Android 构建通过。
- 模拟器已复核：覆盖安装 0.6.7 后，记录页在 412px 宽度无横向滚动，正文区为 300px，保存按钮在首屏内且不再被底栏遮挡。
- 已收敛：首页五维状态从折叠说明改为五个直接可见的状态槽，按真实值填充；房间隐藏与底栏重复的记录、任务、成长文字，并去掉无状态含义的“在场”标签。
- 已修复：PWA 首次激活不再弹出“新版本已经激活”的说明 Toast；只有本标签明确接受更新时才重新打开。
- 已验证：0.6.8 的完整逻辑检查、25 条浏览器闭环、发布门禁与 Android Debug 构建通过；模拟器 412px 无横向滚动，覆盖安装后原记录仍存在。
- 已收敛：生活分身面板删除重复“行动建议”和详细依据；无任务时只显示“开始记录”，有方向时突出“查看今天的行动”，闭环后突出“回看今天”，设置保留为紧凑次级入口。
- 已验证：0.6.9 的完整逻辑检查、25 条浏览器闭环、发布门禁与 Android Debug 构建通过；模拟器面板约 152px 高，设置入口约 51×50px，不遮挡回应，覆盖安装后原记录仍存在。
- 模拟器已验证：0.6.5 APK 在 `emulator-5554` 冷启动，原创形象选择、系统输入法、键盘弹出后滚动与保存可用；断网冷启动后本地记录仍存在。Android 壳不再注册 PWA Service Worker，消除首启无意义更新提示。
- 模拟器已复核：覆盖安装 0.6.5 后，今日、任务和成长页可由真实底栏点击打开；系统页渐进展开且无横向滚动；四项主导航已填满底栏，不再保留旧五栏空位。真机保持未触碰。
- 仍待真机：真实 Android 的后台限制、字体放大、深色模式、小组件尺寸和长时手感继续保留为最终验收项；本轮未触碰已连接的真机。

### 2026-08-19：跨日任务处理与文档整理

- 已完成：待完成任务现在可按日期查询；今日页会展示一次“等待处理”入口，不自动失败、不扣经验、不顺延堆积。
- 已完成：用户可以直接选择已经完成、做了一部分、不再做或外部原因；也可以打开调整流程缩小后再安排。
- 已完成：反馈保存实际完成日期；晚完成 XP 记入实际完成日期，原计划日期仍保留在任务本身。
- 已完成：补充 IndexedDB 回归测试，覆盖等待处理、晚完成日期和 XP 账本日期。
- 已完成：历史 Markdown 集中到 `docs/archive-unused-md/`，根目录只保留 `TODO.md` 与项目规则文件。
- 验证：`npm run check` 通过（含新增跨日回归）；浏览器和 Android 真机待后续按 P0/P1 顺序验证。

### 2026-08-19：拆解候选与每日方向

- 已完成：目标拆解草案在用户修改目标结果、意义、完成证据或分类后自动失效，不能把旧候选继续落库。
- 已完成：每日页接入本地确定性方向选择器，明确恢复、当前 MAIN、当前目标、昨日步骤和探索的优先顺序与解释理由。
- 已完成：补充规则测试，并将目标拆解浏览器流程扩展为“生成 → 修改使草案失效 → 重新生成 → 确认落库”。

### 2026-08-19：手动任务入口简化

- 已完成：手动任务创建默认只要求一句“我现在想做什么”，系统提供理由、五分钟最小版本、预计时间、难度和成长分支默认值。
- 已完成：复杂调整字段收入“调整细节（可选）”，详情和后续编辑能力不变；同时修复拆解草案隐藏状态和键盘跳转的竞态。

### 2026-08-19：反馈驱动的下一步

- 已完成：完成目标行动后自动安排下一条未完成里程碑；使用目标、里程碑和行动指纹去重，不重复堆任务。
- 已完成：目标行动被跳过后不再机械复制；再次跳过时要求填写一个主要阻力，保留用户调整、缩小或暂停的决定权。
- 已完成：补充数据库回归，确认实际完成日期、经验结算和下一里程碑任务均可审计。
- 验证：`npm run test` 通过（16 合约、46 数据库、3 部署、10 发布检查含 2 项环境跳过、8 规则、6 服务端）；`npm run test:e2e` 通过（21/21）。

### 2026-08-19：部分完成与习惯入口简化

- 已完成：目标行动部分完成后留下部分经验和真实进展，并自动生成唯一的“缩小继续”候选；候选仍受每日任务上限约束。
- 已完成：创建习惯默认只要求一句想养成的行为；最低动作由系统生成，其余频率、触发、成长归属、难度、状态和 BONUS 设置收入可选调整。
- 验证：新增部分完成续行动数据库回归；`npm run test` 通过；`npm run test:e2e` 通过（21/21）。

### 2026-08-19：主导航收敛

- 已完成：底部主导航收敛为“今日、任务、记录、轨迹”四项；日历、某日回顾和周复盘仍保留在轨迹上下文与房间入口中。
- 已完成：路由兼容旧地址，进入日历、某日回顾或周复盘时仍正确高亮“轨迹”。
- 验证：增加主导航浏览器断言；`npm run test` 与 `npm run test:e2e` 均通过（21/21）。

### 2026-08-19：备份提醒与温和召回

- 已完成：有本地记录且超过 30 天未导出时，系统显示一次可关闭的备份提醒；导出成功后重新计时。
- 已完成：提醒文案明确持久存储不等于备份，不要求通知权限，也不制造连续登录或惩罚。
- 验证：`npm run test` 通过；新增“备份提醒可关闭”浏览器回归通过。

### 2026-08-19：桌面伙伴快照边界

- 已完成：新增版本化只读小组件快照，集中输出当前 MAIN、最多三个 BONUS、伙伴身份和 XP 等级。
- 已完成：快照写入浏览器/WebView 本地存储；不复制任务结算、XP 或状态规则，原生小组件后续只需读取并回到 App 执行业务操作。
- 验证：新增快照规则测试；`npm run test` 通过（规则测试 9 项）。

### 2026-08-19：任务板勾选完成

- 已完成：待处理任务卡增加触控安全的“完成”勾选，勾选后直接复用现有反馈、XP 账本和下一步重规划。
- 已完成：部分完成、跳过、详细反馈、调整/顺延仍保留为同卡片次级操作；完成后可直接撤销。
- 验证：更新任务反馈浏览器流程；`npm run test` 通过；`npm run test:e2e` 通过（22/22）。

### 2026-08-19：硬截止时间

- 已完成：任务可选设置硬截止时间，计划日、截止时间和实际完成日期分别保存。
- 已完成：截止后任务只显示“截止已过，仍由你决定”，不会自动失败、扣经验或顺延；调整入口可修改或清除截止时间。
- 验证：数据库保存/回读测试、构建和 `npm run test:e2e`（22/22）均通过。

### 2026-08-19：BONUS 不积压

- 已确认：BONUS 习惯只按当天计划生成；错过的日期保留待处理事实，但不会复制到下一天、补课或产生欠账。
- 验证：扩展习惯数据库回归，明确检查前一天待处理 BONUS 不影响今天生成；`npm run test` 通过。

### 2026-08-19：日终下一步

- 已完成：结束今天的收束对话现在显示已有复盘中的“明天准备继续的一步”；如果没有可靠依据，会明确告诉用户不必提前规划。
- 已完成：保留记录、任务反馈、成功证据和未解决选择四部分，下一步可以直接打开任务板决定。
- 验证：更新成功日记浏览器回归；对应流程通过。

### 2026-08-19：轨迹分段入口

- 已完成：日历、成长证据和周复盘页面增加统一的“轨迹”分段入口，并保留旧地址兼容。
- 已完成：当前分段使用明确焦点和 44px 触控区域，避免主导航重新膨胀。
- 验证：构建与核心页面响应式回归通过。

### 2026-08-19：五维状态渐进展示

- 已完成：今日页将五维状态收起为一句近期状态判断和可展开证据；某日回顾保留完整五维明细，校准入口不丢失。
- 验证：`npm run test` 通过；`npm run test:e2e` 通过（22/22）。

### 2026-08-19：截止风险进入每日方向

- 已完成：每日方向选择器识别主线截止已过或 24 小时内临近的情况，优先解释为什么先处理它。
- 已完成：风险提示不改变任务状态、不自动扣分，仍明确保留用户决定权。
- 验证：新增方向规则测试；`npm run test` 通过。

### 2026-08-19：今日方向截止排序

- 已完成：每日方向选择器把主线可选截止风险纳入优先级，截止已过或 24 小时内时给出更明确的开始理由。
- 已完成：风险排序只改变解释和入口，不改变任务状态及经验规则。
- 验证：方向规则测试与构建通过。

### 2026-08-19：今日温和记录提醒

- 已完成：今日没有记录时显示一次可关闭的“今天留下一件真实事情”提示；只需一句话，不要求分类或完整复盘。
- 已完成：提供“写一句”和“今天先不用”两个选择，提醒不会累积或责备。
- 验证：新增空今日页面浏览器回归通过；`npm run test` 通过。

### 2026-08-19：伙伴设置入口

- 已完成：生活分身面板增加“设置与数据”，普通设置和数据隐私不再需要从主导航进入。
- 已完成：四个伙伴动作在 320px 下改为两列布局，保持触控区域和可见性；状态旧入口继续兼容。
- 验证：伙伴面板浏览器回归通过；`npm run test` 通过。

### 2026-08-19：昨日下一步召回

- 已完成：有昨日复盘下一小步时，今日页显示一次可关闭决策提示，用户可去任务板继续、缩小或放下。
- 已完成：提示不会自动复制、顺延或形成欠账，保留用户对行动的最终决定。
- 验证：完整 `npm run test:e2e` 通过（22/22）。

### 2026-08-19：人物状态反馈

- 已完成：生活分身根据真实回访、恢复 cue、主线思考、时间动作和确认完成事件显示“倾听/恢复/思考/庆祝/放松”等状态标签。
- 已完成：状态来源是确定性事件和当前上下文，不随机模拟情绪。
- 验证：构建、规则测试和房间浏览器回归通过。

### 2026-08-19：任务管理入口审计

- 已确认：目标、里程碑、习惯和任务已统一在任务板管理，创建后的编辑、暂停/结束、重规划和历史保留入口均可用。
- 已完成：同步收敛 TODO 中对应重复未勾选项，避免同一能力被重复追踪。

### 2026-08-19：每日方向能力审计

- 已确认：今日方向选择器只给出一个带理由的开始方向；恢复、截止风险、MAIN、目标、昨日步骤和探索均按确定性优先级处理。
- 已确认：任务板保留每日硬上限，并提供完成、部分完成、跳过、缩小、替换、顺延和详细反馈入口。
- 已完成：同步收敛已被规则测试、数据库约束和浏览器流程证明的 P0.3 TODO 项。

### 2026-08-19：伙伴阶段与证据日期

- 已完成：系统记忆卡片直接列出证据发生日期，保留原有编辑、确认和遗忘入口。
- 已完成：根据已确认记忆数量与证据日期丰富度显示“初识/熟悉/默契/成长”阶段，不使用登录天数或连续打卡升级。
- 验证：`npm run test` 通过；完整 `npm run test:e2e` 通过（22/22）。

### 2026-08-19：目标分类建议

- 已完成：目标只填写一句话时，按明确关键词建议生活领域与成长方向；无法判断时不强行归类，用户仍可在可选设置中修改。
- 已完成：目标优先级选项改为面向用户的自然语言，并继续根据现有活动目标自动给出默认建议。
- 验证：构建与 `npm run test` 通过。

### 2026-08-19：P0/P1 路径收敛审计

- 已确认：今日页先给一个有理由的开始方向，任务详情和轨迹入口按需展开；系统设置不再占用主导航。
- 已确认：首次使用、长期中断、无目标和任务反馈路径均保留本地可执行闭环，逾期与 BONUS 不会累积成欠账。
- 验证：完整浏览器回归 22/22；`npm run test` 通过。

### 2026-08-19：里程碑依赖方向

- 已完成：今日方向读取当前重点目标的待完成里程碑；有依赖时在理由中明确说明，不再只显示泛化的“推进目标”。
- 验证：方向规则测试、`npm run test` 和完整 `npm run test:e2e`（22/22）通过。

### 2026-08-19：目标停滞提示

- 已完成：每日方向检查目标开始时间与近 7 天目标任务反馈；缺少推进证据时明确提示“先缩小一步”，不扣分、不自动结束目标。
- 验证：方向规则新增停滞分支测试；`npm run test` 通过。

### 2026-08-19：已验证项归档审计

- 已将 TODO 中已有明确实现、重复记录或已有浏览器证据的项目同步勾选，保留需要真实 Android、真实模型或长期使用才能证明的项目为未完成。
- 当前未勾选项不代表删除需求，而是明确区分“代码已实现”和“外部条件尚未验证”。

### 2026-08-19：领域模式进入每日方向

- 已完成：每日方向读取目标所属领域的阶段模式；暂停领域不再自动成为建议目标，维持和探索领域使用对应的指导理由。
- 验证：规则分支、构建和 `npm run test` 通过。

### 2026-08-19：周复盘领域候选

- 已完成：周复盘增加各领域本周推进证据统计；重点建设但无证据时只提出一个最小调整候选，支持去任务板安排或暂不调整。
- 已完成：保留原有单一主题与 ONE EXPERIMENT 结构，不将领域统计扩展成总分。
- 验证：构建、`npm run test` 和完整 `npm run test:e2e`（22/22）通过。

### 2026-08-19：今日入口收敛与键盘焦点

- 已完成：移除今日页底部重复的记录/任务快捷区，避免与方向卡重复；修复跳到主要内容时被路由标题自动聚焦覆盖的竞态。
- 验证：`npm run test` 与完整 `npm run test:e2e`（22/22）通过。

### 2026-08-19：任务里程碑来源

- 已完成：任务新增显式 `milestoneId` provenance；目标跟进任务、备份导入、数据校验和任务板显示均保留具体里程碑关系。
- 已完成：无效里程碑、跨目标里程碑或非目标任务关联会在数据库边界拒绝。
- 验证：数据库跟进任务测试、构建和 `npm run test` 通过。

### 2026-08-19：任务完成标准

- 已完成：Quest 增加 `completionCriteria`；任务板显示完成标准，普通任务自动采用最低版本，里程碑任务采用里程碑证据。
- 验证：构建、数据库回归、`npm run test` 和完整 `npm run test:e2e`（22/22）通过。

### 2026-08-19：目标路径调整入口

- 已完成：目标来源任务出现方向不合适时，可直接编辑关联目标或暂停目标；暂停需用户确认，保留任务、反馈和经验历史。
- 验证：构建与 `npm run test` 通过。

### 2026-08-19：成长页维护入口收敛

- 已完成：成长页默认展示成长证据、目标、习惯和经验变化；建立分支的结构维护入口移入可展开的高级维护区。
- 验证：构建与 `npm run test` 通过。

### 2026-08-19：里程碑房间纪念物

- 已完成：房间根据已完成里程碑数量显示克制的纪念物，达到三个里程碑切换样式；来源是累计成长证据，不是连续登录。
- 验证：构建与 `npm run test` 通过。

### 2026-08-19：人物指导状态

- 已完成：存在待处理 MAIN 时，生活分身显示“指导”状态；其余状态继续由回访、恢复 cue、时间动作和确认事件驱动。
- 验证：构建与 `npm run test` 通过。

### 2026-08-19：失败与跨日回归归档

- 已确认：重复跳过会要求阻力并停止机械复制；晚完成、硬截止、BONUS 不积压和中断路径均有数据库或浏览器回归覆盖。

### 2026-08-19：产品词典固化

- 已完成：在模型层固化领域、近期状态、目标、里程碑、习惯、任务、成长证据和系统记忆的唯一语义边界，避免把五维状态误当人生目标。
- 验证：类型检查、构建和既有数据/合约测试通过。

### 2026-08-19：指导方式偏好

- 已完成：系统设置增加“温和/直接”指导方式，旧数据默认温和；伙伴提示会按选择调整措辞。
- 已完成：指导方式纳入备份校验与恢复，非法值会被拒绝，事实、经验和安全规则不受影响。
- 验证：`npm run test` 通过；完整 `npm run test:e2e` 通过（22/22）。导入测试确认本地设置优先，不会被备份静默覆盖。

### 2026-08-20：Markdown 文档收敛

- 已完成：根目录只保留 `AGENTS.md` 与 `TODO.md`；历史视觉、线框和像素交互参考统一放入 `docs/archive-unused-md/design/`。
- 已清理：删除与现行实现、TODO 和自动化测试重复的旧产品规格、技术架构、AI 合约、开发计划与审查草案，避免后续误把历史稿当作开发基线。
- 已修正：保留的视觉参考文档改为指向当前资产目录，不再引用已删除的旧文档。

### 2026-08-20：目标拆解信息与月度领域变化

- 已完成：目标拆解严格合约补齐当前阶段、预计投入和关键风险；与既有最终完成证据、2—5 个里程碑、下一步及待确认假设一起在落库前展示，仍只生成可编辑候选。
- 已完成：日历增加月度领域变化，按任务反馈的真实完成日期比较本月与上月证据；区分进步、维持、退化、缺少证据和暂停观察，当前月不会因尚未结束而提前判为退化。
- 已修复：成长页“高级维护”在 320px 下达到 44px 触控高度，并让回归失败直接输出不合格元素。
- 验证：`npm run test` 全部通过；`npm run eval:ai` 离线 17/17；`npm run test:e2e` 完整 22/22 通过。

### 2026-08-20：产品术语收敛

- 已完成：用户界面统一使用“人生领域、成长方向、推进优先级、里程碑”；“近期状态、目标状态、任务结果”各自承担唯一含义。
- 已完成：数据模型为 `Area` 与 `GrowthBranch` 明确对应的用户语义；内部存储类型不做无收益重命名。
- 验证：发布候选测试新增旧界面术语扫描；`npm run test` 全部通过。

### 2026-08-20：执行证据驱动的目标重拆

- 已完成：活动目标可从目标卡发起重新拆解；发送预览逐条展示并允许取消勾选真实任务反馈，权限包络严格绑定任务 ID。
- 已完成：新路径仍需用户编辑并确认；确认后旧待办和未完成里程碑保留为“已被新计划替换”并退出行动面，不删除历史、不扣经验，新下一步可直接安排到今天。
- 已完成：旧里程碑不能再被误确认或结算；目标、旧路径收束和新里程碑写入同一事务。
- 验证：新增合约权限测试、数据库原子替换测试和浏览器完整重拆流程；`npm run test`、离线 AI 17/17 与完整浏览器回归 22/22 均通过。

### 2026-08-20：目标拆解边界与过期保护

- 已完成：严格拒绝缺少必要假设的模糊目标、未说明冲突的并行重点、凭空添加截止日期、把完整目标冒充下一步及其他不可执行草案。
- 已完成：目标、已确认记忆、关联任务或执行反馈在 AI 请求后发生变化时，旧草案不再显示或写入；数据库再以目标版本校验阻止并发覆盖。
- 验证：`npm run test` 全部通过（17 项合约、48 项数据库及其余发布/规则/服务端检查）；离线 AI 17/17；完整浏览器回归 22/22。

### 2026-08-20：指导闭环、伙伴记忆与 Android 小组件

- 已完成：每日方向按恢复、截止、里程碑、停滞、状态容量和领域证据确定性排序；目标任务完成会结算关联里程碑并生成不同的下一动作，撤销时同步回滚里程碑与经验。
- 已完成：不合适的建议可由用户选择编辑、重新拆解、暂停或结束；调整前展示任务事实、目标/里程碑依据、历史反馈与用户确认记忆，今天的反馈会改变次日推荐。
- 已完成：伙伴面板以解释当前方向为主，支持用户直接告诉伙伴限制、偏好和有效方法；已掌握内容可减少主动提醒，周复盘拥有独立人物状态。
- 已完成：Android 原生小组件具备小/中/大布局、隐私模式、MAIN/BONUS/经验/伙伴状态展示、完成 MAIN 与打开任务/记录入口；原生动作回到 WebView 复用同一任务反馈和 XP 账本，不申请通知或悬浮窗权限。
- 验证：`npm run check` 全部通过（17 项 AI 合约、49 项数据库及其余规则/发布/服务端检查）；离线 AI 17/17；发布检查 11/11（原生生产地址项按条件跳过）；完整浏览器回归 23/23；Android `assembleDebug` 使用 JDK 21 构建成功；`git diff --check` 无错误。
- 仍待外部验收：真实 HTTPS 模型服务、Android 小组件的不同桌面/后台/重启真机验证、完整新闭环真机回归和 14 天封闭使用。

### 2026-08-20：Android 小组件与发布链路收口

- 已完成：小组件“任务”动作携带当前 MAIN ID，应用启动后直接聚焦对应任务卡；完成动作仍使用原有反馈、里程碑和 XP 事务。
- 已完成：中、大布局从 APK 已打包资产读取用户选择的男/女角色头像，并按最长边 256px 解码，避免 RemoteViews 传输超限；缺失资产时安全回退应用图标。
- 已完成：`npm run check:android-release` 现在依次校验 HTTPS 根地址、生产包和发布规则，再执行 Capacitor 同步与 Android APK 构建；真实评估地址和可选部署层令牌已加入 `.env.example`。
- 已完成：清理新小组件的 Android Lint 警告，包括字符串资源、Manifest 顺序、API 31 单元格提示和有理由的同步动作写入；最终 Lint 为 0 errors、18 个既有 Capacitor/启动资源提示。
- 验证：`npm run check` 全部通过；离线 AI 17/17；发布检查 12/12（真实原生地址项按条件跳过）；浏览器端到端 23/23；带占位 HTTPS 根地址的一键原生发布检查 13/13 并成功构建；恢复无远程地址后 `lintDebug assembleDebug` 成功；`git diff --check` 无错误。

### 2026-08-20：外部验收阻塞复核

- 已复核：环境中没有真实 HTTPS 服务地址、模型服务端密钥或在线 Android 设备；这些条件连续三轮未变化。
- 当前不能代替真实证据勾选：线上部署与真实模型评估、Android 重启/后台/不同桌面真机验收、完整真机闭环和连续 14 天封闭使用。
- P2 项是封闭使用后由用户作出的产品决策，不以预先实现代替结论。

### 2026-08-20：十四天快速模拟

- 已完成：新增 `test/closed-use.test.mjs` 与 `npm run test:14-day`，在固定 14 日时间线上模拟目标、MAIN、BONUS、记录、完成/部分完成/跳过/豁免、低状态恢复、两天中断、回归、小组件快照和最终结算。
- 已发现并修复：里程碑过去没有稳定顺序，同一毫秒创建时 IndexedDB 可能按随机 UUID 选择“下一阶段”；现在新里程碑保存明确 `order`，列表与跟进任务统一按顺序读取，旧备份继续兼容并使用确定性回退。
- 模拟结果：14 个日历日、12 个活跃日、24 条任务反馈（完成 14、部分完成 3、跳过 5、豁免 2）、4 个里程碑完成、279 XP、最终待处理债务 0。
- 验证：`npm run test:14-day` 通过；`npm run check` 全部通过并自动包含该模拟；发布检查 12/12；完整浏览器端到端回归 23/23。
- 保留真人验收：脚本不能证明用户主观上是否易懂、无压力、愿意留存，也不能替代 Android 桌面启动器和后台限制真机测试。

### 2026-08-20：十四天模拟后的首版取舍

- 产品结论：14 天模拟证明核心数据闭环能够持续运行、反馈会改变后续行动、中断不会制造欠账，因此继续核心方向，不重做产品定位。
- 首版边界：只推进真实 AI 服务、Android 真机验收和上线前用户研究；账户、云同步、跨设备、商店发布、常驻桌宠、更多装饰、季度/年度报告和付费全部暂缓。
- 验收口径：自动化只证明功能行为；理解、压力、信任和留存仍需真人证据，不以脚本结果冒充主观体验。

### 2026-08-20：真机验收批次调整

- 用户决定暂不进行真机实验，重启、离线、后台限制、字体/深色模式、桌面尺寸、小组件和完整核心闭环统一留到后续真机验收批次。
- 当前批次继续以浏览器回归、Android 编译、Lint、APK 和远程 CI 作为工程证据，但不把它们写成真机通过。
- 远程证据：提交 `dca46d8` 的 GitHub Actions 运行 `32329605246` 全部通过；verify、服务容器构建/冒烟/发布、Android 同步/APK 构建与制品上传均成功。

### 2026-08-20：个人 Android 真实 AI 直连闭环

- 产品逻辑更新：个人自用版本不再要求部署 HTTPS 中转服务；Android 原生层持有本机 `.env` 注入的 MiniMax 密钥并直连中国区接口，WebView 只传严格合约请求，密钥不进入 JavaScript。
- 已完成：抽取服务端与原生端共用的 AI 引擎；每日整理、任务反馈、周复盘、系统候选与目标拆解使用同一提示词、解析、证据和安全边界。
- 已完成：增加 Android `QiguangAi` 原生插件、HTTPS/响应大小/超时限制、原生配置检测和本地功能降级；默认切换 MiniMax-M3 并关闭 thinking。
- 已完成：模型入口校正可证明的 Unicode 证据坐标，移除可选候选的未知字段，丢弃不完整任务候选，合并相同事实，处理证据不足周复盘，并对格式错误和可重试 5xx 做有界修正。
- 安全边界：`.env` 继续由 Git 忽略；发布检查确认真实密钥不在 Web 产物中；即时伤害表达先在本地进入求助流程，普通游戏化建议暂停。
- 验证：`npm run check` 全部通过；真实 `npm run eval:ai:direct` 16 类 17/17；浏览器端到端 23/23；`npm run check:release` 12/12（原生项按条件跳过）；`npm run check:android-release` 13/13 并以 JDK 21 完成 154 项 Android Debug 构建。
- 已修复：远程首轮容器冒烟发现运行时镜像未复制新共享 AI 引擎；Dockerfile 已补入该文件并增加发布回归，避免构建成功但容器启动即退出。
- 延期不冒充完成：尚未连接真机，因此安装、重启、后台限制、桌面小组件、原生文件权限与完整设备闭环继续保留为后续统一真机验收。

### 2026-08-20：首版完成态审计

- 已核对：首次使用、成功日记、目标拆解与确认、每日单一方向、任务反馈与经验、逾期处理、反馈驱动重规划、周/月轨迹、伙伴记忆、备份恢复、离线降级、Android 小组件和 MiniMax 原生直连均有正式实现及自动化证据。
- 已修正：删除“硬截止和 BONUS 不积压仍待实现”的过期描述；两项均已实现并进入数据库、十四天模拟或浏览器回归。
- 工程证据：本地与远程当前提交一致；GitHub Actions 的 verify、Android 与容器任务全绿，真实 MiniMax 中国区直连评估 17/17，浏览器端到端 23/23，Android Debug APK 已生成。
- 完成边界：无真机环境可完成的产品与工程功能已闭环；安装、重启、后台限制、桌面小组件、原生文件权限及不同桌面尺寸只能在用户安排的后续真机批次验收，不伪装为已通过。

### 2026-08-20：Android 真机验收入口收口

- 已清理：删除 Capacitor 自带的 `2 + 2` 与错误包名样例测试，替换为栖光正式包名、MiniMax HTTPS 地址和模型配置的原生单元测试，以及正式包名的真机仪器测试。
- 已完成：新增 `npm run test:android-device`；命令固定 D 盘缓存与 Android SDK/JDK 21，只接受一台真实设备，拒绝模拟器，以 `adb install -r` 保留本地数据，启动应用并执行 `connectedDebugAndroidTest`。
- 安全边界：覆盖安装签名不一致时立即停止并要求先导出备份；脚本不执行 `adb uninstall`，也不会在没有真机时继续构建或安装。个人调试签名固定保存在被 Git 忽略的 `.android-user/debug.keystore`，首次安装后必须与数据备份一起保管。
- 验证：无设备门禁自测通过；原生单元测试、AndroidTest Lint、完整 Lint 与 APK 构建通过（233 项 Gradle 任务，0 errors、18 个既有警告）；本机稳定调试签名生成并通过 APK 证书校验；`npm run check` 与 `npm run check:release` 全部通过；提交 `f2502f7` 的 GitHub Actions 运行 `32344055006` 中 verify、Android 和容器三项全绿，APK 制品已上传。
- 仍待用户安排：连接真机后运行该命令，并人工观察小/中/大组件、断网重启、后台回收、200% 字体、深色模式、减少动画、MiniMax 联网恢复和系统分享备份。

### 2026-08-20：GitHub 操作规则

- 已更新项目规则：GitHub 远端交互默认使用 `gh`；纯本地状态、差异、提交和分支操作继续使用 `git`，仅在 `gh` 无对应能力或失败时回退到 `git` 远端命令。

### 2026-08-20：产品逻辑与 TODO 分离

- 已完成：新增 `PRODUCT.md`，集中保存产品定位、设计理念、核心闭环、规则、验收标准、明确边界和实施思路。
- 已完成：`TODO.md` 只保留完成项、待办、验证结果和工作记录，不再混放产品设计正文。
- 已完成：`AGENTS.md` 改为双文档工作基线，并同步根目录文档结构、开始工作和完成定义。
- 验证：`PRODUCT.md` 包含 15 个连续产品设计章节且无进度复选框；`TODO.md` 只有任务、完成基线和工作记录 3 个顶层章节，并保留 2 项真实待办；提交 `d6cc1df` 的 GitHub Actions 运行 `32347036811` 全绿。

### 2026-08-27：任务页与全局文案收敛

- 任务页改为“今天/计划”两层：今天只承载执行，计划管理长期任务与习惯；过期任务也使用同一扁平列表。
- 今日任务使用底部输入栏和圆形 `＋` 快速添加；聚焦时其他内容退后，空输入点 `＋` 仍进入完整设置，保留日期、计数和任务类型能力。
- 任务来源使用蓝、橙、绿、紫四色区分；完成保持单击，部分完成、跳过、编辑和撤销进入移动端底部详情面板。
- 全页面删除非必要说明句；仅保留首次交互指引、安全求助、隐私发送边界、删除/导入风险、错误原因和处理中状态。
- 修正任务与设置焦点样式、页面标题左对齐及人物到达后的朝向；保留原任务结算、里程碑、习惯、XP、撤销和容量规则。
- 验证：`npm run check` 全部通过；浏览器端到端 53/53；320×800 任务页的今天、输入聚焦和计划三种状态已截图复核。

### 2026-08-28：设置栏目触控反馈修正

- 已完成：设置页“人物”等可展开栏目不再显示 Android WebView 的蓝色触摸高亮；键盘操作仍保留轻量下划线焦点提示。
- 验证：前端构建、焦点样式定向浏览器回归和 Android Debug 构建通过；使用原签名无损覆盖安装到 `emulator-5554`，实测“人物”展开及按压过程无蓝色反馈。

### 2026-08-28：今日任务底部添加栏

- 已完成：今日任务快速添加栏固定在底部导航上方；`＋` 的可见圆圈缩至 32px，同时保留 44px 触控区域，输入和完整设置逻辑不变。
- 验证：前端构建和快速添加定向浏览器回归通过；Android Debug 构建后无损覆盖安装到 `emulator-5554`，已截图确认空任务页面的底部位置和按钮尺寸。

### 2026-08-28：场景组件点击外框修正

- 已完成：点击家具或人物热点不再出现浏览器蓝色粗框；场景内原有互动光圈继续承担键盘焦点与行动反馈。
- 验证：首次使用与房间交互定向浏览器回归、Android Debug 构建通过，并已无损覆盖安装到 `emulator-5554`。

### 2026-08-28：任务执行逻辑与看板重构

- 已完成：任务页以“今天 / 计划”分离执行和管理；目标明确区分“建立并开始 / 只保存计划”，新习惯默认在计划日生成打卡，未来任务进入计划，过期普通任务进入“待决定”，错过的习惯只保留原日期事实。
- 已完成：普通任务使用 20px 状态圆和整行完成，完成后显示勾选与标题划线；计数任务整行增加一次并用底色按比例填充，达标只结算一次经验；主行移除来源、最低动作、预计时间等重复信息。
- 已完成：快速添加不再伪造“临时任务”、五分钟或最低动作；详情入口保留完整修改、日期、计数、反馈和撤销能力，任务详情与修改弹层在手机及软键盘可见状态下居中展示。
- 已完成：Android 今日任务小组件同步支持计数任务逐次打卡与进度显示。
- 验证：`npm run check` 通过；`npm run test:e2e` 的 54 条浏览器流程全部通过，含正式页面 30 天闭环；Android Debug 与测试 APK 构建成功，API 34 模拟器 2 条仪器测试通过，签名核验后无损覆盖安装并实看任务行、计数填充、完成状态和居中弹层。

### 2026-08-28：具体条目操作与修改文案收敛

- 已完成：移除任务页全局“管理”按钮和批量选择状态；待办任务、待安排任务、目标与习惯分别在自身详情或“更多”中提供编辑、删除，删除继续使用软删除或结束状态保留历史。
- 已完成：“调整这项行动”改为“修改任务”；快捷操作改为“做个更小版本、换一个任务、改到明天”，字段使用任务名称、为什么要做、最容易开始的一步等自然语言。
- 已完成：推理依据默认折叠为“为什么这样建议”，删除“事实、已确认记忆、不会凭空假设”等面向系统实现的文案；目标、里程碑、截止与历史反馈仍可按需查看。
- 验证：`npm run check` 全部通过；浏览器 53 条主流程和正式页面 30 天闭环通过；修复周复盘保存后立即跳转的测试竞态。Android Debug/测试 APK 构建成功，API 34 模拟器 2 条仪器测试通过，证书核验后已无损覆盖安装并启动。

### 2026-08-28：书桌互动结束朝向修正

- 已修复：左右站立帧变量原先反向绑定，导致书桌最后一帧视觉朝右；现在书桌使用真实左向站立帧，工作台同时恢复为真实右向站立帧，步行与前后朝向不变。
- 验证：书桌走近与互动定向浏览器回归、运行图集映射检查和 `npm run check` 通过；Android Web 资源同步、Debug APK 构建、模拟器无损覆盖安装及启动成功。

### 2026-08-28：UI 设计理念固化

- 已完成：在 `PRODUCT.md` 固化“温暖像素生活感 + 克制的新粗野主义”的视觉定位，以及颜色、字体、信息层级、触控、动效、安全区与可访问性规则。
- 已完成：在 `AGENTS.md` 增加 UI 开发强制基线；后续改动必须先核对产品理念，再按 `DESIGN-CHECKLIST.md` 验收本次命中项。
- 已清理：设计清单删除素描头像、记录页隐藏底栏和旧版本证据等过时规则，改为与当前五项主导航、卡通头像、居中弹层和 Android WebView 交互一致的检查项。
- 已发现：恢复建议卡仍向用户显示英文 `RECOVERY`，已作为后续界面术语清理待办保留，本次未越过文档范围修改功能代码。
- 验证：文档职责与交叉引用已核对；本次没有修改运行代码，因此不执行应用构建或设备测试。

### 2026-08-28：男生横向行走朝向修正

- 已修复：包包与鱼鱼的运行图集横向帧排列不同；现在包包单独绑定真实左向、右向站立帧及六帧步行序列，横向位移与面朝方向一致，鱼鱼保持原有正确映射。
- 已加回归：浏览器会以包包完成首次使用并核对左右站立、步行首尾帧；家具互动锚点改为按当前角色的方向变量验收，不再把男女素材坐标误当成同一套固定像素值。
- 验证：`npm run check` 全部通过；男生方向、家具路径和剩余浏览器流程定向回归通过。Android Web 资源同步及 JDK 17 Debug APK 构建通过。
- 模拟器边界：当前 `emulator-5556` 已安装包的证书与新 APK 不同，系统拒绝覆盖安装；已保留应用与数据，没有卸载或清除。需取得原签名 APK，或由用户确认可丢弃该模拟器数据后再安装。

### 2026-08-28：第二轮设置与界面术语收敛

- 已完成：恢复建议卡将用户可见的 `RECOVERY` 改为“状态照顾”，并把“最小动作”改成更自然的“先做”；内部数据语义保持不变。
- 已完成：设置页按日常设置、功能与设备、数据与隐私、高级设置分组；人物、状态自评、显示与语气、AI、小组件、安装存储、本地数据和规则仍按需展开，危险数据操作与日常选项分离。
- 已加回归：浏览器检查设置分组与栏目顺序、200% 字体无横向溢出、所有栏目触控高度不低于 44px，并确认恢复建议不再暴露英文内部术语。
- 验证：`npm run check`、54 条浏览器 E2E 和发布门禁均零失败；发布检查 17/18 通过、1 条按非原生环境预期跳过。320px、390px 与 320px/200% 字体截图复核无横向溢出，栏目高度为 50px。
- Android：Debug APK 构建成功；为保护 `emulator-5556` 的异签名旧数据，另建 D 盘临时 API 34 模拟器，2 条仪器测试及断网冷启动通过（0.6.18 / 618），验收后已删除临时 AVD。

### 2026-08-31：第三轮移动端组件系统收敛

- 已完成：页面、区块、正文和辅助文字统一为四级字号；按钮统一为 48px 主/次操作和无边框文字操作，输入框、普通状态控件与卡片减少粗边框和阴影，数字使用等宽排版。
- 已完成：所有主页面使用统一底栏安全区；长弹窗跟随 `visualViewport` 高度与位置并保持内容滚动、操作栏可见；任务快捷操作改为 2×2，标题允许两行，里程碑操作不再挤压标题。
- 已完成：首次角色选择明确区分未选、按下和已选，初始焦点不再伪装成选择；生活分身面板增加关闭按钮；设置只保留一个一级栏目展开，导入导出与永久删除分区；周建议收敛为一个主按钮、一个次按钮和两个文字操作。
- 验证：`npm run check`、56 条浏览器回归、发布门禁全部通过；320×480、390×844 与 200% 字体视觉检查无横向溢出。JDK 17 构建 Android Debug/Test APK，独立 API 34 模拟器仪器测试 2/2、断网冷启动与进程检查通过（0.6.18 / 618），未连接或触碰真机。
