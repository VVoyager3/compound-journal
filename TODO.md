# 栖光：当前进度与 TODO

> 当前基线：0.7.0（2026-09-03）。旧版 MAIN / BONUS / 支线、生活领域、提升方向、成长分支和主次目标逻辑已退出当前产品；历史数据只做只读兼容。更早的过程记录保留在 Git 历史中，不再堆放在当前待办里。

## 本轮已完成：五维成长闭环

### 2026-09-07：热力图视觉重设计（已完成）

- [x] 任务与单习惯分析合并为同一套暖灰底格、深绿浓度和正方形几何；删除旧黄绿混用，保持无颜色图例。
- [x] 任务分析只用浓度表达每日完成数量；单习惯以克制的小勾、短横和底格区分完成、漏打、未计划；今天使用不覆盖状态的独立外框。
- [x] 严格尺寸审计、完整 `npm test` 和 47/47 浏览器业务回归通过；55场景重拍文字审计为0，任务与习惯分析截图见 `design/screenshots/20260907-heatmap-redesign/`，第二轮独立视觉复审PASS。

### 2026-09-07：组件合并最终审查（已完成）

- [x] 公共组件与尺寸账本最终复核：`node scripts/audit-ui-code.mjs --strict` 通过，1276/1276 条目标尺寸均已归类，0 待处理、0 漂移；公共样式未恢复平行字体、按钮、列表或页面壳。
- [x] 完整验证通过：`npm test`、39/39 项组件定向测试及 47/47 项浏览器业务回归全部通过；此前记录的 AI 等待超时已由当前全量结果覆盖。
- [x] 重新采集 55 个实际页面与状态至 `design/screenshots/20260907-component-review/`；775 个文字节点零规格偏差、210 个列表行均不低于 45px、243 个日历/热力方格保持正方形、页面预览无横向溢出。
- [x] 独立只读视觉复审通过：逐页核对页面壳、字体、列表、方格及重点新旧页面，未发现阻断组件合并的问题；此前 6 个上下文弹层现已全部纳入公共壳和几何断言。
- [ ] Android WebView 的安全区、键盘及设备字体仍需具备模拟器或设备后单独验收；本机当前无连接设备且无可用 AVD，不以浏览器结果代替设备结论。

### 2026-09-06：直接尺寸逐项迁移（已完成）

- [x] 固定上一轮1276项清单为迁移基线，建立 `design/ui-dimension-decisions.json`，按原声明身份登记删除依据、公共令牌或必要例外；`node scripts/audit-ui-code.mjs --strict` 仅在全部归类且无未登记改动时通过。
- [x] 删除116条始终被公共层相同选择器/属性覆盖的声明，其中92条属于固定基线中的直接尺寸；保留!important、条件规则和未完全覆盖的选择器分支。
- [x] 12条无障碍隐藏文字/文件输入的宽高迁到公共隐藏盒；57条已逐项核对的交互入口改用 `--ui-touch-size`，保持44px默认命中区，不把同为44px的图片、统计和标题内距混入。
- [x] 1276条固定基线已逐项收口：895条迁入公共令牌、92条删除被公共组件稳定覆盖的旧声明、9条删除任意最小尺寸并改为内容自适应、280条保留为明确分类的响应式边界/日历/图表/人物素材/语义图标几何；严格审计为1276/1276、0待处理、0漂移。公共组件层直接尺寸为0，同类列表最低45px、页面边距20px、标题栏44px、区块间隔13px均由同一令牌控制。
- [x] 统一剩余间距、圆角、标题栏、交互行、状态行、勾选标记与操作栏尺寸；简写迁移保留原边数和零内距。新截图55/55生成成功、文本候选0、预览溢出0；所有已测页面为16/14.5/12.5/11.5px四级文字，列表内容仅在真实多行内容时自然增高。
- [x] 迁移门禁覆盖嵌套/重复声明、非法索引、单位大小写、容器单位、属性值语法与未知CSS属性；14项迁移/清理测试通过，未登记漂移为0。
- [x] 同内容CSS像素对比55/55精确一致：独立排查复现未修改CSS首张与后两张不同，确认采集绘制尚未稳定；由独立修复者改为最多5次/总预算10秒、连续两张字节相同后才比较，稳定失败仍报错，未增加像素容差。就绪错误补充脱敏诊断；工具自测和语法检查通过，结果见 `design/screenshots/20260906-dimension-migration/css-parity.json`。`scripts/compare-ui-css.mjs` 默认以暂存区CSS为前态；本批尚未暂存，可重复核对。此项只证明400×866相同夹具内容下本次迁移无视觉回归，不证明剩余旧设计正确。
- [x] 最终独立复审PASS：修复审计发现的非法函数负号、非法属性值、漏检容器单位及被浏览器忽略的`gap-inline`；属性名和值均由CSS语法门禁检查。全量测试、47/47浏览器回归、安全扫描、CodeGraph与差异检查通过；设备验收仍单独保留。
- [ ] 真实Android安全区/键盘/设备字体仍需设备验证。整个前端修复目标保持进行中。
- [x] 字体族补漏完成：业务CSS只允许`font-family: inherit`，日期、分数和引用不再建立第二字体系统；静态门禁同时禁止业务层重建字号、字重、行高、字距与字体族。1276项基线只覆盖宽高、间隔、内外距、圆角等目标属性，grid轨道、定位偏移、边框和动态样式仍由截图、响应式与浏览器测试分别验收，不能由本清单单独推导为全部设备正确。

### 2026-09-06：公共体系收敛与真实页面调节

- [x] 继续删除6个没有运行时入口的旧流程及其CSS（收束今天、同日往年、首记提示、旧复盘依据、旧问卷模式、旧分析下拉），连带移除无效参数、查询和局部计算；`tsc --noUnusedLocals --noUnusedParameters`归零并纳入每次构建。页面与弹层标题栏统一由`titleBar`生成，三列选择统一由`choiceGroup`生成，引导与设置的人物选择只保留`avatarChoiceGroup`这一套DOM/样式/轻量人物资源，日历查找复用标题栏操作；已完成任务直接进入`taskRow`，删除未调用的`quest-row`、`overdue-quest-row`、`habit-momentum-row`、`ledger-row`、`milestone-row`、`habit-log-row`和`analysis-result-row`旧列表皮肤。静态契约阻止旧骨架回流。
- [x] 继续删除无运行时入口的旧记录摘要、日期摘要/证据卡、日期行图标/箭头及分析页固定操作栏；日期记录改为单内容列、行动结果改为“内容 + 结果”双列，不再为已经删除的图标和菜单预留42/34/18px轨道。CSS声明降至4805，严格尺寸审计0漂移；全量静态测试、47项浏览器场景、55页截图及文字审计通过。
- [x] 删除整套无运行时入口的旧记录编辑器皮肤：分类提示、模板选择、编号工具、旧附件按钮、固定提交栏及日期记录内嵌菜单不再保留平行CSS；当前“生活日记 + 每日复盘”只使用现有编辑面板、公共按钮和记录详情入口。CSS声明进一步降至4665，生产CSS降至197.15kB；组件契约、严格尺寸审计、生产构建和全量静态测试通过。重新采集后，记录编写、每日复盘、日期记录、记录详情、日期行动、日期复盘、复盘编辑和版本历史8个相关状态逐像素一致。
- [x] 删除无运行时入口的旧日记条目、条目操作栏、Today主行动卡以及旧周复盘英雄卡/重点卡/实验卡皮肤；保留当前记录详情、公共列表和周复盘调整流程。CSS声明降至4590，生产CSS降至194.16kB；所有被删除的类加入静态防回归名单，严格尺寸审计0漂移，组件契约和生产构建通过。
- [x] 所有弹层由dialogShell建立同一标题栏；返回只补入公共网格，任务结果关闭按钮移入标题栏。55场景全部具备可测公共壳，补齐原6个遗漏场景，已识别文字规格零偏差。
- [x] 栏目标题、空状态、操作组和分段选择器收口为 `src/ui-list.ts` 的唯一 DOM 构造器；删除 `src/app.ts` 中同类公共结构的手写拼装，保留业务事件、ARIA 和专用布局类。静态契约阻止第二套构造方式回流；完整测试及47/47浏览器回归通过。
- [x] 将40余处共用的表单标签构造器迁入公共组件层，并收口3个表单纵向容器及11个栏目文字操作按钮；复杂日期、结果选择和危险确认继续保留专用结构，不扩张成万能组件。新增静态门禁，TypeScript与12项组件契约通过。
- [x] 将18个流程复用的主按钮构造器迁入公共组件层，21处保存/错误反馈统一为 `statusMessage`；保留各流程的禁用、ARIA、异步错误和确认逻辑。静态门禁禁止业务页重新手写同类结构。
- [x] 新增单一 `actionButton` 构造器承载 primary、secondary、quiet、danger 四种既有语义；全部标准按钮已迁移，表单提交、异步禁用、确认与焦点行为保持原逻辑。静态门禁禁止业务页重新手写标准按钮结构。
- [x] 月度成长、事件历史、更多复盘、当天整理、任务历史、周复盘依据和 AI 高级设置统一使用 `optionalDetails`；业务页不再手写第二套可折叠次要内容骨架。
- [x] 今日、任务计划、目标、习惯和状态关联区域统一使用可配置的 `listSection`；任务、目标和习惯的更多操作统一使用 `overflowMenu`，标题骨架、列表间隔和菜单可访问名称由公共组件负责。
- [x] 全屏子页面的页面壳、业务类名和返回键统一由 `dialogShell` 参数生成，删除各页面重复的 `full-screen-editor` 与返回键拼装；导入备份和数据库恢复统一使用原生语义的 `fileButton`。
- [x] UI 调参页改为直接复用生产环境的列表、按钮、操作组、字段和分段选择器构造器；修复 `listSection` 新签名遗漏的预览调用者，并以真实浏览器回归锁定组件预览不再漂移。
- [x] 清理同选择器/同条件被覆盖的旧CSS声明，重复不同值580→0；移除业务CSS全部font/font-size/font-weight/line-height/letter-spacing，字体仅由公共层管理。新增静态门禁及清理器回归。
- [x] 记录的图片/发送控件接入公共button，修复字号收敛后图片入口宽度不足44px的问题。
- [x] 删除调节页8个手写原型和专用CSS，替换为隔离测试数据的真实DOM快照；保持31项参数及旧JSON兼容。左右同模板，候选不接触App数据库、不发AI。
- [x] 独立复核发现并修复快照CSP阻止动态样式、窄窗口沿用截图高度遮住底部按钮两项问题；保留生产安全策略，新增月份网格位置、控制台错误及窗口缩放回归。
- [x] npm test、47/47浏览器业务回归、调节页11视图×320/400/480px与200%标题检查通过。截图及HTML：design/screenshots/20260906-complete-unification/。
- [x] 原始1276条直接尺寸的逐项迁移已在本页顶部完成；严格账本阻止未登记尺寸、删除项复活和令牌漂移。Android设备安全区/键盘/字体仍需单独验证。

### 2026-09-06：本轮提交前验证

- [x] 修复习惯菜单在窄屏越界、首页待打卡数量包含已完成项目；新增菜单边界及打卡数量1→0的浏览器断言。
- [x] `npm test`完整通过（含Web构建与组件测试）；`node --experimental-strip-types test/browser.e2e.mjs` 47/47通过，30天业务循环1/1通过。测试固定计划日、等待页面就绪，并隔离测试服务限流；不降低生产限流、不跳过AI确认。
- [x] 刷新55场景截图，已识别文字规格零偏差；代码清单、CodeGraph及暂存差异检查完成。独立复审通过，安全扫描未发现新增凭据或危险执行。
- [x] README与本文件同步；本轮仅完成下列公共规则及验证修复，不代表其余旧样式迁移已经完成。未执行Android构建或安装。

### 2026-09-06：首批公共规则修复（实际实现）

- [x] 新增浏览器回归先复现18.85px标题行高失败，再修复为共享1.35行高；正文兜底选择器降低优先级，辅助角色统一11.5px/400/1.35，周日期不再私自使用正文大小。
- [x] 清理旧CSS中34处涉及已迁移辅助文字的规则；公共CSS删除尾部信息/统计等重复字体声明。新增静态检查，阻止这些角色再次回到旧样式定义。
- [x] 普通、双按钮和弹层操作使用独立8px间隔，标题间距不再影响按钮。调节页新增“按钮之间”，共31项参数，兼容原30项JSON；不覆盖浏览器已有候选。
- [x] 人物5条专用尺寸提取为`--ui-portrait-*`，视觉尺寸不变；公共组件尺寸直接px检查通过，1em随字体图标保留。
- [x] 55场景重拍、777个已识别文字节点零偏差，字号/字重/行高升级为强制断言。保存状态/习惯统计按辅助语义覆盖；49场景标题栏通过，6个上下文弹层尚无独立几何契约。
- [x] Web构建、10项组件/调节页测试（含先失败后修复的回归、多个视口与200%文字）、审计脚本自检和`git diff --check`通过；抽看02状态详情与32AI设置。未做全业务/Android回归，未提交推送。
- [x] 后续公共覆盖、8个手写原型替换和6个上下文弹层验收已在本页顶部“公共体系收敛与真实页面调节”完成；剩余旧布局尺寸迁移以顶部未完成项为准，不再沿用本阶段2076/580的旧统计。

### 2026-09-06：继续代码审计与分类归属

- [x] 增加可复跑的只读CSS代码清单`node scripts/audit-ui-code.mjs --output design/screenshots/20260906-template-audit/code-audit.json`，附`--self-test`；按CSS语法解析位置、尺寸和同条件重复声明，不把候选数量当独立缺陷。
- [x] 旧CSS目标属性直接尺寸2101条、同选择器/同条件的重复不同值588次；公共CSS直接尺寸7条（人物5条、1em图标2条）。`app.ts`静态定位13处main构建、39处弹层调用、8处数据/视口动态样式。
- [x] 分类整理至`docs/UI_COMPONENTS.md`：3种页面壳及业务入口映射、7组基础组件与跨页面导航/反馈/专用内容、4级普通文字与控件/数字变体；明确目标分类不是完成数量。修正文档20px边距遗漏360px以下16px断点，标记旧逐页文档为历史。
- [x] 修正审计脚本从背景页取标题栏的漏洞；统一只测活动页面/弹层。55场景中6个上下文弹层没有公共标题栏，明确未验证，不能沿用背景测量判定通过。
- [x] 审计脚本自检、6项组件契约测试、55场景活动范围重测、语法检查、`git diff --check`和CodeGraph同步通过。49个场景实际测得共享标题栏；6个弹层未覆盖。仍有185次文字规格候选偏差，不能把门禁通过当作问题已解决。本轮未改正式组件样式、未跑全业务/Android回归。
- [x] `.ui-actions`误借标题间距的问题已在上方首批修复中分离；其余旧样式/原型迁移继续保留待办，不标记全部正确。
- [ ] 日期快照、徽章详情、修改历史、任务结果、删除确认、首次引导需要上下文弹层的独立几何契约与自动验证。

### 2026-09-06：扩大审计范围与人工调节模板整理

- [x] fetch确认上游无新增提交；保留所有已有修改。复查55个页面/状态（补入首次使用的DOM测量）、670个已识别文字节点；标题栏/公共列表几何通过，24次公共分组标题间距均为13px。
- [x] 新增`design/screenshots/20260906-template-audit/text-audit.html`：190次文字规格偏差归为15组选择器/规格组合，含重复状态与语义例外，不是190个独立缺陷。前轮只验证标题字号/字重，不足以声称字体完全统一。
- [x] 在已有调节页归纳全部30项参数、模板清单与真实/原型边界；3个真实组件视图、8个手写原型。补上真实行内距和计数到+1映射、同模板同视口基准、正文组件标题样例；不覆盖用户本地候选。
- [x] 删除PRODUCT旧尺寸表，以批准参数和组件规范为单一规格来源；正式App视觉本轮未变更。
- [x] Web构建、2项真实列表/调节页浏览器测试通过（覆盖参数响应、保存、导入导出及320/400/480px预览）；截图核对模板目录，`git diff --check`与CodeGraph同步通过。未运行全部业务回归或Android测试。
- [x] 组件标题行高、辅助文字覆盖、列表尾部行高和日期专有覆盖，已在上方首批公共规则修复中解决。
- [ ] 明确习惯统计/必要提示的文字语义；补查未标记为公共行的旧结构，并迁移8个手写原型或退出其正式验收用途。
- [ ] 继续覆盖菜单、错误/空状态、长文、大字体和Android设备；不以55张默认状态截图宣称全部统一。

### 2026-09-06：页面、组件与字体语义统一

- [x] 将当前UI规范压缩为唯一有效版本：3类页面、7组组件、4级字体；删除`docs/UI_COMPONENTS.md`中已失效且互相冲突的历史尺寸说明，批准参数仍以`design/approved-ui-20260905.json`为准。
- [x] 记录详情“正文”改用公共组件标题；页面/弹层中的普通`h2/h3`统一为14.5px/800，公共列表行内标题统一为12.5px/400，不再被页面标题规则误伤。状态详情的无记录状态和分数变化入口改用统一列表行，删除旧下划线。
- [x] 截图脚本新增字体语义门禁：页面标题16px/800、组件标题14.5px/800、列表标题12.5px/400；任一实际页面出现额外标题规格即失败。54个页面/状态全部通过，截图见`design/screenshots/20260906-unified-system/index.html`。
- [x] Web构建、8项公共组件/调节页测试、2项状态/记录定向业务回归及`git diff --check`通过；抽查状态详情、记录详情、目标详情。未进行Android安装、提交或推送。

### 2026-09-05：采用用户导出的公共UI参数

- [x] 远端fetch成功，当前分支无新增上游提交；保留已有工作。通过CodeGraph检查公共列表调用关系。
- [x] 用户确认的30项参数保存于`design/approved-ui-20260905.json`，映射到正式公共样式及调整页默认值；400px作为参考视口而非锁定App宽度。小按钮保留44px触控，长内容允许增高。
- [x] 页面标题16px/800、正文12.5px、行最低45px、按钮36px、分段28px、标题间隔4px及分组13px已应用；字段、复盘和热力图同步。尺寸留在公共令牌，不新增运行时样式层。
- [x] Web构建与7项公共组件/调节页测试通过；54页截图与几何断言通过，输出`design/screenshots/20260905-approved-parameters/index.html`，抽查编辑任务、设置及分析。未进行Android安装、全业务回归或提交推送。

### 2026-09-05：正式页面壳统一

- [x] 将一级页面、二级页面及全屏编辑页统一为同一页面壳：左右20px、标题栏44px、标题到正文4px、同级内容组16px；顶部/底部继续尊重系统安全区和底部导航。运行时样式由公共组件层统一覆盖旧页面特例。
- [x] 记录、目标、习惯详情的右上角菜单移入标题栏第三列，移除其对正文流的绝对定位占位；返回、标题和操作使用同一网格对齐，业务回调不变。
- [x] 截图脚本新增页面壳真实DOM断言；54个页面/状态全部通过20px边距、44px标题栏、16px/800标题和4px正文间距检查，截图见`design/screenshots/20260905-approved-pages/index.html`。Web构建与7项组件测试通过。
- [ ] 完整浏览器回归45/47：页面、详情、编辑、删除及200%字体等均通过；任务反馈AI候选与周复盘AI候选两个既有等待项仍分别在30秒超时，本轮未修改AI流程，不能将全套标记为通过。未做Android安装、提交或推送。

### 2026-09-05：相似预览的标题与分组布局统一

- [x] 新增预览共用布局，11种视图的页面/弹窗标题、正文和分组间距只保留一个来源。移除标题margin叠加、07标题混入字段grid、分析页专用返回字符与分组硬编码间距；保留独立字段和重复日期调节项。
- [x] 修正27/32和01/02/04误显示整个组件目录：设置与任务预览使用实际公共列表渲染器。新增实际标题间距读数，沿用已有参数存储和导出格式，不清空用户调节。
- [x] 7项组件/真实列表/调节页回归及Web构建通过；11视图在320/400/480px检查实际标题12/24px、分组8/16px及200%标题字号，弹窗动作保持同排。截图：`design/unified-layout-audit.html`。未执行Android、整套业务回归或提交推送。
- [ ] 当前统一的是调节预览布局；用户确认并导出尺寸后再覆盖App正式布局，不宣称全App所有专用组件已经统一。

### 2026-09-05：编辑任务表单间距调节修复

- [x] 复现旧样例错误：表单参数设12/14px，实际仍为列表分组的16/8px。将样例字段和表单接回`--ui-field-gap`、`--ui-form-gap`，不新建第二套参数。
- [x] 调节项归入“表单间距”：标签到输入框4–24px、输入项之间4–32px；选择编辑任务/目标/日周复盘时自动展开。原参数保留，不改变App正式尺寸。
- [x] 浏览器回归验证实际间距随参数变为12/14px，刷新恢复及JSON导入导出保留；现有窄屏与双按钮同排回归通过。仅修改调节页，未做Android或全量业务回归。

### 2026-09-05：任务编辑底部操作精简

- [x] 按“删除任务”理解用户输入的“删除人物”。应用编辑页与调整页样例底部只保留“删除任务 / 保存修改”，共用等宽双按钮操作区，一行排列；取消由顶部返回/关闭承担。
- [x] 样例移除缩小、替换、改到明天的按钮；实际删除复用现有二次确认与保留历史的删除函数，不改任务结算。构建、2项编辑/快速添加浏览器回归与调整页测试通过；样例320/400/480宽度和200%控件字号保持同一行，无按钮内容溢出。
- [x] 截图复核：`design/task-edit-actions-preview.png`；已验证删除取消保留编辑页、保存修改与确认删除。未进行Android或全量业务回归。

### 2026-09-05：按钮缩小候选

- [x] 调整页左侧按钮可见高度试降至36px，右侧保持44px；新增独立可见高度参数，旧参数保留，恢复基准仍为44px。App默认高度未改变。
- [x] 复用公共按钮样式，通过透明扩展保留至少44px点击范围，预留相应间隔；真实DOM验证四种按钮均36px、右侧44px，上下外延可点击，组件与调整页7项定向测试通过。
- [ ] 用户确认后再将候选高度用于全应用；本轮未做Android、键盘或整套业务回归。

### 2026-09-05：公共组件扩展收敛（人工确认前候选）

- [x] 按用户要求从列表扩展到标题/分组标题、按钮、分段、表单字段、内容框、提示区和操作组；复用现有TypeScript/DOM/CSS，不引入组件框架，不改DB、AI授权或任务结算。
- [x] 目标管理取消大卡固定高度，往年记录、成长明细、分析完成记录和习惯完成方式接入基础行；长正文允许自然增高。记忆、复盘依据、目标阶段、建议和存储状态接入`.ui-panel`。
- [x] 清理174条旧规则的重复几何与字体属性，另删除目标菜单5条旧规则，修复旧伪元素造成的双“更多”图标；保留未迁移业务结构，不靠全局缩放。
- [x] 分段可见36px/整栏42px不变，补足44px实际触控；普通按钮统一44px，字段间隔统一8/20px、多行输入最低96px，操作区统一8px并可换行。
- [x] 更新现有`design/ui-tuner.html`为统一组件入口：真实列表渲染器与公共样式预览，增加按钮/切换/字段/内容框；支持候选参数保存、导入导出，保留用户旧参数，不自动修改App数据。
- [x] `npm run test`（含构建）通过；7项组件合约、真实DOM、调整页测试通过，覆盖320/360/390/400/430及200%字体。修正存储测试未命中实际元素的问题，并更新成长明细旧选择器，仍检查真实数值。
- [x] 更新55页实拍与采集几何断言：`design/screenshots/20260905-shared-components/index.html`；抽查目标列表、习惯表单、AI设置，确认目标菜单仅保留一个图标。CodeGraph同步及`git diff --check`通过。
- [ ] 最新整套浏览器回归46/47：成长触控和账本追溯通过；AI任务反馈等待候选超时仍可复现（上一遍该项通过），未解决，不能将整套判为通过。保留诊断项，不用扩大超时掩盖。
- [ ] 用户确认样板尺寸并冻结参数；日记正文、日历/图表、人物、部分特殊弹层仍保留专用布局，未宣称所有页面完全统一。未做Android验证、提交、推送或关机。

### 2026-09-05：普通列表基础组件修复（人工确认前候选）

- [x] 开始任务fetch成功，当前分支与上游0/0；保留全部既有未提交工作。通过CodeGraph检查调用链，修改后执行同步。
- [x] 新增 `src/ui-list.ts`，公共行/分组/任务渲染器只处理展示与回调，不改变DB、AI授权、任务结算和路由逻辑。今日任务、习惯、已完成、相关任务、设置和问卷等普通列表使用统一基础几何；删除对应旧样式选择器，避免遗留几何覆盖。
- [x] 基准为最低50px，正文13px常规、分组13.5px加粗，上下8/左右12px，标题间距8px、组间16px。只读行不添加箭头或打卡，长内容可增高；修正窄屏热力图末月标签的横向溢出。
- [x] 调整页列表预览直接复用App渲染器和CSS；候选与当前基准并排，隐藏对此样例不生效的旧原型控制项；保留参数保存/导入/导出。无需重新逐页调字体。
- [x] 刷新55页实际截图到 `design/screenshots/20260905-list-foundation/`，非参考稿。组件合约、新增真实列表测试覆盖320/360/390/400/430及200%字体；调整页覆盖参数持久化和交互。构建通过。
- [x] 修正旧发布测试仍要求显示已被用户删除的“成功小记”的陈旧断言，改为禁止恢复该标签；不改变记录业务。
- [x] `npm run test`（含构建）通过；补充“分组字号不得覆盖二级页标题”的失败用例后修复，并通过6项组件/真实列表/调整页测试；AI权限开关改用公共开关。截图脚本等待AI结果实际渲染后再展开，避免重绘关闭详情导致采集超时；最终55张采集与几何断言通过。
- [ ] 此批次历史回归45/47；成长分段触控已在上方组件扩展批次修复并通过。AI反馈测试整套运行时超时仍待诊断，不能当作随机波动忽略。
- [ ] 人工确认列表候选（只需看真实样板），再将导出参数冻结为批准版本；自动差异报告与参数版本门禁尚未完成。
- [x] 标题栏/分段/表单/目标管理列表已在上方组件扩展批次处理；专用内容与原生验证仍保持未完成。

### 2026-09-05：以32为基准的人工组件审查（待用户确认尺寸）

- [x] 同步远端：`codex/state-dimension-detail` 与上游0/0；保留已有未提交修改。使用CodeGraph核对设置、任务与目标入口。
- [x] 代码审计确认：32分组列表和27独立边框行的结构、文字与间距来源不同；02仍是独立只读行；08手动子任务仅在AI生成区域出现；13/23字段写死；20日期弹窗当前仅展示一条摘要。
- [x] 交付 `design/ui-tuner.html`：10类组件预览，右侧使用32真实CSS基准；字体、行高、宽度、间距、计数操作、复盘输入框、热力格均可实时调整；自动本地保存、JSON导入导出、复制参数；不会读取App数据库或请求AI。
- [x] 浏览器回归通过：10类预览×320/400/480宽度无整页横向滚动；同类任务行几何一致；参数重载恢复、JSON导出与导入、非法范围拦截、样例小标题增删改通过。审查页已截图复核。这些仅为原型验证，不代表App功能已完成。
- [x] 49 的“下周重点 / 小尝试”删除图标并合并为一个“下周建议”分组，完全复用 32 的分组列表结构；删除底部重复编辑按钮，仅保留一个“修改”入口进入 50。
- [x] 01/02/04普通列表、喝水到+1操作区、已完成和27设置组已接入公共基础组件；最终数值仍待用户确认。
- [x] 05目标列表已迁入公共基础行，不再保留专用长卡。
- [ ] 10重复日期组的图例到日期间隔仍需单独复核。
- [ ] 07按参考改回居中任务编辑弹窗；08创建目标直接手动添加/编辑/删除子任务，AI不可覆盖手动草稿；离线、取消、保存和编辑冲突需回归。
- [ ] 13增高复盘输入框并支持模板小标题增删改；23按参考的进展/资产分层建立可编辑周模板，需实现历史快照、持久化、备份恢复和旧固定字段兼容。
- [ ] 20提高记录/任务/习惯标记区分度，将放大镜移入搜索框、移除关闭查找，并让日期预览查看该日记录；21简化五维图形与徽章线条；11保留原页面，编号与日记/聊天区域需后续核对。
- [ ] 22压缩“我的周复盘”摘要，合并为单一“生成本周复盘”入口但保留授权；24删除热力图底部分隔、等比缩小方格与分类统计。
- [ ] 除上方已标记的列表迁移外，其余原型需求仍未迁入App。下一轮用用户导出的 `qiguang-ui-review.json` 作为尺寸输入；浏览器验证不能替代Android安全区与键盘验证。

### 后续需求：周期计划、提醒与总结看板

- [ ] 建立统一的计划看板，在一个总览中查看年计划、月计划和周计划，并可逐层进入对应周期；同一目标和任务只保存一份，通过时间范围聚合展示，避免形成三套重复数据。
- [ ] 在计划看板中加入事件提醒总览，集中展示近期节点、目标截止日期、子任务提醒和需要处理的逾期事项；支持按周、月、年筛选，但不与“今日任务”打卡入口混在一起。
- [ ] 在每周、每月和每年结束时生成对应的周期总结，采用类似“年度总结”的可回看页面，汇总已完成目标、任务、习惯、五维成长值和关键记录，并与上一周期比较变化。
- [ ] 周/月/年总结必须基于真实完成记录和用户确认的数据；AI只负责归纳亮点、变化与下一周期建议，不得虚构成果或自动修改计划。
- [ ] 周期总结支持保存历史快照和再次打开查看；年总结重点回答“今年完成了什么、在哪些维度成长、哪些计划没有完成、下一年值得延续什么”。

### 2026-09-04：指定页面逐项审计与比例收敛（进行中）

- [ ] 按实拍编号处理 05/06、08/09、10、16、20、22、24、28、29、33、34、38、39、45、48、49；目标/习惯分段、统一目标工作台、每日与每周打卡、图片编辑、独立 AI 确认页。
- [ ] 按同一逻辑视口测量字号、对齐、宽高、内外边距，保留鱼鱼/包包原素材；修复后独立实拍复核，不以代码测试替代像素审计。
- [ ] 复用上级 `docs/ui/` 的记录结构，隔离 Island Ledger 与 diary-maker；验证后的项目比例记入 PRODUCT.md 并由 UI 记忆引用，页面决定与用户反馈分别记录，不将估算值标记为已验证最优。
- [x] 2026-09-05 完成只读审计：已同步远端，当前分支 `codex/state-dimension-detail` 与远端一致；抽查 01、02、04、05、12、21、27、48 运行截图，并扫描前端文案、焦点样式、旧分类兼容字段与 PRODUCT/TODO/README 冲突。
- [x] 2026-09-05 完成逐页对比审计：按页面语义配对 56 张参考稿与 55 张运行截图，重点对比首屏层级、文字密度、方框压力、底栏一致性、用户路径和旧后台式表达。
- [x] 2026-09-05 建立 UI 组件模板：PRODUCT.md 固定页面标题、区块标题、条目标题、正文、辅助状态、底栏、按钮、分段标签和容器规格；DOM 审计实测修复前页面标题存在 28/27.52/24.8/20px 四档，按钮存在 15/14/12.8px 三档，已在 CSS 中收敛为全局 token。
- [x] 2026-09-05 完成改后视觉截图复核：临时截图目录为 `%TEMP%/qiguang-visual-review-1a034f76b3e44aa0bbcd126a9a8e156b`；核心页确认页面标题、区块标题、按钮、分段标签和主列表行视觉层级已统一，未发现本轮字体/组件规格回退。
- [x] 2026-09-05 完成缩小版视觉候选：临时截图目录为 `%TEMP%/qiguang-compact-review-6c6ea08c13d54bd1acb2bb2e512f5aae`；页面标题降至 24px，正文/按钮降至 13.5px，主列表行降至 52px 左右，并生成参考稿并排图确认核心页与二级页更接近参考稿的紧凑密度。
- [x] 2026-09-05 按用户反馈再缩小一档：页面标题 23px、区块标题 15.5px、条目标题 13.5px、正文/按钮 13px、辅助状态 11px；按钮触控高度仍保持 44px，主列表行约 50px，`npm run build` 通过。
- [x] 2026-09-05 完成同逻辑组件收敛：`今天要做` 与 `习惯打卡` 统一复用 `task-list-item` / `task-row-action`；任务页习惯管理与暂停习惯统一使用 `habit-list-row`，共用行高、操作列宽、内距和标题字号 token；删除 `today-habit-*`、`home-habit-checkin`、旧任务 CTA 与旧习惯行尺寸覆盖；补齐页面标题、区块标题、条目标题、正文、辅助状态、控件文字、页面间距、列表间距、行内间距、表单间距和容器尺寸的 token 分类；`npm run test` 与 `npm run build` 通过。
- [x] 2026-09-05 建立可人工调整的统一 UI 合约：新增 `src/design-system.css` 作为字体、间距、圆角、行高和公共组件的唯一调整入口；页面/弹窗标题、返回键、分段选择、横向筛选、按钮、表单、任务/习惯/管理列表行共用组件契约；习惯打卡通过 `TaskRowPresentation` 参数复用任务行，不再查找并替换内部 DOM；规范写入 `docs/UI_COMPONENTS.md` 并增加自动合约测试。
- [ ] 下一轮先收敛 05 任务计划、12 记录输入、27 设置、48 AI 候选四类页面：计划页仍偏后台列表，记录页底部输入控件存在压迫感，设置页把低频能力暴露过多，AI 候选页仍有测试夹具与候选卡片风格残留。
- [ ] 运行截图缺口：51 今日任务小组件、54 图片查看、56 AI 错误没有对应实拍；必须补采后再验收对应参考稿。
- [ ] 逐页对比后判定 05-07 计划、19-20 AI 发送/候选、22-24 日期回顾、28-32 周复盘、35-36 成就、43-44 AI 设置、45-47 数据安全、50 规则编辑为主要视觉债；先减少边框、合并重复说明、隐藏低频技术项，再做像素收敛。
- [ ] 清理普通用户流程中的说明书式文案：`整理范围与更新`、`确认范围`、`默认不会发送`、`服务端/同源中转`、`系统记忆/规则/依据` 等只保留在隐私确认、错误、危险操作和高级详情中。
- [ ] 统一文档旧逻辑：README 仍描述旧记录入口；TODO 中“四个等宽快速开头与单一滚动编辑器”的旧记录方案与当前“生活日记/每日复盘”冲突；PRODUCT 中“暂停房间互动”与日历快照、房间验收条目并存，需标明阶段边界。

### 2026-09-04：沿用鱼鱼与包包原素材

- [x] 核实今日、日期回顾、人物设置引用旧库 `character-frames/female|male/01-idle-front.png`，首次选择引用旧库 `avatar-female|male-cartoon.png`；没有替换人物资产或改动用户角色设置。
- [x] 在产品规则、设计清单和参考稿提示词中固定原素材来源，移除“白发人物”错误描述；参考稿中偏离鱼鱼/包包的形象不作为人物复刻依据。
- [ ] 旧参考 PNG 中的错误人物尚未逐图替换；后续制图须直接使用原素材，不生成替代角色。

### 2026-09-04：参考稿修订与 UI 重构（进行中）

- [x] 用户确认进入实现：统一正方形、底栏选中尺寸、删除重复范围说明，并用完整居中人物替代场景。
- [x] 修订 10 张受影响参考图；实现五项同尺寸底栏、完整居中人物、日历/分析正方形、记录输入框、五维列表与全屏详情/编辑骨架。发送预览正文允许撑高；目标状态框统一 22×22。
- [x] 生成 55 张隔离仿真数据运行截图与逐页对照入口：`design/screenshots/20260904/index.html`。采集脚本同时断言热力格、日历、习惯、目标状态框为正方形，发送正文不越出选择卡。
- [x] `npm run check` 通过；浏览器主流程与 30 天回归共 47 项通过；另补跑人物离线、320px/200% 字体、成长追溯 3 项通过。Android Emulator 构建、同签名覆盖安装、2 项仪器测试与离线冷启动通过。AI 使用本地合约响应，不代表真实 MiniMax 质量或可用性。
- [x] 第三轮修正后再次通过 `npm run check`（169 项通过、2 项条件跳过）、每日/周复盘与周建议采用 3 项定向浏览器回归、55 张采集几何断言，以及最新 APK 的模拟器构建、签名/安装、仪器测试和离线冷启动。
- [ ] 逐页对照参考稿收敛页面结构、字体、间距和二级页面；独立视觉审计尚未通过，不标记 1:1 完成。
- [ ] 独立审计第三轮为 REVISE，按三轮上限记录视觉 BLOCK。第三轮指出的预览溢出、目标状态框和两个长表单已修正并补拍，但未取得新的独立 PASS；AI 候选仍有旧式卡片，徽章、字体/图标及局部比例与参考稿有差异，55 张实拍未全部独立审完。
- [ ] 补齐真实 Android 键盘/返回草稿、桌面组件、图片预览及 AI 错误状态实拍；真实 MiniMax 端到端、系统分享与导入验收保持未完成，不以浏览器测试替代。
- [ ] 本轮仅保存阶段成果，不合并、不关机；整体 1:1 与全功能闭环验收保持未完成，待补齐上述审计与验证。

### 2026-09-04：全页面参考设计稿

- [x] 使用图像生成工具制作 56 张独立页面与关键状态参考稿，覆盖今日、自评、任务、目标、习惯、生活日记、每日/周复盘、日历、成长、分析、设置及异常反馈。
- [x] 统一审计入口位于 `design/reference-20260904/index.html`；支持编号/名称检索、打开原图和下载，附原生像素尺寸、设计要求及交付清单。
- [x] 按用户 2772×1280 手机规格采用竖屏宽 1280、高 2772 参考画布；工具原生输出为约 853×1844，并非精确目标尺寸，原图未被裁切或拉伸。
- [x] 已逐图检查并对人物身份、输入框、完成反馈、多习惯矩阵、成长徽章、重复操作和导航遗漏进行生成修订；仅交付设计稿，未修改应用代码、数据或签名。
- [ ] 用户逐页审计确认；生成稿的字体、图标、状态栏及局部间距仍有差异，热力格几何与示例统计不是业务或像素级验收结果，不能直接声明 1:1 还原完成。
- [x] 用户已确认进入实现；运行时验证与剩余项见上方本轮记录，不把静态参考图作为交互验收证据。

### 统一产品逻辑

- [x] 身体、心理、关系、工作/学习、玩乐成为全应用唯一分类。
- [x] 状态分数与成长值共用五维，但保持两个概念：状态可以升降，成长值只累计真实行动。
- [x] 新目标、任务、习惯、AI 分析和成长账本不再写入旧生活领域或成长分支。
- [x] 旧领域、旧分支、旧任务类型和旧容量数据仅用于导入与历史解释，不再影响新推荐和新结算。

### 今日与状态

- [x] 今日状态栏只展示五个当前分数；点击某一维进入该维详情。
- [x] 单维详情展示相关任务、AI 已确认记录与真实状态变化，并提供该维 6 题快速自评。
- [x] 设置同时提供单维快速自评与 30/60 题全部评估，不要求用户凭空估分。
- [x] 今日推荐只给出一个当前最值得做的下一步，不向用户展示内部任务类型。

### 目标、子任务与任务

- [x] 新建目标只填写目标名称和完成日期。
- [x] AI 只生成可编辑的子任务草案；每项包含日期、提醒、五维和难度，用户确认后才保存。
- [x] 手动添加子任务使用同一字段和同一任务实体，完成后可推进目标并结算里程碑。
- [x] 目标与阶段任务一次性保存；暂停目标会冻结未完成子任务，恢复时只恢复被暂停的任务，不会复活已取消或旧容量任务。
- [x] 任务页只分“今天 / 计划”：今天负责完成与打卡，计划只负责创建、安排、编辑、暂停、删除和分析。
- [x] 计划页未来任务与目标详情不再提供完成按钮；执行入口只保留在首页“今日”和任务页“今天”。
- [x] 历史日期只展示当日结果，不能从回顾页改写任务反馈。
- [x] 移除每日任务与习惯的隐藏数量上限；用户创建的待办不会因内部角色或容量被拒绝。
- [x] 任务分析按“全部 + 五维”查看，删除学习/生活二分、顶部重复时间选择和任务热力图图例。

### 记录、AI 与成长值

- [x] 记录页改为“生活日记 / 每日复盘”两个子页：生活日记使用消息流和底部输入栏，支持一张本地图片附件；每日复盘使用固定格式表单；AI 整理只在记录保存后提出待确认归档候选。
- [x] 保存后进入完整的当天回顾；“历年今天”与每日一句一一对应。
- [x] AI 把日记整理成五维事件、状态变化和 1—3 点成长值候选；所有候选均需用户确认。
- [x] 用户可以修改候选维度和成长值、确认、拒绝或撤销；重复操作保持幂等。
- [x] 同一现实行动已由任务结算时，日记只补充记录，不重复增加成长值。
- [x] 简单、普通、挑战任务分别结算 2、4、7 点；部分完成向上取半，里程碑首次真实完成增加 5 点。
- [x] 五维成长页展示累计、近 7 天、近 30 天和全部来源记录；徽章不再创建第二套积分。

### 本轮界面收敛

- [x] 设置、状态自评、AI 配置与周复盘删除平行的设置分组骨架，统一由 `listSection` 创建；一级页和二级页统一由 `pageHeader` 创建。
- [x] 删除名不副实的 `iconButton`；23 个普通入口统一由 `actionButton` 创建，日历翻月仅保留一个专用按钮构造器。
- [x] 删除与公共表单栈重复的 `ui-editor-fields`，任务调整快捷操作也改由统一操作组构造器生成。
- [x] 习惯详情的已完成禁用态回归 `actionButton`，生产代码中手写普通按钮骨架归零。
- [x] 页面右上文字操作、复盘标题、设置分组、空状态、AI 预览与详情小节继续复用现有公共构造器；删除对应平行 DOM 骨架。
- [x] 所有原生展开区统一由 `disclosure` 建立 `<details><summary>`；设置项、次要内容和溢出菜单只保留语义外观变体。
- [x] AI 建议、每日复盘、周复盘入口、逾期任务、记录搜索与行动说明书统一使用 `listSection`；图表、目标草案、伙伴选择等专用布局统一复用 `sectionHeading`。
- [x] 日期记录、行动结果和月度概览统一使用 `listSection`；日期预览与复盘调整统一复用 `sectionHeading`。
- [x] 周统计、任务分析、习惯详情与习惯分析统一使用 `metricItem + ui-metrics`，删除习惯详情的第二套指标分隔与间距规则。
- [x] 周复盘、任务分析、习惯详情和单习惯分析的指标容器进一步统一由 `metricGroup` 生成；页面只传标签、数值和既有视觉变体，不再各自拼装指标组 DOM。
- [x] 日历月份与周复盘周期统一使用 `periodNavigator`；删除两套页面专用箭头、居中、触控尺寸及响应式规则，统一为一套三列相对网格。
- [x] 初次引导与人物设置的头像选择统一使用 `avatarChoice`，图片、名称、按钮类型和 ARIA 选择状态不再重复拼装。
- [x] 任务结果、习惯完成方式与状态问卷答案统一使用 `choiceRow`，共享行高、内距、选择标记、选中态和 `aria-pressed`；30/60 题入口保留为导航卡片，避免混淆导航与单选语义。
- [x] 一级/二级页面与全屏弹层返回键统一使用 `backButton`，详情页和任务反馈标题栏操作统一使用 `titlebarAction`；删除习惯详情中创建后从未挂载的“分析”死按钮。
- [x] 删除目标与习惯详情中创建后从未挂载的两套元信息 DOM 及 33 条专用 CSS 声明；页面原有可见信息和行为不变，避免继续维护不可见组件。
- [x] 日历搜索反馈并入 `statusMessage`，删除与保存/异步反馈同尺寸同颜色的 `search-status` 平行样式。
- [x] 删除从未挂载的习惯总览摘要及其跨周期计算、DOM 和专用样式，避免维护不可见的第二套摘要组件。
- [x] 记录页按审核稿改为四个等宽快速开头与单一滚动编辑器；空内容禁用保存，草稿自动保存，主导航保持可见。
- [x] 当天回顾的“总览 / 记录 / 行动”改为真实分段；记录以完整正文方框展示，整框进入编辑、版本和删除详情。
- [x] 新增多习惯近四周总览；单习惯分析移除颜色图例和柱状图，未计划日期保留中性方块，星期结果用直接百分比色块展示。
- [x] 习惯详情最近记录使用带日期的 14 日正方形图；计数习惯继续按每日目标逐次打卡，达标后只结算一次成长值。
- [x] 成长页按统一纸张色调重排总览、五维行和成果列表；五维图标、标题、统计、进度与分隔线使用固定列对齐。
- [x] 目标详情使用统一小方框表达完成状态，并以克制的金色背景标出下一个待完成子任务。
- [x] 并列的标题、按钮、统计与进度组件使用同一字号、尺寸和对齐规则；紧凑来自删减重复信息，不压缩必要纵向间距。
- [x] 新设计页删除规律判断、鼓励语和功能解释等描述性段落，只保留标题、事实数据、状态与操作；文字与边框保持稳定留白。
- [x] 隐藏用户自建任务的“手动”来源；计划习惯卡改成单行本周概述；日期总览移除状态颜色条。
- [x] 记录摘要直接打开大尺寸可编辑详情，历史与删除收进次级菜单；单习惯星期完成率保持正方形。
- [x] 日期回顾新增四项手动每日复盘；本周页新增六项手动周复盘，均可随时修改并进入本地备份。
- [x] 手动复盘与 AI 整理保持独立，不自动改写任务、状态或成长值，也不被 AI 结果覆盖。
- [x] 删除状态、成长、习惯与目标中未再挂载的 20 个旧组件类；这些页面继续复用公共标题、列表、操作、指标和表单体系，不保留“幽灵皮肤”。

### 数据、隐私与迁移

- [x] IndexedDB 升级至 v7，备份格式升级至 v6，并兼容 v3—v6 备份。
- [x] 旧 `challenge` 难度迁移为 `hard`，历史成长值不回算、不丢失。
- [x] v6 原始任务和成长账本中的旧分类迁移为五维，旧容量任务不再出现在当前页面。
- [x] 旧 AI v1 结果（包括合约版本与模型输出版本混用的数据）只读或失效，不能自动修改状态；新 AI 事件一律待确认。
- [x] 从备份替换数据使用单次 IndexedDB 事务；写入失败时保留原数据。
- [x] 本机自定义 API Key 不进入 Web 产物、日志、备份或版本库。
- [x] 应用、Service Worker、Android 版本统一为 0.7.0（versionCode 700）。
- [x] 项目开发规则明确 Android 统一签名、密钥不得入库、证书核验和既有 Debug 安装迁移边界。

## 自动验证

- [x] TypeScript、Vite 构建、规则、AI 合约、数据库迁移、发布词汇与服务端测试通过。
- [x] 数据库 96/96、30 天确定性闭环 4/4、AI 离线评估 17/17 通过。
- [x] 完整 Playwright 主流程最终复跑。
- [x] 27 个真实 390dp 页面截图已刷新；指定页面经两轮独立只读视觉审查后 PASS。
- [x] Android Debug APK 本机重新构建成功，证书 SHA-256 与项目记录的既有 Debug 指纹一致。
- [x] 本轮 05、11、13、15、22 页面真实截图复核与独立视觉审查 PASS；完整浏览器回归、Android Debug 构建及签名核验通过。
- [x] 发布门禁、Android Debug APK 与模拟器门禁最终复跑；覆盖安装保留原应用数据。
- [x] 独立代码审计与提交前检查完成；当前发布提交包含本轮全部范围内变更。
- [x] 每日与周复盘的数据库、备份、编辑持久化和底栏安全回归通过；390×844 真实截图经两轮独立视觉审查后 PASS。
- [x] 完整浏览器用例全部通过；人物房间动画曾出现一次时序超时，单项复跑及其后全部剩余用例通过。
- [x] Android Debug APK 本机重建成功；发现并拒绝错误签名产物后，最终证书 SHA-256 与栖光历史 Debug 指纹一致。
- [x] 30 页统一视觉审计的任务新增入口遮挡、二级页标题骨架、每日复盘边距、目标 AI 双框和底栏透出均已修正；390×844 截图独立复审 PASS。
- [x] 任务、习惯与目标完成后显示紧凑上浮反馈，直接呈现完成状态与实际成长值；减少动态和徽章查看路径已通过独立视觉复审。

## 仍需真实环境或后续产品决策

- [ ] 真人连续使用 30 天，验证理解成本、压力感、信任和留存；自动模拟不能替代真人结论。
- [ ] 在统一真机验收批次验证重启、离线、后台限制、字体放大、深色模式和不同桌面尺寸。
- [ ] 如后续接入 Microsoft To Do，先由用户确认同步范围与冲突规则，本地成长账本继续作为唯一结算来源。
- [ ] 如后续接入小米运动健康，只读取用户主动授权的必要指标，不自动诊断，不默认上传日记或原始健康数据。
- [ ] 知识库连接优先验证用户主动触发的 Markdown 单向导出：Obsidian 写入用户选择的仓库目录；Notion 通过 OAuth 写入用户选择的数据库。双向同步待冲突、删除、隐私与离线规则明确后再评估。

## TEAM-RUN-20260902-01

| 角色 | 状态 | 本轮产出 | 交接 |
|---|---|---|---|
| 产品体验 | complete | 审计五个主页面、计划管理边界、全局术语与信息密度 | 审计项已并入实现 |
| AI / 数据工程 | complete | 五维模型、成长值、AI 确认、去重、v7 数据库与旧备份兼容 | 核心数据测试已通过 |
| QA / 发布 | complete | 更新合约、迁移、30 天、完整浏览器与 Android 模拟器回归 | 全部门禁通过 |
| 产品与交付负责人 | complete | 整合 PRODUCT/README/TODO、审计、版本与发布提交 | 本次提交推送收口 |

决定：前台保持轻量游戏反馈，后台使用可追溯成长账本；AI 只提出候选，用户保留最终决定权。
