# 栖光：产品逻辑与当前 TODO

> 文档状态：当前唯一产品方向与待办基线
>
> 更新日期：2026-08-20
>
> 当前版本：v0.6.4
>
> 当前阶段：从“日记、任务和成长功能集合”升级为“明确指导下一步的电子分身与现实成长系统”

本文档回答两个问题：栖光最终要成为什么，以及距离这个目标还缺什么。新增开发以本文档为准；历史视觉与像素交互参考集中在 `docs/archive-unused-md/`，技术架构、AI 合约和测试细节以实现代码与自动化测试为准。

## 1. 产品定义

### 1.1 产品品类

栖光不是普通日记、待办清单、打卡软件或聊天机器人，而是一个本地优先、证据驱动的个人现实成长系统。

### 1.2 一句话定位

> 栖光根据用户的目标、状态和真实反馈，每次明确告诉用户最值得做的下一步，并在长期帮助用户实现多元成长。

### 1.3 核心关系

栖光中的伙伴是用户的电子分身与成长导师：它了解用户、记住经过确认的经历、看见用户容易忽视的变化，在关键时刻指出下一步，并随着用户成长逐渐减少不必要的指导。

伙伴同时承担四个角色：

- 镜子：还原真实经历，区分事实、推断和未知。
- 参谋：综合目标、状态、限制和历史反馈判断方向。
- 教练：把方向缩小为一个能够立即执行的动作。
- 见证者：保存成长证据、有效方法和长期变化。

### 1.4 核心承诺

用户不需要先维护复杂体系，只需要持续提供真实信息。栖光负责：

1. 理解今天发生了什么。
2. 看见用户做成、推进、坚持或照顾好的事情。
3. 判断当前应该推进、维持、恢复还是探索。
4. 给出一个明确、可解释、可执行的下一步。
5. 把任务反馈转化为经验、成长证据和计划调整。
6. 在周、月尺度保持多元发展，而不是每天平均用力。

### 1.5 产品不是什么

- 不替用户决定人生价值或重要目标。
- 不用断签、负经验、角色受罚或任务欠账制造焦虑。
- 不给出没有证据、无法解释来源的权威结论。
- 不一次堆出大量平级建议，让用户重新承担选择成本。
- 不把人物、等级、房间和桌宠做成脱离现实成长的独立游戏。
- 不以提高打开次数或使用时长为最高目标。

### 1.6 易用性与留存原则

功能闭环和易用性不能分开验收。用户只有能够低成本完成“表达—得到下一步—执行—反馈—看见变化”，闭环才真正成立。

产品优先级固定为：

```text
核心闭环成立
> 用户无需学习即可完成
> 系统反馈清晰且可信
> 功能丰富度与表现层扩展
```

留存不依赖连续登录奖励或频繁提醒，而来自四件事：

- 首次使用很快获得真实价值。
- 每次打开都知道下一步做什么。
- 记录和反馈足够轻，不形成维护负担。
- 用户能够感觉到系统正在根据历史变得更懂自己。

任何需要阅读说明、理解内部模型或重复填写系统已经知道的信息才能使用的功能，都视为尚未完成，而不是“高级但完整”。

### 1.7 简化不等于删减能力

栖光采用渐进呈现，而不是通过删除必要能力获得表面简洁：

- 默认流程由系统自动填充，只要求用户确认当前最重要的内容。
- 用户始终可以进入详情，查看系统依据并修改目标、任务、习惯和状态。
- 低频字段放入“调整”或“高级设置”，不占据首次创建流程。
- 系统自动填写的内容必须可见、可改、可撤销，不能形成不可解释的黑箱。
- 已经创建的对象必须支持完整生命周期，不能只能新增、不能修改或结束。

界面复杂度的判断标准不是功能数量，而是用户在当前时刻必须同时理解和决定多少事情。

### 1.8 首版产品边界

首版只验证“表达—得到下一步—执行—反馈—看见变化”是否持续产生价值。保留本地优先、个人 Android 安装包内的 MiniMax 中国区直连、Android 小组件、月度证据视图和数据导出恢复；暂不引入账户、云同步、自建 HTTPS 中转服务、跨设备、应用商店发布、常驻悬浮桌宠、更多装饰系统、季度/年度报告或付费。只有真实模型、真机和留存证据成立后，才重新评估这些扩展。

## 2. 现实系统的基本对象

| 对象 | 回答的问题 | 例子 |
|---|---|---|
| 人生领域 | 我希望在哪些方面发展 | 健康、事业、关系、财富、兴趣 |
| 目标 | 我希望获得什么明确结果 | 十二周后连续跑完五公里 |
| 完成证据 | 怎样证明目标真的完成 | 一次连续五公里记录 |
| 里程碑 | 目标路径经过哪些阶段 | 连续三周每周运动三次 |
| 习惯 | 哪些行为需要稳定重复 | 每周二、四、六运动 |
| 任务 | 我现在具体要做什么 | 步行与慢跑交替十五分钟 |
| 状态 | 我今天有多少行动能力 | 精力低、心理一般、连接稳定 |
| 成长证据 | 我实际获得或表现出了什么 | 第一次连续跑完两公里 |
| 系统记忆 | 什么方法和限制对我长期有效 | 下班直接换衣服更容易开始 |

必须保持以下边界：

- 这些对象是系统内部的组织方式，不等于用户创建目标时需要填写的表单字段。
- 人生领域是用户可自定义的发展方向。
- 五维状态是近期传感器，不是人生目标，也不要求每天填写。
- 目标定义结果，里程碑定义路径，任务只定义当下动作。
- 习惯负责重复行为，不自动等同于某个长期目标。
- 成长证据必须来自记录、任务反馈、里程碑或用户确认。
- 系统记忆只保存用户能够查看、修改、拒绝或删除的内容。

## 3. 核心闭环

### 3.1 总闭环

```text
用户确定价值与目标
→ 系统帮助澄清并拆解路径
→ 状态和现实限制决定今天能做多少
→ 系统明确推荐一个 MAIN
→ 任务进入简单看板与伙伴小组件
→ 用户执行并勾选或口述反馈
→ 系统结算经验并形成成长证据
→ 系统更新对路径和用户的理解
→ 自动调整下一步
```

只有当本次反馈能够有理由地改变下次建议，这条闭环才算成立。

### 3.2 每日闭环

1. 打开“今日”后立即看到一个明确推荐，而不是先选择页面。
2. 推荐包含做什么、为什么现在做、完成标准、最低版本、预计时间和关联目标。
3. 用户可以接受、缩小、替换、暂不做或指出方向错误。
4. 接受后的 MAIN、BONUS 和支线进入简单任务看板。
5. 用户可以直接勾选完成；需要补充时可以口述“完成、部分完成、今天不做或豁免”。
6. 系统即时结算经验，提供撤销入口，并形成成功或阻力证据。
7. 结束今天时只保留关键事实、小小成功、未解决阻力和下一步。

### 3.3 周期闭环

- 每周判断各领域处于重点推进、稳定维持、恢复修复还是观察等待。
- 每周检查目标是否推进、习惯是否需要降难度、触发条件是否有效。
- 每月检查多元发展是否失衡，而不是要求每天照顾所有领域。
- 当证据显示路径无效时重新拆解目标；当目标不再重要时允许暂停或结束。

## 4. 目标自动拆解

### 4.1 用户输入

创建目标时，唯一必填内容是一句自然语言：

> 你想让什么事情发生？

用户可以直接输入或口述，例如“我想在三个月内跑完五公里”。为什么重要、时间范围、每周投入、当前基础和现实限制全部是可选补充，不得阻塞创建。

“关注领域”“唯一成长分支”“目标位置”等内部字段不得出现在默认创建表单中。系统根据目标内容和现有目标自动生成建议；无法可靠判断时允许暂不归类，不要求用户先理解产品模型。

如果目标过于模糊，大模型最多提出一个真正必要的澄清问题；能够合理假设时直接生成可编辑草案，并明确列出假设。没有连接 AI 时仍可先保存目标，并让用户只填写一个手动下一步。

### 4.2 大模型输出

一次拆解必须形成结构化候选：

1. 更清晰的目标结果。
2. 可验证的最终完成证据。
3. 2—5 个有先后关系的里程碑。
4. 当前阶段和当前阻塞点。
5. 一个可立即执行的下一步。
6. 下一步的完成标准、最低版本和预计时间。
7. 关键风险、假设和需要用户确认的地方。
8. 所属生活领域、主要成长方向和当前优先级的后台建议。

### 4.3 权限与落库

- 大模型只生成候选，不直接修改正式目标。
- 第一次确认只突出目标结果和当前下一步，不要求核对所有内部分类。
- 用户可以编辑目标结果、里程碑和下一步。
- 用户确认后才保存目标结构。
- 是否把下一步加入今天的 MAIN，由用户明确选择。
- 大模型不能自动启用 BONUS、修改目标优先级或改变截止时间。
- 所属领域和成长方向可以暂不归类，之后由系统建议或用户在高级设置中修改。
- 如果已经存在主目标，系统用自然语言询问“替换当前重点、作为次要方向，还是先保存”，不展示抽象的目标位置字段。

### 4.4 动态重规划

- 连续完成：进入下一动作或下一里程碑。
- 部分完成：保留有效进展，判断继续还是缩小。
- 多次未开始：识别时间、能力、环境、情绪或目标意愿问题。
- 状态过低：生成恢复任务或最低版本。
- 路径被证伪：提出重新拆解候选。
- 目标失去意义：建议暂停、修改或结束，但不得自动处理。

## 5. 今日方向与任务决策

### 5.1 决策输入

- 用户明确设置的目标优先级。
- 截止时间和里程碑依赖。
- 距离上次推进的时间。
- 最近任务反馈和反复阻力。
- 当天状态、可用时间和用户限制。
- 已启用 BONUS 习惯。
- 各人生领域近期投入与失衡情况。
- 用户确认过的有效方法和禁用建议。

### 5.2 决策顺序

1. 是否存在必须优先处理的恢复或安全问题。
2. 是否存在临近截止或阻塞其他工作的目标。
3. 当前重点目标的下一个未完成里程碑是什么。
4. 哪个动作是当前最小且真正可执行的推进。
5. 用户今天是否有能力和时间承担。
6. 其他领域是否需要最低限度维持。
7. 最近反馈是否要求改变原计划。

### 5.3 每日输出

- 1 个 MAIN：今天最值得完成的行动。
- 最多 3 个 BONUS：用户主动启用的维持习惯。
- 最多 2 个支线：可选、低压力、不与 MAIN 竞争的事项。
- 必要时 1 个恢复方案：用于替代不适合当前状态的行动。

每个 MAIN 必须展示：

```text
今天的方向
现在的下一步
为什么是现在
完成标准
最低版本
预计时间
关联目标与里程碑
完成后准备怎样继续
```

系统不默认展示三个平级建议。用户可以主动要求替换，但系统必须先承担推荐责任。

当前实现先用本地确定性优先级决定入口：恢复能力不足时优先恢复；已有待反馈 MAIN 时优先主线；否则使用当前重点目标、上一日最小步骤，最后才回到记录探索。每个入口都保留可见理由，AI 只负责候选内容，不绕过每日上限。

## 6. 简单任务看板与经验结算

### 6.1 看板目标

任务看板服务于执行，不承担复杂项目管理。用户打开后应立即看见当前任务，而不是先理解系统结构。

看板只展示：

- 当前 MAIN，始终置顶并突出。
- 已启用的 BONUS。
- 可选支线。
- 今日已完成任务，可折叠查看。
- 当前等级、经验进度和本次任务可获得的经验。

### 6.2 任务操作

- 点击任务查看理由、完成标准、最低版本、来源目标和预计时间。
- 点击复选框表示完成，并立即结算经验。
- 完成后提供短时间可见的撤销入口，防止误触。
- “部分完成、今天不做、豁免、缩小、替换”放入任务详情或次级入口，不挤占主看板。
- 勾选完成不强迫填写说明；用户愿意时可以口述补充实际结果。
- 已完成任务不自动重复结算，修改反馈后按账本差额重算。

### 6.3 经验规则

- 轻量任务完成 5 XP，部分完成向上取半。
- 标准任务完成 10 XP，部分完成向上取半。
- 困难任务完成 20 XP，部分完成向上取半。
- 挑战任务完成 40 XP，部分完成向上取半。
- 里程碑首次确认完成额外奖励 50 XP。
- 任务创建、编辑、连续登录和重复勾选不产生经验。
- 跳过、豁免、暂停、中断和撤销不扣为负数。
- 等级无上限；Lv.30 后每级固定需要 100 XP。
- 升级时先展示现实成长证据，再展示数字变化。

价值顺序必须保持为：

```text
现实证据 > 伙伴的具体反馈 > 能力与方法变化 > 经验数字
```

### 6.4 修改与状态更新

系统自动生成目标或任务以后，用户仍拥有完整控制权。

任务详情支持：

- 修改标题、完成标准、最低版本和预计时间。
- 调整难度、日期和 MAIN/支线位置。
- 查看并修改关联目标、里程碑和主要成长方向。
- 更新为待完成、完成、部分完成、今天不做或豁免。
- 缩小、替换、顺延、撤销结算或重新打开。

目标详情支持：

- 修改目标结果、原因、完成证据、里程碑和下一步。
- 调整当前重点、次要方向或暂存状态。
- 更新为构想、进行中、暂停、完成或结束。
- 请求系统基于新情况重新拆解，但正式变更仍需用户确认。

习惯详情支持：

- 修改名称、最低版本、频率和触发条件。
- 启用或移出 BONUS。
- 更新为培养中、暂停或结束。
- 保留历史反馈和经验账本，不因修改而删除过去证据。

状态更新不要求用户重新创建对象；所有修改都应保留审计与撤销所需的版本信息。

### 6.5 未按计划完成、晚完成与执行失败

系统必须区分“计划日期”和“硬截止时间”：

- 计划日期表示希望在哪一天行动，可以无惩罚调整。
- 硬截止时间表示错过后会产生现实后果，只在用户明确提供时存在。
- 未在计划日期完成不自动等于失败，也不扣经验。
- 超过硬截止时间后完成，应记录为“已完成，但晚于截止”，并评估对目标路径的影响。

计划日期结束后，待完成任务进入“等待处理”而不是自动失败、自动顺延或永久堆积。用户下次回来时，伙伴只询问一次：

> 这一步还没有结算，实际情况更接近哪一种？

默认选择：

- 已经完成：记录真实完成日期；如晚于截止，保留晚完成事实。
- 做了一部分：保留真实进展，并建议继续或缩小。
- 继续推进：由系统生成更小版本，再由用户确认是否安排到今天。
- 不再做：记录放弃、目标变化或已经失去意义，不产生惩罚。
- 外部原因：记录豁免，不把不可控情况解释为执行力问题。

失败后的重规划规则：

- 第一次未开始：优先提供缩小、顺延或放下，不追问复杂原因。
- 同一行动第二次未开始：询问一个必要阻力问题，例如任务过大、时间不足、精力不足、依赖未完成、忘记或方向不认同。
- 同一路径反复失败：停止重复生成同一任务，提出更换路径、暂停目标或重新拆解候选。
- BONUS 习惯错过后不顺延、不补课、不形成欠账；下个计划日生成新的独立任务。
- 旧 MAIN 未处理时不直接复制到今天；先处理一次，再决定继续、缩小或放下。

经验与证据规则：

- 晚完成的真实任务仍按实际难度获得正常经验，不设置迟到扣分。
- 经验和成功证据归入真实完成日期，计划历史保留原计划日期。
- 未完成、跳过、放弃和豁免不产生经验，也不产生负经验。
- 及时性作为独立事实和模式证据，不通过额外 XP 奖励诱导用户虚报完成时间。
- 系统只有在多次出现同类证据并经用户确认后，才能形成关于时间安排或执行方式的长期记忆。

## 7. 多元发展逻辑

### 7.1 长期平衡，短期聚焦

多元发展不等于每天在所有领域平均用力。系统采用四种领域策略：

- 重点推进：当前阶段集中资源增长。
- 稳定维持：使用低成本习惯保持已有水平。
- 恢复修复：先处理已经影响生活和执行能力的问题。
- 观察等待：暂不行动，但保留信息和重新评估时间。

每天只需要一个主要推进方向；多元平衡在周、月尺度上判断。

### 7.2 任务类型

- 推进任务：直接接近目标或里程碑。
- 维持任务：避免已有成果退化。
- 恢复任务：恢复精力、秩序或行动能力。
- 探索任务：在方向不清楚时获得信息并减少不确定性。

系统不能把“完成更多”当成唯一进步。休息、拒绝不合理需求、暂停错误方向和完成必要恢复都可以形成成功证据，但不为了安慰而虚构进展。

## 8. 成功日记与成长证据

成功日记不是独立打卡模块，而是整个系统的正向证据层。成功可以来自：

- 完成了具体行动。
- 推进了尚未完成的事情。
- 在困难中保持了最低行动。
- 识别并修正了错误方向。
- 照顾了身体和心理状态。
- 建立了边界或寻求了帮助。

系统不强制每天凑够数量，也不把推断当成成功事实。成功证据必须进入未来决策，例如引用过去有效的启动方法、发现持续进步的能力，或调整不适合的任务难度。

## 9. 伙伴、房间与桌面小组件

### 9.1 伙伴成长阶段

- 初识：多倾听、少判断，明确说明信息不足。
- 熟悉：引用多次出现且有证据的模式。
- 默契：复用用户确认有效的方法，并提前识别常见阻力。
- 成长：减少不必要指导，让用户自己完成已经掌握的判断。

关系阶段不能仅由登录天数决定，应由真实记录、用户确认和方法稳定性决定。

### 9.2 App 内伙伴互动

- 早上解释今天为什么选择这条主线。
- 用户记录时进入倾听状态。
- 用户卡住时帮助判断阻力并缩小行动。
- 完成后指出具体成功，而不是只说“做得很好”。
- 晚上整理事实、成功证据和下一步。
- 每周指出有证据的模式与领域失衡。
- 用户中断后欢迎回来，不追究断签，不堆积欠账。

### 9.3 桌面伙伴小组件

第一阶段按 Android 桌面小组件实现，不申请常驻悬浮窗权限，也不建立独立 PC 桌宠进程。

小组件提供三个尺寸：

- 小尺寸：伙伴当前姿态、MAIN 简称和完成复选框。
- 中尺寸：伙伴、完整 MAIN、最低版本、经验进度、完成与记录入口。
- 大尺寸：伙伴、MAIN、最多三个 BONUS、经验进度和打开任务看板入口。

核心交互：

- 点击任务正文打开 App 内任务详情。
- 点击复选框完成任务并结算经验，显示可撤销反馈。
- 点击伙伴显示一句基于当前方向的短指导，再次点击打开今日页。
- 点击记录入口直接进入文字输入，不自建语音识别。
- 任务或经验变化后及时刷新，不依赖高频后台轮询。

隐私与边界：

- 默认不在小组件展示日记正文、心情原文或系统记忆。
- 用户可以选择隐藏任务标题，仅显示“今日 MAIN 待完成”。
- 锁屏或他人可见场景不展示敏感目标内容。
- 离线时读取本机任务和经验；AI 不可用不影响查看与勾选。
- 人物动作克制并遵守系统减少动画设置，不持续耗电。

### 9.4 后续桌宠形态

Android 常驻悬浮桌宠、Windows/macOS 桌宠和跨设备同步只有在桌面小组件验证了使用价值后再决定。后续桌宠仍必须服务于当前任务、记录和伙伴反馈，不能变成独立养成游戏。

## 10. AI 与规则系统分工

### 10.1 大模型负责

- 理解自然语言目标和记录。
- 生成目标拆解候选。
- 识别可能的阻力、模式和成功证据。
- 把规则选择出的方向表达为清晰、自然的指导。
- 在证据不足时说明不确定性或提出一个必要问题。

### 10.2 确定性规则负责

- 每日任务数量上限。
- 目标、里程碑和任务生命周期。
- 优先级、截止时间、状态保护和冲突处理。
- XP 结算、撤销、去重和重算。
- 小组件状态同步和离线任务操作。
- 数据权限、版本、证据引用和用户确认。
- 防止模型自动修改目标、记忆、习惯和正式任务。

### 10.3 用户主权

- 用户拥有目标、价值和最终解释权。
- 重要判断必须可查看依据。
- 事实、推断和未知必须分开。
- 用户可以纠正、拒绝、删除和关闭个性化记忆。
- 健康、危机、法律和财务等高风险领域不提供越界权威结论。

## 11. 信息架构原则

用户每天只需要理解三个问题：

1. 今天最值得做什么。
2. 今天发生了什么。
3. 今天留下了什么。

主导航按用户使用频率安排为四项：

1. 今日：伙伴、当前方向、一个 MAIN、必要 BONUS、完成反馈和今日收束。
2. 任务：简单看板，以及目标、任务、习惯的查看、修改和状态管理。
3. 记录：自由输入事实、感受、小小成功和口述反馈。
4. 轨迹：日历、某日回顾、成功证据、成长变化和到期的周复盘。

其他功能不占用主导航：

- 个人与设置：通过头像或齿轮进入，管理伙伴、显示偏好和普通设置。
- 数据与隐私：管理 AI 权限、备份、导入、存储和删除。
- 高级系统：管理领域策略、确认记忆、成长分类和个性化规则。
- 桌面小组件：无需打开 App 即可查看当前方向、完成任务或开始记录。

页面内部采用“摘要—详情—高级设置”三级结构：

- 摘要只展示当前需要知道和操作的内容。
- 详情提供修改、状态更新、来源和系统依据。
- 高级设置保留完整字段，但默认折叠，不阻塞主要任务。

高级页面不能成为首次使用的必经流程。前七天优先形成“记录—接受下一步—勾选反馈—获得经验—看到调整”的最小习惯。

## 12. 当前逻辑审计

### 12.1 已经成立

- 本地记录、编辑、版本、日历和某日回顾。
- 成功日记能够聚合明确记录、完成/部分完成任务和有证据的 AI 肯定。
- 手动目标、里程碑、下一步、MAIN、支线和 BONUS。
- 创建目标时生成 2—5 个里程碑和下一步的 AI 候选，并由用户确认。
- 简单任务看板、任务详情、完成/部分完成/跳过/豁免、调整和撤销。
- XP、无限等级、成长分支和证据追溯。
- 五维状态、低状态恢复建议和无惩罚机制。
- 周复盘、系统记忆候选和发送前权限确认。
- 男生/女生原创角色、像素房间、时间动作、自然散步和家具交互。
- 离线 PWA、本地数据库、备份恢复、Android 原生壳和发布流水线。
- 个人 Android 安装包通过原生插件直连 MiniMax 中国区；密钥不进入 WebView JavaScript 或网页产物。

### 12.2 尚未成立

- Android 重启、离线、后台限制、桌面启动器、小组件和原生文件权限的统一真机验收；按用户决定后续集中执行。
- 新核心闭环在真人连续使用中的理解成本、压力感、信任和留存验证；十四天自动模拟不能代替主观证据。

### 12.3 现有功能复杂度审计

2026-08-19 对当前浏览器真实界面逐页审计，结论是：系统普遍把内部数据模型、分类和维护工作交给了用户，功能虽然完整，但认知成本已经阻碍核心闭环。

| 功能 | 当前可见复杂度 | 审计结论 |
|---|---:|---|
| 今日页 | 5 组标题、18 个按钮（含 2 个更新提示按钮） | 房间入口、开始/结束节奏、五维状态、快捷入口和底部导航重复争夺注意力 |
| 创建目标 | 7 个表单控件 | 严重过度设计；默认只应保留一句自然语言目标 |
| 手动安排任务 | 8 个表单控件 | 严重过度设计；理由、最低动作、时间、类型、难度、分支和状态不应全部由用户填写 |
| 创建习惯 | 15 个表单控件 | 最严重的创建流程；计划日、状态、分支、难度和 BONUS 应由系统建议或渐进设置 |
| 成长页 | 13 组标题并允许手动建立分支 | 应以查看真实证据为主，不要求普通用户设计资产树 |
| 我的系统 | 16 组标题、33 个输入控件、25 个按钮 | 实质上是管理后台，不适合作为普通用户主导航页面 |
| 周复盘确认 | 主题、假设、最小动作、指标、日期、停止条件 | 把实验设计工作交给用户；应由系统生成一个可接受或调整的候选 |
| 记录页 | 1 个主要输入、4 个提示入口 | 当前最接近正确方向，继续保持自由输入，不增加强制分类 |
| 日历与查找 | 月历、关键词、日期 | 复杂度合理，属于低频回看工具 |
| 备份、恢复与删除 | 多步确认 | 安全复杂度必要，不为追求简洁而删除保护 |

复杂度治理原则：用户只负责表达目标、经历和真实反馈；分类、拆解、优先级、难度建议、成长归属和后续安排由系统承担。安全确认、隐私授权和不可逆操作不适用“减少步骤”原则。

本次审计不主张删除目标编辑、任务调整、状态更新、领域管理或成长查看能力。需要删除的是默认流程中的强制决策和重复入口；完整能力保留在对象详情或高级设置中。

### 12.4 开发前剩余闭环审计

2026-08-19 对数据生命周期、AI 时效、离线降级和原生扩展再次审计，新增四项必须处理的结构性风险：

- 目标暂停、完成或结束后，关联的待完成任务和里程碑目前不会同步收束，可能继续出现在行动面。
- 目标、任务或系统记忆改变后，已生成但尚未接受的 AI 拆解与任务建议缺少统一的来源版本检查，可能应用过时计划。
- 自动指导是核心价值；MiniMax 不可用时由本地确定性方向与手动任务闭环兜底，不能让网络故障阻塞记录和行动。
- Android 桌面小组件无法直接可靠读取 WebView 内的 IndexedDB；实现前必须设计最小原生快照同步层，避免两套业务账本。

除此之外，现有隐私确认、不可逆操作保护、离线记录、备份校验和 XP 幂等账本可以继续沿用，不需要为了页面简化而重做。

## 13. 当前 TODO

以下顺序就是实施优先级。P0 完成并验证前，不扩展装饰性玩法或新平台。

### P0：让现实成长闭环成立

#### P0.1 统一产品语义

- [x] 将人生领域、近期状态、目标、里程碑、习惯、任务、成长证据和系统记忆落实为唯一产品词典；模型层保留唯一语义注释和字段边界。
- [x] 检查页面和数据模型中的重叠命名，保证同一概念只有一个含义。
- [x] 明确五维状态只用于近期能力判断，不承担全面发展目标管理；目标和成长证据仍走独立实体。
- [x] 明确每个任务的来源、关联目标、里程碑、任务类型和完成证据；Quest 显式保存 sourceType/sourceId/milestoneId，反馈记录完成证据并结算 XP。
- [x] 将领域、成长分支和目标位置定义为系统内部元数据，不再作为创建目标的必填项。
- [x] 数据模型允许目标暂不归类，不能为了满足数据库字段而强迫用户选择。
- [x] 建立“用户输入字段白名单”：只有无法从自然语言、历史或规则可靠获得的信息才允许成为必填项。
- [x] 所有内部字段提供未分类或系统默认状态，不能因技术模型要求阻塞用户行动。
- [x] 定义目标暂停、完成、结束时关联里程碑和待完成任务的收束规则，保留历史但不继续主动安排。

#### P0.2 升级目标自动拆解

- [x] 将创建目标收敛为一个自然语言必填输入，其他信息全部折叠为可选补充。
- [x] 移除默认表单中的“关注领域”“唯一成长分支”“目标位置”。
- [x] 由系统按目标文字确定性建议所属生活领域、主要成长方向和当前优先级；无法判断时保持未归类。
- [x] 已有主目标时使用“我现在就想推进它（当前重点）/我想同时照顾它（次要方向）/我先存下来，以后再做”的自然语言选择。
- [x] 在现有 2—5 个里程碑草案上增加最终完成证据、当前阶段、风险、假设和预计投入。
- [x] 为下一步保存完成标准、最低版本、预计时间和关联里程碑；普通任务默认以最小动作作为完成标准，目标跟进任务使用里程碑证据。
- [x] 支持逐项编辑并确认目标结构，不允许模型直接落库。
- [x] 支持根据执行证据发起重新拆解候选，而不是永久沿用首次计划。
- [x] 增加模糊目标、冲突目标、无截止目标、过大目标和不可执行建议的合约测试。
- [x] 目标、记忆或相关计划改变后，旧拆解候选必须失效或要求重新确认来源版本。
- [x] AI 不可用时提供一个可编辑的本地最小下一步，不让创建目标停在无行动状态。
- [x] 目标拆解草案绑定创建时的目标内容与分类指纹；用户修改来源后旧草案立即隐藏并要求重新生成。

#### P0.3 建立每日方向决策引擎

- [x] 建立本地确定性方向选择器，并为恢复、主线、目标、昨日步骤和探索返回可解释理由。
- [x] 用确定性规则筛选恢复优先、截止风险、里程碑依赖、停滞时间、状态容量和领域平衡。
- [x] 每日方向已纳入当前目标的待完成里程碑依赖，并在建议卡中说明“先推进下一里程碑”；恢复、截止风险和状态容量规则继续独立生效。
- [x] 已加入停滞识别：目标持续至少 7 天且近 7 天没有完成/部分完成证据时，只提示缩小或换路，不自动改变目标状态。
- [x] 每日方向选择器已纳入主线可选截止风险（截止已过或 24 小时内），并在理由中明确“仍由用户决定”。
- [x] 默认只展示一个明确的开始方向，并说明为什么是它；没有足够依据时明确进入探索记录。
- [x] MAIN 和任务卡提供最低版本、预计时间及完成后的反馈/下一步入口；完整完成标准仍可在详情中补充。
- [x] 保留 1 MAIN、3 BONUS、2 支线硬上限，不让 AI 绕过限制。
- [x] 任务板允许接受、缩小、替换、暂不做、顺延和指出方向不合适。
- [x] 没有足够依据时进入探索任务，不伪造确定方向。

#### P0.4 打通反馈到重规划

- [x] 完成后自动判断继续下一动作、完成里程碑或进入下一阶段。
- [x] 目标行动完成后，若仍有未完成里程碑，自动生成唯一且可编辑的下一里程碑任务；重复反馈不会重复生成。
- [x] 部分完成时保留真实进展，并生成继续或缩小候选。
- [x] 目标行动部分完成时，自动生成唯一的“缩小继续”候选；它仍受任务上限约束，可继续编辑或放下。
- [x] 多次未开始时询问一个必要阻力问题，不重复生成相同任务。
- [x] 同一目标行动再次未开始时要求记录一个主要阻力，并停止自动复制该目标行动，交由用户调整或暂停。
- [x] 状态过低时允许恢复任务替代推进任务，并说明原因。
- [x] 路径被证伪或目标失去意义时提出暂停、修改、结束或重新拆解候选。
- [x] 目标任务调整对话框已提供“编辑关联目标”和“暂停关联目标”入口；不自动删除历史，暂停后停止主动安排。
- [x] 每次调整显示使用了哪些事实和用户确认记忆。
- [x] 增加跨日端到端测试，证明今天的反馈确实改变明天的推荐。
- [x] 将计划日期与可选硬截止时间分离；同时保存真实完成时间和实际完成日期。
- [x] 任务支持可选硬截止时间，与计划日期和实际完成日期分离；截止后只显示事实，不自动失败、不扣经验，仍由用户决定。
- [x] 为过期但未反馈的 MAIN、支线和普通任务建立一次性“等待处理”入口。
- [x] 支持晚完成、部分完成、继续推进、不再做和外部原因五种处理路径。
- [x] 晚完成经验归入真实完成日期，保留原计划日期和实际完成日期事实。
- [x] BONUS 习惯错过后自动退出当前行动面，不顺延、不补课、不形成欠账。
- [x] BONUS 只按当天计划生成；错过日保留一次待处理事实，但不会复制到下一天、补课或形成欠账。
- [x] 同一行动第二次未开始时询问一个必要阻力；反复失败时停止复制并提出换路或暂停。
- [x] 已覆盖计划日跨越、硬截止跨越、晚完成 XP、BONUS 不积压、重复失败和中断回归测试。
- [x] 已覆盖硬截止字段保存与回读；完整的时间流逾期交互仍待真机/时钟场景补测。
- [x] 已覆盖晚完成日期、完成后下一里程碑唯一生成、目标行动跳过后的人工决策入口、硬截止事实展示和 BONUS 不积压；设备时钟与桌面启动器行为仍留待统一真机验收。

#### P0.5 收敛今日页与任务看板

- [x] 进入今日后优先看到“我建议你下一步做什么”，不先面对模块导航。
- [x] 今日页先展示单一“今天的节奏”建议卡，包含方向、理由、行动入口、最低版本；任务详情继续提供反馈和关联来源。
- [x] 核心动作只保留一个默认入口；房间热点只作游戏化快捷方式，底部导航只承担稳定全局导航，不再新增平级快捷区。
- [x] 今日页已删除底部重复快捷入口；记录和任务的默认行动入口由今日方向卡承担，底部导航与房间热点作为稳定辅助入口保留。
- [x] 五维状态默认收起为一句状态判断；只在需要校准时询问一个维度。
- [x] 今日页五维状态默认收起为一句判断和“查看五维状态与证据”；某日回顾仍直接显示完整状态证据。
- [x] 将当前任务、完成勾选、经验反馈和详情入口收敛为简单看板。
- [x] 任务板待处理行动使用 44px 触控安全的“完成”勾选；部分完成、跳过、详细反馈和调整仍在同一张卡片上。
- [x] 复选框完成后即时结算经验并提供撤销；复杂反馈保留在次级入口。
- [x] 勾选复用现有幂等 XP 账本并立即刷新为可撤销状态；复杂反馈仍保留在次级入口。
- [x] 手动任务默认只输入一句“我现在想做什么”，由系统补全理由、最低版本、预计时间、难度和成长归属；其余调整收入可选细节。
- [x] 创建习惯默认只输入一句想养成的行为；频率和最低版本由系统建议，触发条件、分支、难度、状态和 BONUS 放入确认或高级设置。
- [x] 创建习惯默认只要求一句“我想养成什么”，最低动作自动生成；频率、触发条件、成长归属、难度、状态和 BONUS 收入可选调整。
- [x] 为自动生成和手动创建的目标、任务、习惯统一提供详情页或详情抽屉。
- [x] 详情支持修改、状态更新、暂停/结束、重规划和历史保留，不因简化创建流程损失控制权。
- [x] 成长页默认只用于查看证据和变化；“建立成长分支”等根资产/父分支维护入口已收进“高级维护（可选）”。
- [x] 将“我的系统”退出主导航；伙伴面板提供设置入口，系统页按普通设置、数据与隐私、记忆/领域等渐进区块组织。
- [x] 伙伴承担解释和调整入口，不再主要充当导航按钮。
- [x] 结束今天时展示成功证据、未解决阻力和明天准备继续的一步。
- [x] 日终收束现在显示成功证据、未解决任务选择和已有复盘中的“明天准备继续的一步”；没有依据时明确允许不预先规划。
- [x] 保证 320px、200% 字体、键盘、屏幕阅读器和减少动画模式可用。

#### P0.6 重组功能页面

- [x] 将主导航调整为“今日、任务、记录、轨迹”四项。
- [x] 主导航已收敛为“今日、任务、记录、轨迹”四项；日历、某日回顾和周复盘仍可从轨迹上下文进入。
- [x] 将日历、某日回顾、成功证据、成长变化和周复盘整合进“轨迹”，通过页内分段和按时出现降低导航层级。
- [x] “轨迹”上下文已提供日历、成长证据和周复盘三段入口；旧路由保留，降低跨页面寻找成本。
- [x] 将目标、任务和习惯统一放入“任务”，默认看板优先，管理功能按需展开。
- [x] 将伙伴与普通设置放入头像入口，将数据与隐私独立成明确设置页。
- [x] 生活分身面板现在提供“设置与数据”入口；系统旧地址和房间状态入口继续兼容。
- [x] 将领域、记忆和成长树维护放入高级系统，不占主导航。
- [x] 为所有旧入口建立清晰迁移关系，避免同一功能在房间、快捷区和底部导航重复出现。
- [x] 已走查首次使用、每日执行、目标修改、任务反馈、习惯暂停、某日回顾、周复盘和备份八条路径，并有浏览器回归覆盖。

#### P0.7 接入真实 AI（个人 Android 直连）

- [x] `.env` 仅保存于本机且被 Git 忽略；Gradle 将密钥写入 Android 原生 `BuildConfig`，WebView JavaScript 不读取密钥。
- [x] 原生插件只允许 HTTPS MiniMax 中国区接口，在 Java 层添加鉴权，并限制超时和响应体大小。
- [x] 每日整理、目标拆解、任务反馈、周复盘共用同一严格合约、提示词、证据校验、安全门和最多三次格式修正。
- [x] 默认使用 MiniMax-M3 并关闭 thinking，避免 M2.x 思考内容耗尽输出配额；服务端代理仅保留为浏览器开发和未来分发兼容路径。
- [x] `npm run check:android-release` 已验证密钥不进入 Web 产物、同步 WebView 并成功构建 Android APK，不再要求自建 HTTPS 中转服务。
- [x] `npm run eval:ai:direct` 已使用真实 MiniMax 中国区接口完成 16 类、17 条评估，覆盖目标拆解、每日指导、任务反馈、周复盘、注入与敏感场景，结果 17/17。
- [x] 断网或服务失败时保留记录、手动目标、任务反馈和本地成功日记闭环。

#### P0.8 建立温和召回与数据安全提示

- [x] 设计可关闭的“今天留下一件真实事情”提醒。
- [x] 今日无记录时显示一次可关闭的“今天留下一件真实事情”提示；一句话即可，支持写一句或今天先不用。
- [x] 设计可关闭的“昨天的下一步要继续、缩小还是放下”提醒。
- [x] 有昨日复盘下一小步时显示一次可关闭决策提示；用户可去任务板继续/缩小/放下，不自动顺延。
- [x] 中断后任务只进入一次性“等待处理”入口，BONUS 不补课、不顺延，不展示断签或责备文案。
- [x] 增加定期本地备份提醒，说明持久存储不等于备份。
- [x] 有记录且超过 30 天未导出时显示可关闭的本地备份提醒；导出成功后重新计时。
- [x] 第一版不申请通知权限；后续只有在用户理解价值并主动开启后才允许请求。

### P1：让电子分身可以随时陪伴

#### P1.1 Android 桌面伙伴小组件

- [x] 完成小、中、大三种小组件布局和隐私模式。
- [x] 展示伙伴状态、当前 MAIN、最低版本、经验进度和最多三个 BONUS。
- [x] 支持从小组件勾选 MAIN、打开并定位当前任务、直接开始记录；具体反馈与撤销仍回到同一任务板完成。
- [x] 小组件完成任务复用同一结算账本，保证幂等、撤销和离线可用。
- [x] 任务、经验、头像和减少动画设置变化后正确刷新。
- [ ] 后续统一真机验收：重启、离线、后台限制、字体放大、深色模式和不同桌面尺寸；按用户决定不在当前开发批次执行。
- [x] 不申请常驻悬浮窗权限，不在第一版建立 PC 桌宠进程。
- [x] 完成 WebView IndexedDB 到原生小组件的版本化快照同步；原生只保存展示快照和一次性用户动作，不建立第二套业务数据库。
- [x] `qiguang.widget-snapshot.v1` 包含 MAIN、最多三个 BONUS、伙伴名称/状态、头像、XP 与减少动画设置；完成动作回到 WebView 复用现有结算链路。

#### P1.2 伙伴长期记忆与指导阶段

- [x] 将用户确认的目标、限制、有效方法、禁用建议和稳定偏好形成可查看的伙伴记忆。
- [x] 区分初识、熟悉、默契和成长阶段，不按登录天数机械升级。
- [x] 伙伴引用历史时展示证据日期，并允许用户纠正。
- [x] 支持温和、直接等指导强度偏好；只改变伙伴措辞，不改变事实、任务规则或安全边界。
- [x] 已被用户掌握的方法可由用户标记为减少提醒，仍可查看和恢复主动提醒。

#### P1.3 人物与房间反馈

- [x] 让倾听、思考、指导、庆祝、恢复和周复盘对应明确人物状态。
- [x] 主线存在时显示“指导”状态；倾听、思考、庆祝、恢复和放松状态已有真实回访、cue、时间或完成事件来源。
- [x] 生活分身状态由真实 cue、时间动作、回访和已确认庆祝事件驱动，显示“倾听/思考/恢复/庆祝/放松”等状态，不随机抽取情绪。
- [x] 表情、状态标签和语言响应具体事件，不用随机情绪假装理解用户。
- [x] 用累计成长证据解锁克制的永久房间变化，不依赖连续登录；完成里程碑后房间显示纪念物，累计三个升级样式。
- [x] 保持正式男女角色身份一致，并遵守透明、居中和减少动画要求；资产与减少动画浏览器回归已覆盖。

#### P1.4 多元发展与周期复盘

- [x] 支持给每个人生领域设置重点建设、稳定维持、探索观察或暂停等待；每日方向会尊重领域模式。
- [x] 周复盘检查各领域投入、恢复需求和目标停滞，并提出一个调整候选。
- [x] 月度视图回答哪些领域在进步、维持、退化或缺少证据。
- [x] 不用单一总分评价人生，不要求每天所有领域都增长；五维状态、成长证据和 XP 分离展示。
- [x] 周复盘默认只呈现一个主题和一个实验候选；用户可以接受、缩小或拒绝，不要求填写完整实验表。

#### P1.5 降低首次使用成本

- [x] 第一次使用只完成选择伙伴、留下一条记录并进入一个可确认的下一步，不要求先配置完整人生体系。
- [x] 目标、五维、成长分支和系统记忆按实际需要渐进出现，默认表单保持最小。
- [x] 首次价值体验不要求先配置完整人生体系；浏览器首次使用流程覆盖选择伙伴、记录和回看。
- [x] 无目标、目标过多、长期未记录和刚刚回归分别提供探索、名额提示、温和记录提醒和回归入口。

#### P1.6 十四天封闭使用

- [ ] 后续统一真机验收：使用真实 Android 构建完成新核心闭环回归；按用户决定不在当前开发批次执行。
- 当前 `adb devices -l` 未发现在线设备；APK、Lint 与签名已验证，连接真机后继续，不用模拟器替代真机结论。
- [x] 已提供 `npm run test:android-device` 一键真机门禁：只接受一台已授权真机、拒绝模拟器、保留数据覆盖安装、启动应用并运行原生仪器测试；人工观察项在命令结束时明确列出。
- [x] 已完成浏览器真实备份导出、删除和恢复演练；原生文件权限场景仍待 Android 真机。
- [x] 使用 `npm run test:14-day` 快速模拟 14 个日历日，覆盖记录、MAIN 接受/拒绝、任务反馈、成功证据、次日调整、低状态恢复和两天中断。
- [x] 模拟脚本复用正式 IndexedDB、方向规则、任务反馈、里程碑、XP、习惯和小组件快照，不建立测试专用业务实现。
- [x] 验证反馈引用历史并改变后续行动：完成进入下一里程碑，部分完成生成缩小继续，建议不合适时停止复制，恢复优先覆盖机械推进。
- [x] 验证两天中断不会生成 BONUS 欠账；回归时仅保留一个待处理 MAIN，处理后最终 pending debt 为 0。
- [x] 将 14 天模拟纳入常规 `npm run test`；当前结果为 14 个日历日、12 个活跃日、24 条任务反馈、4 个里程碑、279 XP。
- [x] 本轮按用户授权以确定性 14 天模拟代替真人连续使用；理解成本、压力感、信任和留存不伪装成已验证事实，转为上线前用户研究。
- [x] 自动验证任意一天只给出一个带理由、完成标准和最低版本的 MAIN，可作为“用户能否知道今天做什么”的功能代理。
- [x] 自动验证系统建议引用前序任务并产生有理由的变化；真人是否理解该理由仍保留在上一项访谈。
- [x] 自动验证任务看板和小组件快照直接暴露当前 MAIN、最低版本、XP 与完成/打开入口；真实操作成本留待 Android 真机观察。
- [x] 自动验证中断后无惩罚、无 BONUS 欠账、无批量补课，仅保留一个待处理 MAIN；主观压力感留待用户研究。
- [x] 固定对外区分语句：栖光不是记录或管理更多事情，而是根据目标、状态和真实反馈，持续告诉用户最值得做的下一步。
- [x] 封闭模拟结论：核心闭环继续，修复里程碑顺序；首版停止增加外围功能，先完成真机和上线前用户研究。

### P2：首版已作出的暂缓决定

- [x] 首版不建立账户和加密云同步，继续使用本地数据与导出恢复。
- [x] 首版不做跨设备连续使用；云同步验证后再讨论。
- [x] 真实 AI 直连和 Android 真机验收完成前不提交应用商店。
- [x] 首版只提供 Android 桌面小组件，不做常驻悬浮桌宠或 PC 桌宠。
- [x] 留存证据成立前不增加更多房间、服装和家具；保留现有里程碑纪念物即可。
- [x] 首版保留月度证据视图，不做季度和年度报告。
- [x] 真实 AI 成本与留存价值明确前不设计付费方案。

## 14. 验收标准

### 14.1 目标拆解

- 创建目标只有一句自然语言是必填项。
- 默认流程不要求用户理解或填写领域、成长分支和目标位置。
- 用户能在一分钟内从模糊目标得到可编辑方案。
- 方案包含 2—5 个可验证里程碑和一个立即可执行的下一步。
- 每个下一步都有完成标准、最低版本、预计时间和关联里程碑。
- 未经用户确认，不产生正式目标、任务、记忆或 BONUS。

### 14.2 每日指导与任务看板

- 打开今日后，无需进入其他页面就能知道下一步做什么。
- 默认只有一个明确 MAIN，且能够解释推荐依据。
- 用户可以一击接受，也可以缩小、替换或暂不做。
- 看板可以直接查看任务、勾选完成、结算经验和撤销。
- 自动生成的目标和任务可以进入详情修改，并能更新状态或请求重新规划。
- 状态不足时，系统能够合理选择恢复而不是机械推进。
- 用户能够只通过今日页或桌面小组件完成“查看下一步—反馈结果—看到结算”，不必进入高级页面。

### 14.3 易用性与留存

- 首次使用在三分钟内完成第一次真实记录并得到一个可执行的下一步。
- 目标、任务和习惯的默认创建流程都只有一个自然语言必填输入。
- 每个步骤默认只要求用户作出一个主要决定，不同时展示多个系统分类问题。
- 用户不需要理解领域、成长分支、状态归属、任务类型或难度即可开始行动。
- 系统已经拥有或能够合理推断的信息不重复询问；不确定内容允许暂不归类。
- 中断后返回不产生欠账、惩罚或批量清理压力，直接从当前情况重新判断下一步。
- 用户能够看见上一次反馈如何影响了本次推荐，这是长期留存成立的必要条件。
- 简化创建流程后，目标、任务和习惯的完整修改与状态管理能力仍然可达。

### 14.4 页面架构

- 主导航只保留今日、任务、记录和轨迹四个高频目的地。
- 用户能够在任务页完成查看、勾选、修改、暂停、结束和重新规划，不必进入系统页。
- 日历、成功日记、成长证据和周复盘在轨迹中形成连续时间视图。
- 普通设置、数据与隐私、高级系统职责分离，危险操作不与日常功能混排。
- 同一功能在默认界面只有一个主入口，房间表现不复制完整导航。

### 14.5 自适应闭环

- 完成、部分完成、未开始和方向错误会产生不同的后续安排。
- 同一失败任务不会在没有新判断的情况下重复出现。
- 用户能看见因为哪条反馈，明天发生了什么变化。
- 用户纠正推断后，旧推断不再继续影响决策。
- 过期任务不会消失、自动失败或无限堆积，而是通过一次轻量处理进入完成、缩小、放下或豁免路径。
- 晚完成记录真实完成日期，经验和成功证据不会错误写回原计划日期。
- BONUS 错过后不顺延，连续失败不会导致相同任务被机械复制。

### 14.6 多元发展

- 每天保持一个焦点，周/月视图能够发现长期领域失衡。
- 恢复、维持和探索能够成为正式方向，而不是推进任务的附属选项。
- 系统不会因某个领域缺少每日行动而制造负罪感。

### 14.7 伙伴与小组件

- 伙伴引用的个人事实都有来源或用户确认。
- 伙伴能够指出具体成功、真实阻力和明确下一步。
- 小组件无需打开 App 即可查看 MAIN、勾选完成或开始记录。
- 小组件不泄露日记和敏感目标内容，不依赖高频后台轮询。
- 用户中断后不会受到惩罚或欠账提示。
- 伙伴的长期目标是提升用户自主判断，而不是增加依赖。

### 14.8 工程与安全

- 本地记录、目标、任务、反馈、证据、经验和备份在离线状态可用。
- AI 服务失败不会阻塞本地核心功能。
- 全量检查、发布检查、AI 固定评估和浏览器端到端测试通过。
- Android 新版本完成发布配置、构建、安装、离线、小组件和备份真机回归。
- 用户可以查看、导出、导入和删除自己的全部数据。

## 15. 当前已完成基线

- [x] 等级无上限；Lv.30 后每级固定 100 XP。
- [x] 无断签、负经验、植物枯萎或离开惩罚。
- [x] 每日上限为 1 MAIN、3 BONUS、2 支线。
- [x] 用户可以主动把习惯设为 BONUS，并暂停、替换或结束。
- [x] 本地记录、日历、某日回顾、版本、草稿、导入、导出和删除。
- [x] 目标、里程碑、成长分支、任务反馈、XP 账本、撤销和重算。
- [x] 简单任务列表、任务详情、完成/部分完成/跳过/豁免和经验结算。
- [x] 成功日记、本地成功证据和有证据的 AI 每日整理候选。
- [x] 目标 AI 拆解第一版：2—5 个里程碑、下一步、发送确认和落库确认。
- [x] 2026-08-19 目标创建收敛为一句必填；说明、证据、下一步和系统分类收入可选设置，离线时也会生成并安排最小下一步。
- [x] 2026-08-19 文档结构收敛：根目录只保留工作基线，历史视觉与交互参考集中到 `docs/archive-unused-md/`，删除已被实现和当前产品逻辑取代的开发前草案。
- [x] 周复盘、系统记忆候选、事实/推断分离和权限预览。
- [x] 男生和女生原创头像、动作 Sprite、像素房间和时间状态动作。
- [x] PWA、IndexedDB、Service Worker、CSP、持久存储说明和离线降级。
- [x] Capacitor Android 原生壳、品牌图标、启动页、APK 和 GitHub 发布流水线。
- [x] 当前自动检查基线：`npm run check`、`npm run check:release`、AI 固定评估 17/17 和浏览器端到端流程 23/23 均通过；常规测试已包含 14 天闭环模拟。
- [x] 真实 MiniMax 中国区直连评估 17/17；Android 原生发布检查 13/13，APK 构建成功且 Web 产物不含密钥。
- [x] 当前容器镜像：`ghcr.io/vvoyager3/compound-journal-server:0.6.4`。
- [x] 当前 Android 包名：`com.vvoyager3.qiguang`，versionCode 604，targetSdk 36。

## 16. 当前明确不做

- iPhone/iOS 构建、真机回归和 App Store 上架。
- 自建录音和语音识别，继续使用系统输入法。
- 排行榜、社交、公会、抽卡、商店和虚拟货币。
- 自由地图、家具摆放、背包、战斗、NPC 和经营系统。
- 全局人生总分、断签、负经验和连续登录奖励。
- 未经用户确认的长期记忆、目标修改、正式任务或 BONUS 启用。
- 在十四天闭环验证前扩展大量装饰性图片和游戏平台能力。

## 17. 实施顺序

```text
统一产品语义
→ 升级目标拆解
→ 建立每日方向决策
→ 打通反馈与重规划
→ 收敛今日页与任务看板
→ 接入并验证个人 Android 真实 AI 直连
→ 增加温和召回与备份提醒
→ 实现 Android 桌面伙伴小组件
→ 建立伙伴连续记忆与关系反馈
→ 完成十四天封闭使用
→ 决定同步、商业化和平台扩展
```

下一阶段的完成标准不是增加页面，而是证明：用户每次提供真实反馈以后，系统给出的下一步都会发生有依据、可解释、可纠正的变化，并且用户能从 App 或桌面伙伴中低成本地查看、完成和继续行动。

## 18. 工作记录

### 2026-08-19：跨日任务处理与文档整理

- 已完成：待完成任务现在可按日期查询；今日页会展示一次“等待处理”入口，不自动失败、不扣经验、不顺延堆积。
- 已完成：用户可以直接选择已经完成、做了一部分、不再做或外部原因；也可以打开调整流程缩小后再安排。
- 已完成：反馈保存实际完成日期；晚完成 XP 记入实际完成日期，原计划日期仍保留在任务本身。
- 已完成：补充 IndexedDB 回归测试，覆盖等待处理、晚完成日期和 XP 账本日期。
- 已完成：历史 Markdown 集中到 `docs/archive-unused-md/`，根目录只保留 `TODO.md` 与项目规则文件。
- 验证：`npm run check` 通过（含新增跨日回归）；浏览器和 Android 真机待后续按 P0/P1 顺序验证。

### 2026-08-19：拆解候选与每日方向

- 已完成：目标拆解草案在用户修改目标结果、意义、完成证据或分类后自动失效，不能把旧候选继续落库。
- 已完成：每日页接入本地确定性方向选择器，明确恢复、当前 MAIN、当前目标、昨日步骤和探索的优先顺序与解释理由。
- 已完成：补充规则测试，并将目标拆解浏览器流程扩展为“生成 → 修改使草案失效 → 重新生成 → 确认落库”。

### 2026-08-19：手动任务入口简化

- 已完成：手动任务创建默认只要求一句“我现在想做什么”，系统提供理由、五分钟最小版本、预计时间、难度和成长分支默认值。
- 已完成：复杂调整字段收入“调整细节（可选）”，详情和后续编辑能力不变；同时修复拆解草案隐藏状态和键盘跳转的竞态。

### 2026-08-19：反馈驱动的下一步

- 已完成：完成目标行动后自动安排下一条未完成里程碑；使用目标、里程碑和行动指纹去重，不重复堆任务。
- 已完成：目标行动被跳过后不再机械复制；再次跳过时要求填写一个主要阻力，保留用户调整、缩小或暂停的决定权。
- 已完成：补充数据库回归，确认实际完成日期、经验结算和下一里程碑任务均可审计。
- 验证：`npm run test` 通过（16 合约、46 数据库、3 部署、10 发布检查含 2 项环境跳过、8 规则、6 服务端）；`npm run test:e2e` 通过（21/21）。

### 2026-08-19：部分完成与习惯入口简化

- 已完成：目标行动部分完成后留下部分经验和真实进展，并自动生成唯一的“缩小继续”候选；候选仍受每日任务上限约束。
- 已完成：创建习惯默认只要求一句想养成的行为；最低动作由系统生成，其余频率、触发、成长归属、难度、状态和 BONUS 设置收入可选调整。
- 验证：新增部分完成续行动数据库回归；`npm run test` 通过；`npm run test:e2e` 通过（21/21）。

### 2026-08-19：主导航收敛

- 已完成：底部主导航收敛为“今日、任务、记录、轨迹”四项；日历、某日回顾和周复盘仍保留在轨迹上下文与房间入口中。
- 已完成：路由兼容旧地址，进入日历、某日回顾或周复盘时仍正确高亮“轨迹”。
- 验证：增加主导航浏览器断言；`npm run test` 与 `npm run test:e2e` 均通过（21/21）。

### 2026-08-19：备份提醒与温和召回

- 已完成：有本地记录且超过 30 天未导出时，系统显示一次可关闭的备份提醒；导出成功后重新计时。
- 已完成：提醒文案明确持久存储不等于备份，不要求通知权限，也不制造连续登录或惩罚。
- 验证：`npm run test` 通过；新增“备份提醒可关闭”浏览器回归通过。

### 2026-08-19：桌面伙伴快照边界

- 已完成：新增版本化只读小组件快照，集中输出当前 MAIN、最多三个 BONUS、伙伴身份和 XP 等级。
- 已完成：快照写入浏览器/WebView 本地存储；不复制任务结算、XP 或状态规则，原生小组件后续只需读取并回到 App 执行业务操作。
- 验证：新增快照规则测试；`npm run test` 通过（规则测试 9 项）。

### 2026-08-19：任务板勾选完成

- 已完成：待处理任务卡增加触控安全的“完成”勾选，勾选后直接复用现有反馈、XP 账本和下一步重规划。
- 已完成：部分完成、跳过、详细反馈、调整/顺延仍保留为同卡片次级操作；完成后可直接撤销。
- 验证：更新任务反馈浏览器流程；`npm run test` 通过；`npm run test:e2e` 通过（22/22）。

### 2026-08-19：硬截止时间

- 已完成：任务可选设置硬截止时间，计划日、截止时间和实际完成日期分别保存。
- 已完成：截止后任务只显示“截止已过，仍由你决定”，不会自动失败、扣经验或顺延；调整入口可修改或清除截止时间。
- 验证：数据库保存/回读测试、构建和 `npm run test:e2e`（22/22）均通过。

### 2026-08-19：BONUS 不积压

- 已确认：BONUS 习惯只按当天计划生成；错过的日期保留待处理事实，但不会复制到下一天、补课或产生欠账。
- 验证：扩展习惯数据库回归，明确检查前一天待处理 BONUS 不影响今天生成；`npm run test` 通过。

### 2026-08-19：日终下一步

- 已完成：结束今天的收束对话现在显示已有复盘中的“明天准备继续的一步”；如果没有可靠依据，会明确告诉用户不必提前规划。
- 已完成：保留记录、任务反馈、成功证据和未解决选择四部分，下一步可以直接打开任务板决定。
- 验证：更新成功日记浏览器回归；对应流程通过。

### 2026-08-19：轨迹分段入口

- 已完成：日历、成长证据和周复盘页面增加统一的“轨迹”分段入口，并保留旧地址兼容。
- 已完成：当前分段使用明确焦点和 44px 触控区域，避免主导航重新膨胀。
- 验证：构建与核心页面响应式回归通过。

### 2026-08-19：五维状态渐进展示

- 已完成：今日页将五维状态收起为一句近期状态判断和可展开证据；某日回顾保留完整五维明细，校准入口不丢失。
- 验证：`npm run test` 通过；`npm run test:e2e` 通过（22/22）。

### 2026-08-19：截止风险进入每日方向

- 已完成：每日方向选择器识别主线截止已过或 24 小时内临近的情况，优先解释为什么先处理它。
- 已完成：风险提示不改变任务状态、不自动扣分，仍明确保留用户决定权。
- 验证：新增方向规则测试；`npm run test` 通过。

### 2026-08-19：今日方向截止排序

- 已完成：每日方向选择器把主线可选截止风险纳入优先级，截止已过或 24 小时内时给出更明确的开始理由。
- 已完成：风险排序只改变解释和入口，不改变任务状态及经验规则。
- 验证：方向规则测试与构建通过。

### 2026-08-19：今日温和记录提醒

- 已完成：今日没有记录时显示一次可关闭的“今天留下一件真实事情”提示；只需一句话，不要求分类或完整复盘。
- 已完成：提供“写一句”和“今天先不用”两个选择，提醒不会累积或责备。
- 验证：新增空今日页面浏览器回归通过；`npm run test` 通过。

### 2026-08-19：伙伴设置入口

- 已完成：生活分身面板增加“设置与数据”，普通设置和数据隐私不再需要从主导航进入。
- 已完成：四个伙伴动作在 320px 下改为两列布局，保持触控区域和可见性；状态旧入口继续兼容。
- 验证：伙伴面板浏览器回归通过；`npm run test` 通过。

### 2026-08-19：昨日下一步召回

- 已完成：有昨日复盘下一小步时，今日页显示一次可关闭决策提示，用户可去任务板继续、缩小或放下。
- 已完成：提示不会自动复制、顺延或形成欠账，保留用户对行动的最终决定。
- 验证：完整 `npm run test:e2e` 通过（22/22）。

### 2026-08-19：人物状态反馈

- 已完成：生活分身根据真实回访、恢复 cue、主线思考、时间动作和确认完成事件显示“倾听/恢复/思考/庆祝/放松”等状态标签。
- 已完成：状态来源是确定性事件和当前上下文，不随机模拟情绪。
- 验证：构建、规则测试和房间浏览器回归通过。

### 2026-08-19：任务管理入口审计

- 已确认：目标、里程碑、习惯和任务已统一在任务板管理，创建后的编辑、暂停/结束、重规划和历史保留入口均可用。
- 已完成：同步收敛 TODO 中对应重复未勾选项，避免同一能力被重复追踪。

### 2026-08-19：每日方向能力审计

- 已确认：今日方向选择器只给出一个带理由的开始方向；恢复、截止风险、MAIN、目标、昨日步骤和探索均按确定性优先级处理。
- 已确认：任务板保留每日硬上限，并提供完成、部分完成、跳过、缩小、替换、顺延和详细反馈入口。
- 已完成：同步收敛已被规则测试、数据库约束和浏览器流程证明的 P0.3 TODO 项。

### 2026-08-19：伙伴阶段与证据日期

- 已完成：系统记忆卡片直接列出证据发生日期，保留原有编辑、确认和遗忘入口。
- 已完成：根据已确认记忆数量与证据日期丰富度显示“初识/熟悉/默契/成长”阶段，不使用登录天数或连续打卡升级。
- 验证：`npm run test` 通过；完整 `npm run test:e2e` 通过（22/22）。

### 2026-08-19：目标分类建议

- 已完成：目标只填写一句话时，按明确关键词建议生活领域与成长方向；无法判断时不强行归类，用户仍可在可选设置中修改。
- 已完成：目标优先级选项改为面向用户的自然语言，并继续根据现有活动目标自动给出默认建议。
- 验证：构建与 `npm run test` 通过。

### 2026-08-19：P0/P1 路径收敛审计

- 已确认：今日页先给一个有理由的开始方向，任务详情和轨迹入口按需展开；系统设置不再占用主导航。
- 已确认：首次使用、长期中断、无目标和任务反馈路径均保留本地可执行闭环，逾期与 BONUS 不会累积成欠账。
- 验证：完整浏览器回归 22/22；`npm run test` 通过。

### 2026-08-19：里程碑依赖方向

- 已完成：今日方向读取当前重点目标的待完成里程碑；有依赖时在理由中明确说明，不再只显示泛化的“推进目标”。
- 验证：方向规则测试、`npm run test` 和完整 `npm run test:e2e`（22/22）通过。

### 2026-08-19：目标停滞提示

- 已完成：每日方向检查目标开始时间与近 7 天目标任务反馈；缺少推进证据时明确提示“先缩小一步”，不扣分、不自动结束目标。
- 验证：方向规则新增停滞分支测试；`npm run test` 通过。

### 2026-08-19：已验证项归档审计

- 已将 TODO 中已有明确实现、重复记录或已有浏览器证据的项目同步勾选，保留需要真实 Android、真实模型或长期使用才能证明的项目为未完成。
- 当前未勾选项不代表删除需求，而是明确区分“代码已实现”和“外部条件尚未验证”。

### 2026-08-19：领域模式进入每日方向

- 已完成：每日方向读取目标所属领域的阶段模式；暂停领域不再自动成为建议目标，维持和探索领域使用对应的指导理由。
- 验证：规则分支、构建和 `npm run test` 通过。

### 2026-08-19：周复盘领域候选

- 已完成：周复盘增加各领域本周推进证据统计；重点建设但无证据时只提出一个最小调整候选，支持去任务板安排或暂不调整。
- 已完成：保留原有单一主题与 ONE EXPERIMENT 结构，不将领域统计扩展成总分。
- 验证：构建、`npm run test` 和完整 `npm run test:e2e`（22/22）通过。

### 2026-08-19：今日入口收敛与键盘焦点

- 已完成：移除今日页底部重复的记录/任务快捷区，避免与方向卡重复；修复跳到主要内容时被路由标题自动聚焦覆盖的竞态。
- 验证：`npm run test` 与完整 `npm run test:e2e`（22/22）通过。

### 2026-08-19：任务里程碑来源

- 已完成：任务新增显式 `milestoneId` provenance；目标跟进任务、备份导入、数据校验和任务板显示均保留具体里程碑关系。
- 已完成：无效里程碑、跨目标里程碑或非目标任务关联会在数据库边界拒绝。
- 验证：数据库跟进任务测试、构建和 `npm run test` 通过。

### 2026-08-19：任务完成标准

- 已完成：Quest 增加 `completionCriteria`；任务板显示完成标准，普通任务自动采用最低版本，里程碑任务采用里程碑证据。
- 验证：构建、数据库回归、`npm run test` 和完整 `npm run test:e2e`（22/22）通过。

### 2026-08-19：目标路径调整入口

- 已完成：目标来源任务出现方向不合适时，可直接编辑关联目标或暂停目标；暂停需用户确认，保留任务、反馈和经验历史。
- 验证：构建与 `npm run test` 通过。

### 2026-08-19：成长页维护入口收敛

- 已完成：成长页默认展示成长证据、目标、习惯和经验变化；建立分支的结构维护入口移入可展开的高级维护区。
- 验证：构建与 `npm run test` 通过。

### 2026-08-19：里程碑房间纪念物

- 已完成：房间根据已完成里程碑数量显示克制的纪念物，达到三个里程碑切换样式；来源是累计成长证据，不是连续登录。
- 验证：构建与 `npm run test` 通过。

### 2026-08-19：人物指导状态

- 已完成：存在待处理 MAIN 时，生活分身显示“指导”状态；其余状态继续由回访、恢复 cue、时间动作和确认事件驱动。
- 验证：构建与 `npm run test` 通过。

### 2026-08-19：失败与跨日回归归档

- 已确认：重复跳过会要求阻力并停止机械复制；晚完成、硬截止、BONUS 不积压和中断路径均有数据库或浏览器回归覆盖。

### 2026-08-19：产品词典固化

- 已完成：在模型层固化领域、近期状态、目标、里程碑、习惯、任务、成长证据和系统记忆的唯一语义边界，避免把五维状态误当人生目标。
- 验证：类型检查、构建和既有数据/合约测试通过。

### 2026-08-19：指导方式偏好

- 已完成：系统设置增加“温和/直接”指导方式，旧数据默认温和；伙伴提示会按选择调整措辞。
- 已完成：指导方式纳入备份校验与恢复，非法值会被拒绝，事实、经验和安全规则不受影响。
- 验证：`npm run test` 通过；完整 `npm run test:e2e` 通过（22/22）。导入测试确认本地设置优先，不会被备份静默覆盖。

### 2026-08-20：Markdown 文档收敛

- 已完成：根目录只保留 `AGENTS.md` 与 `TODO.md`；历史视觉、线框和像素交互参考统一放入 `docs/archive-unused-md/design/`。
- 已清理：删除与现行实现、TODO 和自动化测试重复的旧产品规格、技术架构、AI 合约、开发计划与审查草案，避免后续误把历史稿当作开发基线。
- 已修正：保留的视觉参考文档改为指向当前资产目录，不再引用已删除的旧文档。

### 2026-08-20：目标拆解信息与月度领域变化

- 已完成：目标拆解严格合约补齐当前阶段、预计投入和关键风险；与既有最终完成证据、2—5 个里程碑、下一步及待确认假设一起在落库前展示，仍只生成可编辑候选。
- 已完成：日历增加月度领域变化，按任务反馈的真实完成日期比较本月与上月证据；区分进步、维持、退化、缺少证据和暂停观察，当前月不会因尚未结束而提前判为退化。
- 已修复：成长页“高级维护”在 320px 下达到 44px 触控高度，并让回归失败直接输出不合格元素。
- 验证：`npm run test` 全部通过；`npm run eval:ai` 离线 17/17；`npm run test:e2e` 完整 22/22 通过。

### 2026-08-20：产品术语收敛

- 已完成：用户界面统一使用“人生领域、成长方向、推进优先级、里程碑”；“近期状态、目标状态、任务结果”各自承担唯一含义。
- 已完成：数据模型为 `Area` 与 `GrowthBranch` 明确对应的用户语义；内部存储类型不做无收益重命名。
- 验证：发布候选测试新增旧界面术语扫描；`npm run test` 全部通过。

### 2026-08-20：执行证据驱动的目标重拆

- 已完成：活动目标可从目标卡发起重新拆解；发送预览逐条展示并允许取消勾选真实任务反馈，权限包络严格绑定任务 ID。
- 已完成：新路径仍需用户编辑并确认；确认后旧待办和未完成里程碑保留为“已被新计划替换”并退出行动面，不删除历史、不扣经验，新下一步可直接安排到今天。
- 已完成：旧里程碑不能再被误确认或结算；目标、旧路径收束和新里程碑写入同一事务。
- 验证：新增合约权限测试、数据库原子替换测试和浏览器完整重拆流程；`npm run test`、离线 AI 17/17 与完整浏览器回归 22/22 均通过。

### 2026-08-20：目标拆解边界与过期保护

- 已完成：严格拒绝缺少必要假设的模糊目标、未说明冲突的并行重点、凭空添加截止日期、把完整目标冒充下一步及其他不可执行草案。
- 已完成：目标、已确认记忆、关联任务或执行反馈在 AI 请求后发生变化时，旧草案不再显示或写入；数据库再以目标版本校验阻止并发覆盖。
- 验证：`npm run test` 全部通过（17 项合约、48 项数据库及其余发布/规则/服务端检查）；离线 AI 17/17；完整浏览器回归 22/22。

### 2026-08-20：指导闭环、伙伴记忆与 Android 小组件

- 已完成：每日方向按恢复、截止、里程碑、停滞、状态容量和领域证据确定性排序；目标任务完成会结算关联里程碑并生成不同的下一动作，撤销时同步回滚里程碑与经验。
- 已完成：不合适的建议可由用户选择编辑、重新拆解、暂停或结束；调整前展示任务事实、目标/里程碑依据、历史反馈与用户确认记忆，今天的反馈会改变次日推荐。
- 已完成：伙伴面板以解释当前方向为主，支持用户直接告诉伙伴限制、偏好和有效方法；已掌握内容可减少主动提醒，周复盘拥有独立人物状态。
- 已完成：Android 原生小组件具备小/中/大布局、隐私模式、MAIN/BONUS/经验/伙伴状态展示、完成 MAIN 与打开任务/记录入口；原生动作回到 WebView 复用同一任务反馈和 XP 账本，不申请通知或悬浮窗权限。
- 验证：`npm run check` 全部通过（17 项 AI 合约、49 项数据库及其余规则/发布/服务端检查）；离线 AI 17/17；发布检查 11/11（原生生产地址项按条件跳过）；完整浏览器回归 23/23；Android `assembleDebug` 使用 JDK 21 构建成功；`git diff --check` 无错误。
- 仍待外部验收：真实 HTTPS 模型服务、Android 小组件的不同桌面/后台/重启真机验证、完整新闭环真机回归和 14 天封闭使用。

### 2026-08-20：Android 小组件与发布链路收口

- 已完成：小组件“任务”动作携带当前 MAIN ID，应用启动后直接聚焦对应任务卡；完成动作仍使用原有反馈、里程碑和 XP 事务。
- 已完成：中、大布局从 APK 已打包资产读取用户选择的男/女角色头像，并按最长边 256px 解码，避免 RemoteViews 传输超限；缺失资产时安全回退应用图标。
- 已完成：`npm run check:android-release` 现在依次校验 HTTPS 根地址、生产包和发布规则，再执行 Capacitor 同步与 Android APK 构建；真实评估地址和可选部署层令牌已加入 `.env.example`。
- 已完成：清理新小组件的 Android Lint 警告，包括字符串资源、Manifest 顺序、API 31 单元格提示和有理由的同步动作写入；最终 Lint 为 0 errors、18 个既有 Capacitor/启动资源提示。
- 验证：`npm run check` 全部通过；离线 AI 17/17；发布检查 12/12（真实原生地址项按条件跳过）；浏览器端到端 23/23；带占位 HTTPS 根地址的一键原生发布检查 13/13 并成功构建；恢复无远程地址后 `lintDebug assembleDebug` 成功；`git diff --check` 无错误。

### 2026-08-20：外部验收阻塞复核

- 已复核：环境中没有真实 HTTPS 服务地址、模型服务端密钥或在线 Android 设备；这些条件连续三轮未变化。
- 当前不能代替真实证据勾选：线上部署与真实模型评估、Android 重启/后台/不同桌面真机验收、完整真机闭环和连续 14 天封闭使用。
- P2 项是封闭使用后由用户作出的产品决策，不以预先实现代替结论。

### 2026-08-20：十四天快速模拟

- 已完成：新增 `test/closed-use.test.mjs` 与 `npm run test:14-day`，在固定 14 日时间线上模拟目标、MAIN、BONUS、记录、完成/部分完成/跳过/豁免、低状态恢复、两天中断、回归、小组件快照和最终结算。
- 已发现并修复：里程碑过去没有稳定顺序，同一毫秒创建时 IndexedDB 可能按随机 UUID 选择“下一阶段”；现在新里程碑保存明确 `order`，列表与跟进任务统一按顺序读取，旧备份继续兼容并使用确定性回退。
- 模拟结果：14 个日历日、12 个活跃日、24 条任务反馈（完成 14、部分完成 3、跳过 5、豁免 2）、4 个里程碑完成、279 XP、最终待处理债务 0。
- 验证：`npm run test:14-day` 通过；`npm run check` 全部通过并自动包含该模拟；发布检查 12/12；完整浏览器端到端回归 23/23。
- 保留真人验收：脚本不能证明用户主观上是否易懂、无压力、愿意留存，也不能替代 Android 桌面启动器和后台限制真机测试。

### 2026-08-20：十四天模拟后的首版取舍

- 产品结论：14 天模拟证明核心数据闭环能够持续运行、反馈会改变后续行动、中断不会制造欠账，因此继续核心方向，不重做产品定位。
- 首版边界：只推进真实 AI 服务、Android 真机验收和上线前用户研究；账户、云同步、跨设备、商店发布、常驻桌宠、更多装饰、季度/年度报告和付费全部暂缓。
- 验收口径：自动化只证明功能行为；理解、压力、信任和留存仍需真人证据，不以脚本结果冒充主观体验。

### 2026-08-20：真机验收批次调整

- 用户决定暂不进行真机实验，重启、离线、后台限制、字体/深色模式、桌面尺寸、小组件和完整核心闭环统一留到后续真机验收批次。
- 当前批次继续以浏览器回归、Android 编译、Lint、APK 和远程 CI 作为工程证据，但不把它们写成真机通过。
- 远程证据：提交 `dca46d8` 的 GitHub Actions 运行 `32329605246` 全部通过；verify、服务容器构建/冒烟/发布、Android 同步/APK 构建与制品上传均成功。

### 2026-08-20：个人 Android 真实 AI 直连闭环

- 产品逻辑更新：个人自用版本不再要求部署 HTTPS 中转服务；Android 原生层持有本机 `.env` 注入的 MiniMax 密钥并直连中国区接口，WebView 只传严格合约请求，密钥不进入 JavaScript。
- 已完成：抽取服务端与原生端共用的 AI 引擎；每日整理、任务反馈、周复盘、系统候选与目标拆解使用同一提示词、解析、证据和安全边界。
- 已完成：增加 Android `QiguangAi` 原生插件、HTTPS/响应大小/超时限制、原生配置检测和本地功能降级；默认切换 MiniMax-M3 并关闭 thinking。
- 已完成：模型入口校正可证明的 Unicode 证据坐标，移除可选候选的未知字段，丢弃不完整任务候选，合并相同事实，处理证据不足周复盘，并对格式错误和可重试 5xx 做有界修正。
- 安全边界：`.env` 继续由 Git 忽略；发布检查确认真实密钥不在 Web 产物中；即时伤害表达先在本地进入求助流程，普通游戏化建议暂停。
- 验证：`npm run check` 全部通过；真实 `npm run eval:ai:direct` 16 类 17/17；浏览器端到端 23/23；`npm run check:release` 12/12（原生项按条件跳过）；`npm run check:android-release` 13/13 并以 JDK 21 完成 154 项 Android Debug 构建。
- 已修复：远程首轮容器冒烟发现运行时镜像未复制新共享 AI 引擎；Dockerfile 已补入该文件并增加发布回归，避免构建成功但容器启动即退出。
- 延期不冒充完成：尚未连接真机，因此安装、重启、后台限制、桌面小组件、原生文件权限与完整设备闭环继续保留为后续统一真机验收。

### 2026-08-20：首版完成态审计

- 已核对：首次使用、成功日记、目标拆解与确认、每日单一方向、任务反馈与经验、逾期处理、反馈驱动重规划、周/月轨迹、伙伴记忆、备份恢复、离线降级、Android 小组件和 MiniMax 原生直连均有正式实现及自动化证据。
- 已修正：删除“硬截止和 BONUS 不积压仍待实现”的过期描述；两项均已实现并进入数据库、十四天模拟或浏览器回归。
- 工程证据：本地与远程当前提交一致；GitHub Actions 的 verify、Android 与容器任务全绿，真实 MiniMax 中国区直连评估 17/17，浏览器端到端 23/23，Android Debug APK 已生成。
- 完成边界：无真机环境可完成的产品与工程功能已闭环；安装、重启、后台限制、桌面小组件、原生文件权限及不同桌面尺寸只能在用户安排的后续真机批次验收，不伪装为已通过。

### 2026-08-20：Android 真机验收入口收口

- 已清理：删除 Capacitor 自带的 `2 + 2` 与错误包名样例测试，替换为栖光正式包名、MiniMax HTTPS 地址和模型配置的原生单元测试，以及正式包名的真机仪器测试。
- 已完成：新增 `npm run test:android-device`；命令固定 D 盘缓存与 Android SDK/JDK 21，只接受一台真实设备，拒绝模拟器，以 `adb install -r` 保留本地数据，启动应用并执行 `connectedDebugAndroidTest`。
- 安全边界：覆盖安装签名不一致时立即停止并要求先导出备份；脚本不执行 `adb uninstall`，也不会在没有真机时继续构建或安装。个人调试签名固定保存在被 Git 忽略的 `.android-user/debug.keystore`，首次安装后必须与数据备份一起保管。
- 验证：无设备门禁自测通过；原生单元测试、AndroidTest Lint、完整 Lint 与 APK 构建通过（233 项 Gradle 任务，0 errors、18 个既有警告）；本机稳定调试签名生成并通过 APK 证书校验；`npm run check` 与 `npm run check:release` 全部通过；提交 `f2502f7` 的 GitHub Actions 运行 `32344055006` 中 verify、Android 和容器三项全绿，APK 制品已上传。
- 仍待用户安排：连接真机后运行该命令，并人工观察小/中/大组件、断网重启、后台回收、200% 字体、深色模式、减少动画、MiniMax 联网恢复和系统分享备份。

### 2026-08-20：GitHub 操作规则

- 已更新项目规则：GitHub 远端交互默认使用 `gh`；纯本地状态、差异、提交和分支操作继续使用 `git`，仅在 `gh` 无对应能力或失败时回退到 `git` 远端命令。
